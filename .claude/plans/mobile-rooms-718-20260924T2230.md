# Plan: the project room on a phone (kosmos #718, Josh's mobile priority 2026-09-24)

Owner: Kano (rooms + "waiting on you"). Plan and bar: #718 comment 5825987811.

## Finished looks like (this PR: the room)
At 375x667, 393x852, 430x932, 412x915, light and dark, Chromium and WebKit (WebKit is
Playwright's engine build, an approximation, not Safari). WHERE EACH IS MEASURED (as of
iteration 26): the committed check (render-room-msgbox-2806) runs under ENGINES=chromium,webkit
by hand; the gate (tools/browser-checks.sh) runs it in Chromium only. Its arms cover the phone
at 375 (touch, light; one dark arm), the room both lifted to a 297px box and IN PLACE beside the
real sticky composer, a 1180 touch tablet, a 1280 mouse desktop, and the one-screen layout's
Settings. Whole-page content past the screen is a COMMITTED arm since iteration 27: all six project
views at 375/393/430/412 and 360, both themes, both engines (iteration 29 added the other four
views and 360). Raiden's harness (--strict, not on main yet) supplies the screenshot sheets. The
earlier ad hoc audit (not committed) is superseded by both.
- no horizontal scroll, no clipped text (measured by the audit script);
- the conversation is the first thing on a project page;
- a long post reads as lines, not a column of two or three words;
- a reaction can be added by touch, with thumb-size targets;
- the composer placeholder stays on one line.

## Measured before -> after (Chromium / WebKit, same numbers)
Measured with the ad hoc phone audit (~/work/workers/kano/phone-audit.js: a sandboxed board
with seeded rooms, real 375/393/412/430 pages, both engines; not committed). The committed
check's phone arm renders the real #pj-room in a 297px stand-in and reads 199px there.
- bubble width at 375: 167px -> 215px; 393: 233; 412: 252; 430: 270 (Chromium and WebKit agree).
- tap on a message: add-reaction bar never shown -> shown (opacity 1, pointer-events
  auto), four 36x36 buttons. Control: without the tap handler, nothing shows.

## Decisions
- Phone rules scoped to #pj-room / the room column, at max-width 30rem, so the Direct
  Message thread (Scorpion's, shares .msg) and desktop are untouched.
- #3361 (Josh): a bubble never grows into the opposite avatar column. Kept by shrinking the
  avatar to 28px and mirroring the column (28px + 14px), not by dropping the gutter. The gap
  stays 14px: at 8px the tail's ground mask (.msg-bd::after) painted over the avatar's inner
  6px (Scorpion caught it on the DM; the phone arm now asserts the mask clears the avatar). The 2806 check
  gains a phone arm on the REAL #pj-room (its narrow arm renders a detached .thread that the
  scoped rules never reach, so it could not see this). Control: the old 48px gutter in the
  phone rule fails the new arm.
- Tap to react: a tap on a message body toggles .rxn-show on that one row, only under
  (hover: none). Rejected: always showing the bar on every message on a phone (clutter).
- Not touched here (other owners): page gutters and header (Raiden, app frame), the Direct
  Message thread and composer (Scorpion), safe areas (Raiden, shared frame).
- Link cards (.lpv): the grey 3px left bar becomes a full 1px border (SUPERSEDED in iteration 7: 0.5px, matching .att) (Liu Kang: Josh's rule
  covers any colour). Shared with the Direct Message; one owner (me), Scorpion told.

## Weakest part
Chromium does not ellipsize a textarea placeholder: it cuts it on one line without the
"...". It no longer spills a clipped second line, which was the defect, but it is not pretty.

## Screenshots
Every audit run sandboxes AGENT_WORKFORCE_HOME (#3675) and passes a privacy tripwire before
each shot (no email but the fixture's or a reserved domain, nothing key-shaped, not the
Mac's user name; a hit exits 3 and deletes the run). Control: a planted real-looking email
exits 3 with 0 PNGs. The first audit ran before this and was deleted unshared.

## Review iteration 1 (what changed, and what is measured vs reasoned)
- Sticky hover: phones keep :hover on the last tapped element, so the base .msg:hover reveal
  would open the bar on any tap and stop the second tap closing it. In (hover: none) the room
  now reveals only on .rxn-show or focus. REASONED from browser behaviour: Playwright's touch
  emulation does not reproduce sticky hover, so the check cannot see the defect or the fix;
  it measures the toggle itself (open, close, a file tap does not toggle, a mouse click
  toggles nothing).
- The bar (about 156px on a phone) is pinned to an agent bubble's LEFT, so it cannot cross
  the thread's left edge beside a short bubble; top -12px so the first post's bar is not cut.
- A picked reaction or a tap outside the room closes an open bar.
- The tap listener now sits before the data-open-agent listener, which asks to stay last.
- New arms in render-room-msgbox-2806: file name inside the ROOM (not the bubble: without
  the fix both grow together; control: card left -379 fails it), touch toggle open/close,
  file tap, mouse control, operator tail mask. Control: no toggle fails the touch arm.
- web.room-phone-718.test.js pins conversation-first and the touch rules. Control: order 0
  fails it.
- (SUPERSEDED by iteration 2: conversation-first is now a DOM move, not CSS order.) Tablets
  30rem to 68rem keep the old order.

## Review iteration 2
- BLOCKER, fixed: conversation-first was CSS order, the #1017 defect (reading order differs
  from tab order). Now a DOM move (pjPhoneOrder) at max-width 30rem, back after Members/Files
  when wider, keeping the room's scroll. The check measures the real DOM order at 375 and 800;
  control (no move at load) fails it.
- The phone arm keeps the thread's real max-height and overflow, and asserts the first post's
  reaction bar is not clipped by the thread (control: top -18px, bar at -5, fails it).
- The long-file-name fix is ONE rule, .msg-b .att-name, for the room and the Direct Message
  (both use .msg-b), Scorpion's exact declaration; the second of us to merge drops the copy.
- Reaction pills 36px on a phone (were 32). Touch targets stay 36px, under the 44px guidance
  for these small repeated controls; the Allow card's primary buttons are 44 (other branch).

## Review iteration 3
- The file-tap arm could not fail (the download link navigated the page away and a zero read
  from nothing passed): navigation is stopped, the tap is real, and the room must still hold
  its rows before the read.
- Turning a phone crosses 30rem and moves the column, which blurred the composer and closed
  the keyboard mid-post: pjPhoneOrder puts focus back (preventScroll). Arm: focus survives
  375 -> 800 -> 375; control (no refocus) fails it.
- A repaint rewrites the rows and dropped an open reaction bar: RXN_SHOW_POST plus a
  MutationObserver put it back on the same post. Arm; control (no re-apply) fails it.
- PJ_PHONE_MQ names the phone width once in the script (the CSS block is the third copy).
- Known limit, accepted: phones in LANDSCAPE (about 667 to 932 wide) are above 30rem and keep
  the Members/Files-first order, like tablets.

## Review iteration 4
- Coverage: a short post of the person's own (right-aligned; its bar keeps the right anchor)
  is tapped in the phone arm: its bar stays inside the thread, and every target in it and a
  reaction pill measure at least 36px. Control: dropping the phone size rules fails it.

## Review iteration 5
- A tap BETWEEN messages (the thread's padding) now closes the bar for good; before, it only
  hid it and the next repaint brought it back. Arm hit-tests a point whose topmost element is
  the thread itself (the first attempt tapped the header and passed vacuously; caught by its
  control). Control: the old early return fails it (afterGap 1).
- Picking a reaction blurs the picked button: a tapped button keeps focus on Android and
  .rxn-quick:focus-within would hold the bar up. Arm focuses then clicks a pick; control
  (no blur) fails it (focus in bar, opacity 1). REASONED for real Android; the arm forces the
  focus Playwright's tap does not give.
- PJ_TOUCH_MQ names the touch query; the second-tap arm no longer claims sticky-hover
  coverage (emulation cannot show sticky hover).

## After convergence: the long-file-name fix, corrected (Scorpion m605)
The file-name rule (.msg-b .att-name line clamp) was not the real fix: the person's own
bubble is content-sized in a right-aligned column, so a one-line name set its width. The
fix is to cap that bubble at its row: #pj-room .msg.you .msg-bd { max-width: min(78ch,
100%) }. Measured: with the cap alone, the 120-character name stays inside the room (the
2806 phone arm passes; with neither rule it fails at card left -379). The name keeps its
ellipsis. Scorpion made the same change scoped to the DM in #3687 and dropped his name rule,
so there is no shared rule any more and nothing for either of us to remove on merge.

## Review iteration 7 (the loop re-opened after the correction)
- WARNING, fixed: the phone reorder moved the conversation column, which holds the composer.
  Moving it blurs the composer, and the focus() put back afterwards runs outside a tap, where
  iOS does not bring the keyboard back: turning a phone mid-post (portrait 375 to landscape
  667+) would very likely have closed the keyboard while activeElement still said "composer".
  The Members/Files column now moves around it instead (same order both ways); the room and
  composer never leave the page, and the focus/scroll restore code is gone. New browser arm:
  a MutationObserver on the grid counts removals of the conversation column across a turn
  (must be 0, and the order must really have changed, or the arm is vacuous). REASONED for
  real iOS (no WebKit keyboard in Playwright); what is measured is that the node never moves.
- CONVENTION, fixed: PJ_PHONE_MQ / PJ_TOUCH_MQ repeat the CSS queries by value. A test now
  reads each constant and asserts the CSS block with exactly that query holds the room rules.
  Control: moving the CSS to 32rem fails it.
- WARNING, fixed: the "no CSS order" pin was vacuous if the block text changed. It now asserts
  the phone block was found, and bans order: on .pjmid/.pjsplit in any rule on the page.
  Control: order: -1 on .pjcol.pjmid fails it.
- NIT, fixed: sticky hover left the last-tapped bubble tinted; the touch block drops the tint
  unless the row's bar is open. Pinned; control (rule removed) fails.
- NIT, fixed: a tap on the shared emoji picker's padding (it lives outside the room) closed
  the bar while the picker stayed open. The outside-tap listener now ignores the picker.
- NIT, fixed: the link card went to 1px while the file card beside it is 0.5px. It now matches
  .att (0.5px), keeping only Josh's change (no left bar). Pinned; control (bar back) fails.
- NIT, fixed: RXN_SHOW_POST is declared before the listener that writes it.
- NIT, noted in the CSS: an agent's open bar briefly covers the bubble's top-left (its name).
- NIT, accepted: the geometry arms lift #pj-room to body at 297px, so the phone column padding
  is measured only by the uncommitted audit and the after-shots, not by a committed check.

## Review iteration 8 (sonnet, read-only: machine reserved)
- WARNING, fixed: the DOM-move pins were bare substrings, so swapping the phone and wide
  branches would still pass. The pin now matches the whole if/else. Control: the branches
  swapped fails it.
- WARNING, deferred (not an issue on reading): the room's click listeners must keep the
  file-open (refgo) handler first because web.win32-board-copy.test.js lifts the FIRST one.
  That test is itself the guard: a listener inserted above refgo makes it fail loudly. This
  branch adds its listener after refgo (checked by line order) and pins its own place before
  the data-open-agent one. What would change my mind: that test lifting by a pattern that a
  new listener could also match.
- NIT, accepted and already in Decisions: landscape phones (above 30rem) keep the
  desktop order. Raised to Liu Kang in the PR body for Josh's eye.
- NITs, no change: padding shorthand (standard CSS order), placeholder ellipsis (disclosed),
  28px gutter duplication (pinned at runtime by the far-equals-near arm).

## Review iteration 9 (opus, read-only: machine reserved)
- WARNING, fixed: at thumb size the open bar (about 44px, top -12px) covered most of a one-line
  bubble, so the tap meant to close it landed on an emoji and reacted. On a touchscreen the
  bar now sits wholly above the message (bottom: 100% + 4px); where the thread's top edge
  would cut it, pjRxnPlace adds .rxn-below and it opens under the message (the first post).
  New arms: the open bar does not cover its short bubble; a second tap on that bubble closes
  the bar with zero reactions sent (a spy on rxnToggle, with a project open so the pick path
  can actually reach it). The first-post clip arm now proves the flip. Controls pending the
  browser run (reservation): the old -12px rule, and pjRxnPlace removed.
  Trade-off named: the bar above now overlaps the bottom of the PREVIOUS message while open,
  so a tap there hits this bar; it is visible, and the bubble you tapped stays free.
- WARNING, fixed: iOS Safari sends no click to a document listener for a tap on plain
  content, so "a tap outside closes the bar" mostly never fired there. It listens for
  pointerdown now. Pinned. REASONED for real iOS (Playwright's touch emulation clicks anyway).
- WARNING, deferred: page-level no-horizontal-scroll at all four widths, dark, WebKit has no
  committed guard in THIS check (its geometry arms lift the thread to a 297px box). The
  committed guard is Raiden's shared harness (docs/browser-checks/mobile-shots.js, --strict
  fails on any overflow) on the real sandboxed board, project-room screen; I run it for the
  after-shots and link its report from the PR. Duplicating it here in a static page would be
  a weaker copy. What would change my mind: the harness not merging with this PR.
- NITs, fixed: a pick from the full picker closes the bar too; the open post is keyed with
  its project (post ids are per project); no grey iOS tap flash on the thread; the pjPhoneOrder
  comment says the THREAD's scroll is kept, not the page's; the rotation arm checks the order
  really changed in the same turn.
- NITs, no change: the DOM-move pin is whitespace-strict (fails closed); double-tap opens then
  closes (harmless).

## Review iteration 10 (sonnet, read-only)
- WARNING, fixed: the column that moves (Members/Files) holds buttons and links; a keyboard or
  VoiceOver user focused there lost focus on a turn. Focus is put back (buttons, not text
  fields, so no keyboard caveat). Arm: #pj-add-member keeps focus 375 -> 800 -> 375.
- WARNING, fixed: a reaction pill was 36px only in the phone block, so a phone turned sideways
  (touch, wider than 30rem) got desktop-size pills. The pill rule moved to the touch block.
- NITs, fixed: the bar comment names its anchor (.msg-b: the bubble and its reactions); a tap
  on an existing pill closes the bar like a pick; 30rem's reason is stated at the constant;
  why the outside listener and the observer are not touch-gated is stated.
- NIT, no change: GUT naming in the sibling arm.

## Browser run after the reservation (00:55 CDT onward, Chromium, headless)
render-room-msgbox-2806: all passed (every arm, including iterations 7 to 10's).
Controls, measured: the old -12px overlapping bar fails "does not cover its own short
bubble" (bar 372-408 over bubble 384-441); removing pjRxnPlace fails the first-post clip arm
(barTop -27); removing the Members/Files focus restore fails its arm (focus -> none).

## Review iteration 11 (opus, read-only; browser run by me)
- BLOCKER, real (MEASURED): every row's .msg-b is its own stacking context, so a bar opened
  BELOW its post (the first one) sat under the next message, and every emoji lost its tap to
  it. The earlier arms checked position and clicked from script, so they could not see it. New
  arm hit-tests each emoji's centre with elementFromPoint: before the fix hits
  [false,false,false,false], after [true x4]; the same arm on a bar ABOVE its post passes. Fix:
  the open row's .msg-b gets z-index 4 in the touch block. Pinned.
- WARNING, fixed: out of its bubble the bar lay over another message with no ground; it now has
  a surface, a hairline and a soft shadow (tokens, both themes).
- WARNING, fixed: placement compared only with the thread's top, but on a phone the page itself
  scrolls; it now also compares with the top of the screen (visualViewport). REASONED: the
  committed arm cannot put the thread off the top of a static page.
- WARNING, raised, not mine to change here: other solid left bars remain in the page (.dmoff,
  .msg-valve, .pj-msg.unsure, a --warn-border rule). The Allow card's is removed on
  push-tap-718. Sent to Liu Kang to route; noted for the PR body.
- CONVENTION, fixed: the outside listener now returns at once unless a bar is open, as its
  comment says; the comment also says a page scroll starting outside closes the bar.
- NIT, fixed: links and buttons in the room keep an iOS tap flash; only the thread's own
  (whole-thread) flash is removed.
- NITs, no change: (hover: none) vs (pointer: coarse) (a hover-reporting phone falls back to
  the hover reveal, still reachable); the thread's 26rem min-height on a 667px phone is the
  app frame's (Raiden); no text field exists in Members/Files today (the reviewer checked).

## Review iteration 12 (sonnet, read-only)
- NIT, fixed: closing the bar leaves no .rxn-below behind.
- CONVENTION, fixed: the room's own-bubble cap repeats the base 78ch; pinned equal. Control:
  80ch fails it.
- Already in the ledger: 36px (not 44) for the small repeated reaction targets; other left
  bars (now kosmos#3692, routed by Liu Kang). No bottom-edge flip needed (reviewer checked).

## Review iteration 13 (opus, read-only, post-rebase; browser runs by me)
- WARNING, fixed: the open row's lift (z 4) outranked the sticky room composer (z 2), so the
  newest post, the one most likely to get a reaction, could paint over the text field. Now z 1
  (neighbours are z auto); a pin reads both values and requires lift < composer. Control:
  z 4 fails it. The below-bar hit arm still passes at z 1.
- WARNING, fixed: placement ignored the sticky app header and the composer. pjRxnVisibleBand
  is the part of the thread a person can see (thread box, screen, sticky header, composer).
- WARNING, fixed (MEASURED): on a post taller than the thread neither side fits; the bar is now
  pinned (position fixed) to the top of the visible thread. New arm: a post scrolled to its
  middle and tapped; the bar is in view and all four emoji take their taps
  ({"inView":true,"pinned":true}). Control: no pin fails it (bar at 1633, far off screen).
- WARNING, fixed: iOS zooms into a field under 16px and pans sideways; the room composer and
  its @mention mirror are 16px on a touchscreen, pinned equal. REASONED for real iOS
  (Playwright WebKit does not zoom).
- WARNING, routed (not this branch's code): main's first-visit tip (#3574) anchors at
  #pj-add-member, which on a phone now sits below the room; tipPlace does not check the
  bottom edge, and its text lists Members before Conversation. Sent to Liu Kang.
- WARNING, routed: more left bars for kosmos#3692 (.msg .quoteb, .rst-list li). Sent.
- WARNING, routed: the Direct Message has no touch path for reactions (its bar is reachable
  only by sticky hover). Scorpion's screen; told him. In the PR body.
- NITs, fixed: a row with no post id no longer opens (and lifts) a bar; a focused composer is
  kept in view after the phone turns; the tap flash colour is the neutral hover tint (both
  themes).
- CONVENTION, fixed: measured numbers moved out of code comments (they live here).
- NITs, no change: pins are source pins (the behaviour is in the browser check); coverage
  runs in a lifted 297px room, Chromium, light; the harness shots cover sizes, themes, WebKit.

## Two fixes asked for by colleagues (after iteration 13)
- Projects list overflow at 375 (Raiden m603; Raiden's harness, before on main: projects,
  se, Chromium, .panel 330 > 327, both themes). Cause: at 56rem and below the row is two auto
  columns (Add Project | sort + view toggle), each at its natural width, 3px over. At phone
  width (30rem) the right cell may shrink and the sort's select truncates its label. Probed on
  a kept harness board: no overflowing element after the change. Pinned; the harness
  after-shots are the behaviour check.
- The first-visit project tip (#3574, on main) anchored at #pj-add-member, which on a phone
  now sits below the room (this branch's reorder), so its card fell off the screen. Liu Kang
  (m673/m676): fix it here. tipPlace takes the first on-screen match in PAGE order, so the
  anchor list now names the conversation header first; on a phone that is first in the page,
  wider Add member is. Arm: at 375 the card is inside the screen at the conversation; at 800
  it still anchors at Add member. Control, measured: the old anchor puts the card at 817-1036
  on an 800px screen. The tip's wording (Members listed first) is #3574's owner's (Liu Kang
  commented there).

## Review iteration 14 (sonnet, read-only)
- WARNING, fixed: the sort select's text-overflow had no overflow: hidden (nor nowrap), so a
  shortened label would be cut with no ellipsis (clipped text). Both added; pinned.
  (SUPERSEDED after the after-sheets: the sort never shrinks now and may wrap; no ellipsis rule.)
- CONVENTION, fixed: the projects-row block's 30rem is a third copy of the phone width; now
  pinned equal to PJ_PHONE_MQ. Control: 32rem fails it.
- NIT, fixed: pjPhoneOrder says why the third child (Tasks) stays last.
- NIT, not changed: the first commit's subject "#718 mobile: ..." (history, not code).

## Review iteration 15 (opus, read-only; browser runs by me)
- WARNING, fixed (MEASURED): a pinned (position fixed) bar stayed put while the thread or page
  scrolled, floating over other posts but reacting to its own. Any scroll or resize now closes
  a pinned bar, the same rule as the fixed emoji picker (#2834). Arm: pin, scroll the thread,
  bar closed with no inline position left. Control: the scroll listener removed fails it
  ({"shown":1,"pinnedLeft":true}).
- WARNING, fixed: a repaint re-applied the bar through the observer and could pin it over a post
  no longer on screen (inverted visible band). Only a tap may pin now; the observer path closes
  the bar when the post is out of view or would need pinning. REASONED (no arm: the static page
  cannot put the thread above the viewport with a live repaint).
- NITs, fixed: closing a row (switching rows, a second tap) clears its placement too
  (pjRxnResetRow); pjView (navigation) closes the bar; the tall-post arm asserts all four hits
  and the pinned state; RXN_BAR_GAP_PX names the 4px gap, pinned to the CSS calc; the tip and
  pjPhoneOrder comments now match the code; touch-action: manipulation on the thread (no iOS
  double-tap zoom for open-then-close); the sort pin's message no longer promises an ellipsis
  on every engine (what stops the overflow is the select shrinking).

## Review iteration 16 (sonnet, read-only; browser run by me)
- NIT, fixed (MEASURED): a tap on a link, button or name in ANOTHER row returned before the
  "one bar at a time" close, so an open bar stayed up. It now closes other rows' bars and leaves
  the link its own job. Arm: open a bar, tap a link in another row, no bar open. Control: the
  close removed fails it (shown 1).
- Everything else it raised is already in the ledger (other left bars on #3692; the DM's touch
  path is Scorpion's, agreed in m671; the first commit subject).

## Review iteration 17 (opus, read-only; browser runs by me)
- WARNING, fixed: iOS zooms any field under 16px, and the room's search, its who picker and the
  projects sort were 13px. All 16px on a touchscreen; pinned. REASONED for real iOS.
- WARNING, fixed (MEASURED): the stacking and pinning arms all ran on a room lifted to body, so
  they could not see its real ancestors or the sticky composer. New IN-PLACE arm: the room stays
  in its column, a short screen puts the sticky composer over the thread, a post that runs under
  it is tapped; every emoji takes its tap and the composer stays on top (precondition asserted:
  the open row really overlaps the composer). Control: the old z 4 fails it
  (composerOnTop false). The first version of this arm was vacuous (the row never overlapped the
  composer; its z 4 control passed), caught by that control and reworked with the overlap
  asserted.
- WARNING, fixed (MEASURED): a pinned bar closed at the next repaint (a new post) even with its
  post still on screen. A bar a tap pinned may re-pin on a repaint (a scroll would already have
  closed it). Arm: pinned, repaint, still pinned. Control: never re-pin fails it.
- CONVENTIONS, fixed: the sort comment no longer promises an ellipsis on every engine; the
  repaint comment sits on the observer; the tap listener's header names the other-row close.
- NITs, fixed: pjView's guard is a try (the real hazard is load order, not a missing function);
  a long comment line rewrapped; an inline comment moved above its rule.
- NIT, corrected here: the bar's shadow is a raw rgba (not a token); on the dark theme the
  hairline, a token, is what outlines it.
- NITs, no change: source pins carry text, the browser arms carry behaviour; the first commit's
  subject.

## Review iteration 18 (sonnet, read-only; browser run by me)
- WARNING, fixed (MEASURED): when nothing of the thread shows (a very short window, sticky
  header and composer taking the rest), a TAPPED bar returned early and stayed open with no
  placement. Now it closes, tap or not. Arm (a stubbed empty band): the tapped bar closes.
  Control: the old guard (close only when not a tap) fails it (shownAfter true).
- NIT, fixed: iOS reports the keyboard and pinch zoom on window.visualViewport, not a window
  resize; its resize and scroll now close a pinned bar too. REASONED (Playwright does not raise
  visualViewport events).
- Already in the ledger: the first commit's subject.

## Review iteration 19 (opus, read-only; browser runs by me)
- WARNING, fixed by narrowing the claim: "every field on these pages is 16px" was not true. The
  Tasks column's New task inputs (.tk-inp), the part assignee (.tkwho-sel) and add-member's
  #pj-one-add are still 13px. Those are the Tasks and members surfaces, not the room: the comment
  now names only the room's fields and the projects sort, and the rest is routed to Liu Kang.
- WARNING, fixed: the "link in another row" arm never checked a bar was open first. It now
  asserts exactly one open bar, on another row, right before the tap (openBefore 1).
- WARNING, fixed (MEASURED): scrolling inside the full emoji picker closed a pinned bar; the
  scroll closer ignores the picker, as the picker's own closer does (#2834). Arm: a scroll
  targeted at the picker keeps the pinned bar. Control: the guard removed fails it.
- CONVENTION, fixed: the "stays last" pin anchors on the data-open-agent listener's code, not its
  comment; the phone block also bans grid-row/column/area on the two columns (control: grid-row
  on .pjcol.pjmid fails it).
- NITs, fixed: RXN_PINNED clears when a re-placement lands above or below; a row with no bar
  closes instead of staying lifted; a repaint's re-applied bar has no fade (rxn-still); a bar
  revealed by keyboard focus gets the same ground; the simulated repaints strip rxn-below too,
  as the real rewrite does.

## Every field at 16px on a touchscreen (Liu Kang m705)
Liu Kang ruled this into this branch: the Tasks and members fields (#nt-what, #nt-who, .tk-inp,
.tkwho-sel, #pj-one-add) at 16px under (hover: none), plus a SWEEP arm, not a hand list. The sweep
(every input, select and textarea in #panel-projects, #nt-modal and #am-modal, a dynamic
.tkwho-sel added) found TEN more fields under 16px than the five named (pj-say 15px; tk-say,
tk-due, pjs-add-pick, pj-name, pj-path, pj-add-pick, pj-invite-code, pj-join-code, pj-join-pick
13px). DECISION (reversible, one rule): the touch rule covers the sweep's whole scope (every
field in those three places) plus the five by name (.tk-inp also appears outside the project
page). Font size only, no layout rule. Measured: sweep 24 fields, none under 16px; before the
rule, 10 under (that run is the control). Pinned inside the touch query only. Told Liu Kang; he
can narrow it back to the five if he prefers.
- Desktop check (Liu Kang m707: the two dialogs also open from outside the project page): a
  1280-wide page with a mouse, the same sweep. MEASURED: 23 fields, none at 16px, the named ones
  at their desktop 13px. Control: the rule ungated (@media all) makes all 23 16px and fails it.

## Found by the after-sheets, not by any check: the projects row overlapped (MEASURED)
The harness after-shots (projects, iPhone SE) showed the 16px sort drawn over Add Project,
covering half its label, while the harness reported no overflow (nothing left the panel). Cause:
a max-width on a select inside a box sized to that select resolves against itself and never
shrinks. New arm (phone, touch, 375): Add Project, the sort and the view toggle do not overlap,
all inside the row, the sort's label whole (a copy measured OUTSIDE the row gives its natural
width) and the toggle's buttons not squeezed. It failed on the overlap first (add 24-158, sort
from 119). Two of my own measurements were vacuous on the way and were caught by controls:
- measuring the select's natural width in place (bounded by the shrunk box) could not fail;
  fixed by measuring a copy outside the row;
- "no wrap" first passed because the toggle silently shrank to 58-62px (its natural width is
  78); fixed by requiring the toggle's buttons at their own width.
Fix: Add Project at its narrowest (two lines, as on main), the sort and toggle never shrink and
may wrap (the toggle drops to a second line on an SE: 142 + 8 + 78 = 228 > 212 available).
Measured: no overlap, inside, sort 142/142, toggle 78. Control: no wrap (toggle free to shrink)
fails it (toggle 62). REASONED: wider phones fit on one line.

## Review iteration 20 (opus; the first try on sonnet failed: the reviewer model hit a weekly usage limit, one spawn failure)
- W1 (sort label cut at 16px on an SE): fixed in the commit above (the overlap arm).
- W2 (no overflow measurement since every field went to 16px) and W5 (96 commits behind main):
  the branch is rebased next and the 2806 check, the full suite and the --strict harness shots
  are all re-run on the new base before any number goes in the PR.
- W3 + C2, fixed: ONE 16px field rule, scoped to #panel-projects, #nt-modal and #am-modal; the
  room's own duplicate line and the unscoped names (.tk-inp reached Settings) are gone. The
  mirror (a div, not a field) keeps its own line so it matches the composer. Pin: no unscoped
  .tk-inp in the touch block.
- W4, fixed: the desktop arm asserts that NO swept field is 16px (not just five named ones).
- W6, fixed: the empty-band arm asserts its stub was actually called.
- W7, fixed: the .tkwho-sel probe (inside a swept root, so it could not test a class rule) is
  gone with the class rule itself.
- C1, fixed: no silent catches. RXN_* state is var (no temporal dead zone), so pjView calls
  pjRxnClose directly; the focus restore calls focus() directly.
- NITs, no change: the shadow on black (the hairline carries it, noted before); -webkit-sticky
  on iOS before 13; a tap inside the open row leaves RXN_PINNED (consistent state).
- Dialog fit at 16px (review 20, W2): the Tasks and add-member dialogs shown at 375, no field off the side, no page scroll. MEASURED on the rebased base. Control: a 520px field fails it.
- Full suite on the rebased base (9392 tests): 1 failure, browser-checks-selectors (#758): my link arm gave a check-made link an id (#arm-link) the page does not have, which the lint reads as a stale selector. Now a data-arm attribute; the lint and the check pass. Suite to be re-run.

## Review iteration 21 (opus, rebased base; browser runs by me)
- W1, fixed (MEASURED): the 16px rule was scoped to #panel-projects, but the one-screen layout
  (960px and up, an iPad) appends Settings, Tasks and Create agent INSIDE #panel-projects, so on a
  touchscreen their fields went to 16px, and my comment said they did not. Scoped to the project
  page's own six views (#pj-list-view, #pj-one-view, #pj-task-view, #pj-docs-view,
  #pj-settings-view, #pj-add-view) plus the two dialogs. New arm: Settings moved inside the
  projects panel the way placeAppSettings does it keeps its sizes (20 fields, none at 16,
  .tk-inp 13). Control: the old scope fails it (20 Settings fields at 16px, Plus sign-in among
  them). The sweep and desktop arms use the same eight roots (roots asserted).
- W2, fixed (MEASURED): the unit test's media-block reader stopped at the first "\n}", and main's
  one-line touch block made it run ~219 lines on, so the touch-only pin could pass with the rule
  outside. It now counts braces, skipping comments and strings (both hold braces on this page); a
  soundness test asserts blocks never overlap and each closes inside its own <style>, and that the
  one-line block is read as its own. Controls: the rule moved to top level in place fails; the
  rule moved to right after the one-line block (the reviewer's trap) fails. (My first soundness
  rule, "no block contains @media", was wrong: nested dark-mode @media is valid and present.)
- N1, fixed: "as on main" was true only on an SE; the comment now says two lines on every phone.
- N2, fixed: the dialog arm counts only rendered fields. N3, fixed: the desktop arm asserts all
  roots were found. N4, fixed: the link branch closes through pjRxnClose (one close path).
- N5, no change (noted): a bar revealed by keyboard focus on a touch device with a hardware
  keyboard gets no placement pass. N6, noted in the CSS: below 375 (320, folded) the sort at full
  width can overflow; outside the four target sizes.
- Routed: the top-level Tasks view (#panel-tasks, #3559) keeps 13px fields in the tab layout.

## Review iteration 22 (opus; browser runs by me)
- W1, fixed (MEASURED): every touch arm ran in Chromium only, including the pinned bar
  (position fixed inside a scrolling thread), where old iOS WebKit had bugs. The 2806 check takes
  ENGINE=webkit; under Playwright's WebKit build (not Safari) every arm passes, including the
  pinned tall-post arm, the in-place stacking arm, the 16px sweep and the projects row. The one
  WebKit-only noise (file:// "Not allowed to load local resource", the harness loading from disk)
  is filtered like its Chromium wording already was.
- W2, fixed: the unit test's media reader also read "@media" mentioned inside CSS comments as
  blocks (2 junk blocks, one swallowing a real dark-mode block: 41 blocks found before, 66 now).
  It skips matches inside comments; the soundness test (retitled to say what it checks) asserts
  no query spans lines or a comment end. Control: without the skip it fails.
- CONVENTION, fixed: descriptive names in pjRxnVisibleBand, pjRxnResetRow and pjRxnPlace.
- NITs, fixed: the toggle's width is compared with a copy measured outside the row (a button
  against its own computed width could not fail; control: toggle free to squeeze fails); the gap
  arm fails cleanly instead of throwing; the sweep's floor is 20 (24 measured); the scroll
  listener returns first when nothing is open.
- NITs, no change: the .asb helper bubble is not in the visible band (only while it shows,
  bottom right; reasoned); Members/Files list scroll resets on a turn across 30rem (a DOM move);
  the tip text, landscape order and -webkit-sticky are known and routed.
- Process note: an inline comment I added ended a line inside a chk() call and broke the check's
  syntax; both runs then printed nothing. Caught by reading the raw output; node --check now runs
  on the check after each edit, as the page already gets.

## Review iteration 23 (opus; browser runs by me)
- W1, fixed: WebKit was a by-hand ENGINE switch the gate never set, named unlike the sibling
  #718 check. Now ENGINES=chromium,webkit (render-dm-phone-718's switch), every arm per engine,
  failures labelled with their engine, an unknown name refused (exit 2). MEASURED: both engines,
  268 arms, all pass. The gate (tools/browser-checks.sh) still runs Chromium only; WebKit is
  measured by hand and says so.
- W2, fixed (MEASURED): no committed arm covered the dark theme. New dark phone arm: the open bar
  has an opaque ground distinct from the dark thread and a 1px edge, and takes its taps; passes in
  both engines. Control: the ground rule removed fails it (transparent). The 393/412/430 sizes are
  covered by the harness shots (all four sizes, both themes, both engines, --strict), linked from
  the PR; that harness is Raiden's PR, not yet on main.
- CONVENTION, fixed: tappedRow, openRow, focusedElement.
- NITs, fixed: comment placement; long lines rewrapped; the dialog arm measures the dialog card
  (.rm-box) and drops a page-scroll test that could not see a fixed box; the desktop arm pins the
  named fields to 13 and the count floor to 20; three arms now assert their precondition (the bar
  reopened before the closing tap; the bar really overlaps the message before it; the mouse click
  really reached the row); the lifted arms say they read the real sticky header; "pinned" has one
  record (pjRxnPinned reads RXN_PINNED); the 16px rule skips checkboxes and radios.

## Review iteration 24 (opus; browser runs by me)
- W1, fixed with a measurement (MEASURED): the 16px rule also applies on touch tablets at wide
  layouts. DECISION: keep it unbounded (whether iPadOS zooms small fields is not measured here, and
  a width bound would also drop landscape phones, which do zoom). New arm at 1180 wide, touch:
  every field in the project view and both dialogs is 16px and stays inside its column or dialog
  card. Both engines pass. Control: a 900px field fails it (and the dialog arm).
- W2, disclosed: page-level overflow at 393/412/430, dark, WebKit is covered by Raiden's harness
  (--strict, all sizes, both themes, both engines; sheets linked from the PR), which is not on
  main yet. The PR body says so plainly.
- W3, fixed: the file-card arm counts clicks on the card (cardClicks 1), since the bar was already
  closed and "shown 0" held whatever the tap hit.
- NITs, fixed: the second-tap close goes through pjRxnClose; the pjRxnPinned wrapper is gone
  (RXN_PINNED read directly); the dark arm's comment says what ground it measures; the stale
  "on main" comment is gone; the field roots and skipped types are read ONCE from the page's own
  rule (FIELD_ROOTS, FIELD_SKIP) by every sweep, and the check refuses to run if it cannot read them.
- NITs, no change: short names inside the check's evaluate callbacks (test code, local); Add
  Project at two lines on every phone (a switch at 390px would add a breakpoint for one word).
- For the PR body: the room's other left bars (.msg-valve, .msg .quoteb, .pj-msg.unsure) are on
  kosmos#3692 (Liu Kang: quotes excluded on purpose).

## Review iteration 25 (opus; browser runs by me)
- W1, fixed: the tablet arm swept one of six views; it now sweeps FIELD_ROOTS (all six views and
  both dialogs), floor 20 fields.
- W2, fixed: the phone 16px sweep read its places from the rule, so it could only prove the rule
  reaches what the rule names. It now sweeps the WHOLE #panel-projects plus both dialogs,
  excluding only the other screens the one-screen layout moves in (Settings, Tasks, Create agent).
- W3: rebased onto main after this commit and the check re-run on the new base (below).
- W4, disclosed (carried): page-level overflow at 393/412/430, dark, WebKit rests on Raiden's
  harness, not yet on main; in the PR body.
- C1, fixed: the check's results passed to chk have descriptive names. C2, fixed: the page's
  loop variables in the new code are named; PJ_PHONE_MQ / PJ_TOUCH_MQ keep "MQ" (media query, the
  names the tests and comments already use). C3, fixed: no measured numbers in the projects-row
  comment.
- N2, fixed: a positive control for the reaction spy (one real emoji tap counts exactly 1).
  N3, fixed: the focus arm asserts focus reached the button before the click.
  N4, fixed: the plan's superseded statements are marked.
- N1, no change (decided): closing ANY open bar on scroll would also fire on the scroll a new post
  causes (the thread keeps to the bottom), undoing "a repaint keeps the bar"; an unpinned bar's
  only failure is being clipped at the thread edge until the next tap. N5, not measurable here:
  whether iOS blurs the composer on a tap in the room (a keyboard-close resize would then close a
  just-pinned bar) needs a real device.
- After the rebase (main +25): the tablet arm's floor of 20 was set without measuring and failed in both engines (15 fields render at 1180; all 16px, none outside). Floor set to 12 from the measurement. ENGINES=chromium,webkit: 274 arms, all pass, on the rebased base.

## Review iteration 26 (opus; browser runs by me)
- W1, fixed (MEASURED): since #3700 (on main) tipPlace keeps any card on screen, so "the card is in
  view" could no longer fail, and the anchor half re-ran the selector instead of reading what
  tipPlace did. The arm now reads the result: a POINTING card (not tipPlace's arrowless "flat"
  fallback) beside the conversation header at 375, beside Add member at 800 (any of tipPlace's
  four sides). Control on THIS base: the old anchor gives a flat card at 375 (fails). The tip
  comment now says why in #3700 terms.
- W2, fixed: the plan's "where each is measured" paragraph is current (both engines by hand, the
  gate Chromium only, the arms that exist, the harness for whole-page overflow).
- CONVENTION, fixed: the unit test's media reader uses descriptive names. The browser check's
  evaluate callbacks keep short rect names (R, A, S, T): local to a few lines of geometry.
- NITs, fixed: Escape closes an open touch bar too (an iPad keyboard); the 16px comment says what
  each arm sweeps; the desktop arm no longer pins 13px by value (zero-at-16 is the leak test);
  long comment lines rewrapped. NIT, no change: the tap flash and shadow are raw rgba values (no
  token for either exists; both noted since iteration 17).

## The surface gate (#2518) on the rebased base: measured, then overridden per check
The full suite (9442 tests, 0 failed) went red on the #2518 surface gate: web/index.html lines
changed that mention tokens nine other browser checks key on (am-modal, pj-post, pj-add-view,
pj-docs-view, d-dmthread). Each of the nine was RUN on this branch before overriding it: all
pass (render-agentdm-3414, render-composer-stroke 63 passed, render-dm-phone-718,
render-dm-reactions-3650, render-pjadd-back-2850, render-pjmode-style-3495 28 ok,
render-project-members-3387 40 passed, render-subview-cleanup-3502 13 passed,
render-type-to-focus-3283 18 assertions). Per-check trailers in the commit say so. (My first
loop over them used `timeout`, which this Mac lacks: every run was exit 127 and "0 fails" meant
nothing; caught and re-run directly.)

## Review iteration 27 (opus; browser runs by me)
- W1, fixed: three of my surface-gate trailers gave false reasons ("touched only by a comment
  naming #d-dmthread"). I had read only the gate's FIRST matched token. Recomputed with the gate's
  own library: agentdm-3414 is hit by d-dmthread, msg, msg-bd, msg-av; dm-phone-718 by d-dmthread,
  msg-b, msg-bd, msg-av; dm-reactions-3650 by every rxn token (the shared picker's pick handler
  changed); subview-cleanup-3502 also by pj-settings-view. The next commit carries corrected
  trailers (the gate reads the newest first). render-mention-blue-2922 is not gated at all (its
  annotation sits inside a doc comment the parser cannot read) though this branch changes
  pj-post-mirror / pj-mirror-in; run on this branch: 149/149. Its annotation is that file's owner's.
- W2, fixed (MEASURED): a committed guard for the whole page at all four phone sizes and both
  themes, in the real layout (project page with a long post, a long file name and a link card;
  the projects list). Nothing may run past the SCREEN. Two things found while making it able to
  fail: (1) an ancestor clips the page, so it never scrolls sideways even with a 520px column
  (the first version passed that control); the arm now checks every rendered element's box,
  exempting only real sideways scrollers (.mdtablewrap, pre), NOT every overflow:auto box (the
  thread's overflow-x computes to auto and would have exempted the room). (2) In a mobile context a
  page wider than the screen WIDENS innerWidth to fit (measured 545 on a 375 screen), so any
  comparison against innerWidth cannot fail; the page, dialog and tip arms now compare against
  visualViewport's width. Controls: the 520px column fails all eight; the long-file-name bug put
  back fails all eight; a 520px dialog field fails the dialog arm. Both engines: 290 arms pass.
- W3, fixed: a pick from the full picker closes the touch bar BEFORE the reaction's round trip.
- NITs, fixed: textareas in the 16px scope get line-height 1.35; the 16px comment says the New task
  dialog is 16px on the top-level Tasks screen too; the tablet arm bounds each field by its column,
  dialog card or panel and says so. NIT, carried: the tip's wording (Members first) is #3574's.
- My slip, again: an inline comment swallowed the rest of a line in the tablet arm (fieldRect
  undefined), which node --check cannot see (the result still parses). Caught by the run crashing;
  every added line was then scanned for a // with code after it (none left).

## Review iteration 28 (opus; browser runs by me): a BLOCKER I introduced in iteration 27
- BLOCKER, fixed (MEASURED): iteration 27's textarea line-height rule (1.35) also matched the room
  composer #pj-post (a textarea in #pj-one-view), while its @mention mirror, which draws the text
  the person reads once the value is not empty, keeps 1.5. On a touchscreen the typed text drifted
  off the cursor line by line and the last line was clipped. New arm: composer and mirror match in
  font size and line height, and the mirror is no taller after three lines. Before the fix it
  failed ({"lineHeight":["21.6px","24px"],"heights":[83,90]}); after, 24/24 and 90/90, both engines.
  The rule is now textarea.tk-inp only (the form boxes it was for). Unit pin: the line-height
  rule's places equal the 16px rule's, and no unqualified textarea rule exists (control: the
  unqualified rule fails it). The mirror comment no longer claims a unit pin that only matched
  text; the browser arm is the pin.
- W2, fixed: the whole-page arm's comment claimed a link card it does not render and project cards
  a server-less page cannot have; it now says what it renders (and the link card's width is the
  unit pin .lpv = .att).
- W3, fixed: the composer-stroke and type-to-focus trailers now list every change to #pj-post
  (16px on touch, the one-line placeholder at phone width, the mirror at 16px, the scroll on
  rotation; line height unchanged), with both checks and mention-blue re-run AFTER the fix
  (63 passed, 18 assertions, 149/149).
- W4, fixed: the tablet arm shows one view at a time and bounds each field by its nearest real
  container (column, dialog card, card, field row, else the view). Control: a 900px field fails it
  ("pj-name in field").
- CONVENTION, no change: short rect names in the check's local geometry (noted each round).
- NITs: the tip wording is #3574's; the pinned bar is measured only lifted (no fixed-containing
  ancestor, reviewer checked); the placeholder ellipsis may clip rather than show "..." in WebKit
  (one line either way). Both engines: 292 arms pass.

## Review iteration 29 (opus; browser runs by me)
- W1, fixed (MEASURED): the whole-page arm covered 2 of the 6 views; now all six, one at a time.
- W2, fixed (MEASURED): 360x780 (the common Android width) added to the whole-page arm; all six
  views, both themes, both engines: nothing past the screen. My comment "below 375 the sort can
  overflow" was wrong (360 fits); it now says measured down to 360, 320 and folded not measured.
- W3, fixed: the Settings arm asserted Settings' own sizes (so it would fail if Settings fixed its
  own zoom); it now asserts each field's size is EQUAL at home and inside the projects panel.
  Control: a separate leak rule (#panel-projects .tk-inp at 16px) fails it (19 Settings fields
  changed). (The old wide scope cannot even be run: the check refuses to start when it cannot read
  the six-view scope, exit 2, by design.)
- W4, fixed: fresh trailers for pjadd-back, pjmode-style, project-members, subview-cleanup (both
  rules named: the 16px field rule and the .tk-inp line-height rule) and agentdm (the "none in the DM
  thread" wording narrowed to its tokens); each is a mouse-only check (no hasTouch/isMobile, grepped),
  so (hover: none) rules are inert there; each re-run on this code, all pass.
- W5, fixed (MEASURED): a viewport change (the keyboard closing when the pinning tap left the
  composer, a turn) now RE-PLACES a pinned bar instead of closing it; it closes only if its post
  is off screen. Arm: pinned, window + visualViewport resize, still pinned and on screen. Control:
  close-on-resize fails it. Scroll still closes a pinned bar.
- W6, fixed in the comment: "a phone" is one width across the PROJECT pages; the Direct Message's
  phone rules use 40rem (its owner's), so between 480 and 640 they differ. Not aligned here.
- W7, fixed: render-room-msgbox-2806 now carries a line-leading Browser-check-surface annotation
  (pj-room rxn-quick rxn-show rxn-below pj-list-view sortctl viewtoggle nt-modal am-modal
  pj-post-mirror pjmidhead pj-add-member; each token checked present in the page).
- NITs, no change: visualViewport as the reference (its controls fail as they should); a synthetic
  picker scroll without a synthetic non-picker control (the real-scroll arm shows the close);
  engine-loop indentation; Add Project two lines everywhere (decided); short names; commit subjects.
  Both engines: all arms pass.

## Review iteration 30 (opus; browser runs by me)
- W1, fixed (MEASURED): on iOS the keyboard closing both resizes the visual viewport AND scrolls
  the window, and a scroll still CLOSED a pinned bar, undoing iteration 29's re-place. Any scroll
  now re-places a pinned bar too: it stays over its own post while the post fills what shows and
  closes once the post has left the screen. Arm: thread scroll plus a resize and a window scroll
  together keep it pinned over its room; the post moved off screen closes it. Control: close on
  scroll fails it. (This supersedes iteration 15's "any scroll closes a pinned bar".)
- W2, fixed (MEASURED): a visible band thinner than the bar pinned it under the sticky composer or
  header. Now it closes. Arm (a stubbed 20px band); control: the check removed fails it.
- W3, fixed (MEASURED): four close paths had no behavioural arm; each now opens a bar with a real tap
  and asserts closed with no state left: Escape; a pick from the full picker (closed while the
  reaction request is still in flight); a tap outside the room (target asserted outside); pjView
  navigation. Controls: each path removed fails its arm (the picker one: closing after the request).
- W4, fixed: the whole-page, dialog and tip arms assert their reference width equals the phone's
  (an engine that zoomed out to fit would widen visualViewport too), and each whole-page view must
  render at least 3 elements (measured minimum 6).
- CONVENTION, fixed: the pinned-bar scroll comment says what the code does now.
- NITs: the whole-page header comment updated (five sizes, six views); trailers broadened; the
  one-screen layout on a touch tablet is not measured (the static page runs the tab layout; noted);
  the tip anchor also changes desktop when #pj-add-member is hidden (points at the conversation
  instead of a flat card; for the PR body); iPadOS with a trackpad reports hover: none, so the bar
  opens on a click there, not on hover (for the PR body). Both engines: 308 arms pass.

## Review iteration 31 (opus; browser runs by me)
- W1, fixed: three comments still said a scroll closes a pinned bar; they say re-places now.
- W2, fixed (MEASURED): only a PINNED bar was re-placed on scroll or viewport change, so an ordinary
  bar whose post reached the thread's top edge sat clipped, and stayed open after its post left.
  Any open bar is re-placed now (pinned ones with tap authority, ordinary ones without: they flip
  below, or close when neither side fits or the post has left). This differs from the rejected
  "close any bar on scroll" (iteration 25): a repaint's scroll now re-places, not closes. Arm: an
  ordinary bar at the visible top edge flips below and stays in view; the post scrolled away closes
  it. Control: re-placing pinned bars only fails it.
- W3, fixed (MEASURED): a pinned bar sat at the top of what shows even when its post's own top was
  showing, over the message above. It pins at max(band top, post top) + gap, kept inside what shows.
  Arm (room below the sticky header so the post's top can show): bar at the post's top + 4, clear of
  the message above. Control: the old band-top pin fails it (bar 255 over the message ending 259).
  Two arm-writing traps on the way: a post placed at the ROOM's top edge was really under the page's
  sticky header (lifted room), which is correct to close; both arms now place posts relative to
  pjRxnVisibleBand, and the pin arm moves the room below the header so its case is reachable.
- CONVENTION, fixed: every surface-gate check re-run on THIS code (all ten, including the touch check
  render-dm-phone-718, and mention-blue), and fresh trailers say so.
- NIT, fixed: opening an agent from a room row closes a touch bar (navigation closes it).
- NITs, no change: FIELD_SKIP types vs the rule's :not list (none visible); the tip wording (#3574);
  the "no CSS order" guard is broader than needed (fails closed); the room check's annotation is wide
  by intent (it is the guard for those surfaces); commit subjects (history; the PR title is clean).
  Both engines: 312 arms pass.

## Review iteration 32 (opus; browser runs by me)
- W1, fixed: my round 31 trailer called render-dm-phone-718 "a touch check". It is not (its header:
  "viewport widths, not mobile emulation (no isMobile/touch)"; its page has no hasTouch). I had
  repeated round 31's reviewer's claim into a trailer without grepping the file. Corrected trailer:
  a viewport-width check with no touch, so the document listeners' touch path is reasoned for the DM,
  not measured there.
- W2, fixed: the agentdm trailer said "room-only listeners"; the scroll, pointerdown, resize and
  visualViewport listeners are document/window-wide, gated on an open ROOM bar. Corrected.
- NIT, fixed (MEASURED): a reaction's reply rebuilds .rxns in place (rxnsInner), giving a fresh bar
  with no pin while the row keeps rxn-show; the observer now re-pins it. Arm rebuilds the row with
  rxnsInner (my first version copied innerHTML back, which kept the inline pin and could not fail:
  its control passed; caught and fixed). Control: without the observer branch, the bar stays unpinned.
- NIT, fixed (MEASURED): a tall post's bar tapped with its top showing opened above, unpinned, and
  closed when that top scrolled away though the post still filled the screen. A post taller than
  what shows now has pin authority on re-place. Arm; control (no authority) fails it.
- NIT, fixed: scroll re-placing is coalesced to one per animation frame (iOS momentum scroll).
- NIT, fixed: the placement comment sits on pjRxnPlace.
- NITs for the PR body: a pinned bar covers the post's first line (a tap there reacts; accepted
  trade-off vs covering the message above); landscape phones keep the desktop order; the one-screen
  layout on a touch tablet is not measured for the tap bar; the tip wording (#3574).
- Both engines: 316 arms pass.

## Review iteration 33 (opus; browser runs by me)
- W, fixed (MEASURED): the "repaint keeps a pinned bar pinned" arm simulated a repaint by copying
  innerHTML back, which kept the bar's inline pin, so it could not fail (the same trap as round 32's
  first arm, in its twin: I fixed one and not the other). The simulation now drops the open class
  AND the inline style, as paintRoom does. Control: without the observer's re-place on a full
  repaint, it fails (shown 1, pinned false); before the fix it passed that way.
- NIT, fixed: pjRxnReplacePinned renamed pjRxnReplaceOpenBar (it re-places any open bar).
- NIT, fixed: window resize and visualViewport resize/scroll use the per-frame pjRxnReplaceSoon.
- NITs, fixed: the RXN_PINNED, tall-post pin and viewport comments say what the code does; the
  whole-page arm reads its views from FIELD_VIEWS; the unit test header says it pins source.
- Ruling after this round (Liu Kang m773): each round fixes only its own findings; if round 38 is
  not clean, the PR opens with open findings by severity and he decides what blocks.
- Both engines: 316 arms pass; all nine gated checks re-run on this code.

## Review iteration 34 (opus; browser runs by me)
- W1, fixed (MEASURED): the room observer's two exits (another project showing; the open post gone
  after a repaint) cleared the post and project but left RXN_PINNED and any open-row class or pin.
  Both now call pjRxnClose. Arm: a repaint that drops a pinned bar's post clears its state.
  Control: the old branch fails it (pinned stays true).
- W2, duplicate: landscape phones and small tablets (30rem to 68rem) keep the old order. Decided
  in iteration 3; goes in the PR body. No comment added (m773: no new scope).
- NITs, deferred: the source pins' regexes are tied to formatting (they fail closed, never vacuous);
  the /* scanner counts JS and attribute text (harmless, all blocks sit in the top <style>, and the
  reader has a soundness test); .pjcol.pjmid padding is a bare class inside the phone block.
- Validation on this round's predecessor: 9525 tests, 0 failing, recorded failed only for the #3795
  leak (it writes during the run). A worktree-only exclude was used as a stopgap (m785) and removed
  once #3797 merged (m788); the PR's validation runs without it, on main containing #3797.
- Both engines: 318 arms pass; all nine gated checks re-run on this code.

## Review iteration 35 (fable; a different model from 33 and 34)
- CONVENTION, fixed: this ledger stopped at iteration 32. Iterations 33 to 35 added.
- NITs, deferred (m773: fix only a round's blocking findings, add no scope): pjRxnReplaceSoon reads
  its queued flag before the post guard (unreachable, nothing dispatches mid-script);
  pageScrolls and offenders repeat one filter; the dialog-fit arm restores `hidden` but not
  `inert` (later arms do not read it); the pinned bar is measured only in the lifted room, not in
  place (the reviewer checked no ancestor makes fixed resolve to anything but the viewport, so the
  disclosure is accurate; it stays reasoned, and goes in the PR body).

## Review iteration 36 (opus)
- W, fixed (MEASURED): the six pages this branch added to the room check had no script-error
  listener, so a handler that threw after the line an arm reads would pass. Each now collects page
  errors (the theme loop's file:// filter) and asserts none before closing. It found real errors at
  once, caused by the check itself: it REMOVED #firstrun, which the app reads; it now hides it
  (the global [hidden] rule is !important). Control: a throw at the end of pjRxnClose on Escape
  left every state arm green and turned only the phone page's error check red.
- NITs, deferred (m773): PJ_CURRENT set by the spy arm is never reset (later arms still pass, the
  project-mismatch exit has its own arm path through pjRxnClose); a bar opened by keyboard focus on
  a touch iPad is not placed (the tap path is; PR body, with the iPad trackpad note); the tip body
  still numbers Members first on a phone (#3574 owns the tip wording); the source pins' regexes
  are tied to formatting and the media reader wants one space before `{` (fail closed; noted in
  iteration 34).
- Both engines: 348 arms pass (30 new error checks).

## Review iteration 37 (fable)
- W1, fixed (MEASURED): a bar opened by keyboard focus on a touchscreen (an iPad with a keyboard,
  VoiceOver) never ran placement, so on the thread's first post it opened above and was cut by the
  thread's top (raised as a NIT in iteration 36, a WARNING here; fixed now rather than disclosed).
  A focusin listener flips it below where the top would cut it, and focusout drops the flip; a
  flipped row is lifted like an open one (z 1, still under the composer). Arm: focus a bar button on
  the first post (the room below the sticky header, so the post really shows: my first version
  put it under the header, where the post itself is hidden, and failed on in-view for that reason).
  Controls: without the listener, not flipped and not in view; without the lift, flipped and in
  view but every tap lands on the next message.
- W2, measured (no code change): the projects row above 30rem on a touchscreen (16px sort, the
  two-column no-wrap row). The row arm now runs at 375, 490, 540 and 600: no overlap, nothing cut
  or squeezed (tightest at 490: 76px between Add Project and the sort).
- C1, fixed: the phone block's avatar size and gap are one custom property each (--room-av,
  --room-av-gap); the avatar-28px and far-equals-near arms still pass.
- C2, fixed: the theme loop uses watchErrors, so the file:// filter exists once.
- NITs, deferred (m773): "wider than a phone" comment wording for landscape; the DOM-move pin's
  failure message; a closing-brace comment for the engine loop; the scope-count exit message.
- Both engines: 356 arms pass; all nine gated checks re-run on this code.

## After convergence: rebased onto #3755 (tips pass 2) and #3800
- #3755 replaced the single project tip with four steps (Members, Conversation, Files, Tasks),
  each ringing its own area and scrolling it into view as the step opens. My tip change (the
  anchor list `.pj3 > .pjmid .pjmidhead, #pj-add-member`) conflicted; it is dropped and main's
  steps kept (rebased with conflicts to main's side; an interdiff of my patch before and after
  shows that anchor as the ONLY difference).
- The tip arm now walks the real stepped tip (tipShow, then its Next button) at 375 and at 800:
  every step is a pointing card (not "flat"), on screen, with its area on screen. All four pass
  in both engines. The unit pin on the old anchor is removed.
- Disclosed, not changed: on a phone the steps visit Members (now below the conversation) before
  the Conversation; the step order is #3755's (Josh's copy).
- An arm broke on the new base for the arm's own reason: "the post leaves" scrolled a six-post
  room by 2000px, but the room could only scroll 265px, leaving 24px of the post showing (so the
  bar rightly stayed). It now has twelve posts and asserts the post really left (postGone).
- Both engines: 356 arms pass; the nine gated checks re-run on this base: all pass.
