# Plan: a web push tap opens the agent (kosmos #718, mobile pass)

Pair of kosmos-relay push-session-718 (the coordinator passes a validated `session`).
Shape agreed with Johnny (iOS) and Liu Kang: https://<address>/?tab=detail&agent=<session>,
the board's own link (syncUrl writes it, WANT_AGENT reads it at boot, openDetail matches
by sessionName). A link to an agent that is not on the board goes to the board home (Part 2).

## Finished looks like
- web/sw.js boardUrlFor adds ?tab=detail&agent=<session> only when session matches
  ^[a-z0-9][a-z0-9_-]{0,63}$, via URLSearchParams; anything else keeps today's URL.
- Tests: the good links (with and without address), the notification carries it, and every
  trick is dropped. Control: removing the regex check turns the hostile test red (measured).
- The Android TWA uses this service worker, so Android lands the same way.

## Decisions
- Re-check on the phone even though the coordinator checks: the service worker must not
  trust a push. Same rule as the coordinator and the iOS app, one shape everywhere.
- Weakest part: a found agent with a non-conforming session name gets no deep link.

## Part 2: where the tap lands on a phone
A tap opens ?tab=detail&agent=<session>, but on a phone the agent page stacks (max-width
56rem): identity, five full-width section boxes and Files come first, so the question sat
about two screens down on an iPhone SE. detailRevealTalkOnPhone scrolls the Direct Message
section into view on the two ARRIVALS, only when the stacked layout is on:
- the push link at boot: SETTLED ONCE (the boot open sits in the poll,
  which runs every few seconds; review iteration 1 caught the reveal re-running there). If
  the agent opened, reveal; if it is not on the board (a push for an agent removed since),
  go to the board home instead of the empty agent page the link used to leave, and stop
  reopening it, after TWO statuses without it (review iteration 2: an agent a moment late
  on the first status must still open).
- the Answer button: it already put the question on screen (it focuses the composer); the
  reveal is a belt-and-braces call before that focus, re-measured with it in place.
Measured, sandboxed board, Chromium and WebKit (engine build, not Safari), SE:
push link: question off screen (scrollY 0, top 1036) -> fully on screen; Answer: on screen
before and after; a link to a missing agent: stuck on an empty agent page -> the board home;
scrolled back to the top after landing and waited 7s: stays put (no re-pull).
Tests: web.push-landing-718.test.js pins the one-shot settle, the fallback home, the Answer
call site, the shared 56rem breakpoint, and the behaviour (phone scrolls, computer does not,
hidden/missing section left alone).
Control: dropping the boot call fails it.
Not touched: the Direct Message layout itself (Scorpion's screen).

## Part 3: the other "waiting on you" controls, at thumb size
- The Allow card (#askcard, a phone asking to use this Kosmos): its 3px gold LEFT BAR is
  now a 1px gold border all round (Josh, 2026-09-24: no solid coloured left bars on cards;
  same attention, same gold token). On a touchscreen its buttons are 44px tall (were 34
  and 24). Measured at all 4 sizes x 2 themes x 2 engines (the card is injected at
  /api/remote/pending in the audit; a sandbox has no tunnel): no left bar, 44px buttons,
  no horizontal scroll. Test pins it; control (bar restored) fails it.
- "Answer" on a needs-you card: a larger invisible hit area on a touchscreen (about 40px
  tall, inset -8px -6px so it does not reach the next row in the list layout), the pill
  layout unchanged.

## Review iteration 3
- settleWantAgent (one function, tested by behaviour with a fake clock): a link that landed
  is never reopened (the old poll reopened it whenever CURRENT went null, e.g. after removing
  a different agent); a missing agent goes to the board home only 4s after the first miss
  (two boot ticks arrive ~100ms apart, so counting statuses was not a grace at all).
- A 4s hold after the reveal puts the conversation back if content above it (the Files list,
  painted from its own fetch) pushes it down; it stops at the person's first touch, wheel,
  key or click. MEASURED: with 12 files arriving 1.5s late, Playwright Chromium and WebKit
  land the question fully on screen with or without the hold (both compensate). REASONED:
  real Safari may not anchor scroll, so the hold is kept as a tested safeguard (unit test
  with fake timers; control: disabling the re-scroll fails it).
- A structural edit left stray braces that broke the page script while every unit test
  passed (they lift single functions). Every structural edit now gets a whole-page syntax
  check (new Function over each inline script) before anything else.

## Review iteration 4
- The Answer path reveals only if that agent actually opened (it can have left the board
  since the card painted); settleWantAgent already had the same guard.
- The windows are named: WANT_AGENT_GRACE_MS (4000), REVEAL_HOLD_MS (4000),
  REVEAL_HOLD_TICK_MS (150); the tests drive those values and a test pins them.
- The hold's window listeners are capture + passive, the file's idiom.

## Review iteration 5
- The hold now ends on ANY focus change (focusin) as well as touch, wheel, key and click: the
  Answer path focuses the composer and the on-screen keyboard scrolls it into view; the hold
  must never pull it back under the keyboard. The unit test reads the real stop list from the
  page (control: dropping focusin fails it) and checks every listener is removed with its
  own capture flag.
- The hold also ends when the section's panel is no longer rendered (offsetParent null).
- The grace and the hold use performance.now (a wall-clock change cannot stretch them);
  REVEAL_HOLD_DRIFT_PX names the 2px drift allowance.
- A link is spent once the person opens a different agent during the grace.
- Browser check: a real hit test for Answer (touch catches a tap 5px above the text, a mouse
  does not), and the hold with REAL timers under overflow-anchor: none. MEASURED: Chromium's
  anchoring kept the section still by itself (rightAfterPush 0), so anchoring is turned off
  to stand in for an engine without it; then 300px of late content is put back to 0, and with
  the hold disabled it stays at 300 (fails).

## Review iteration 7 (after the rebase onto 627c4104)
- DEFERRED, filed as kosmos#3689: sw.js trusts any domain-shaped `address` (pre-existing;
  VAPID signing is what protects it; `session` cannot widen it). Needs a suffix allowlist or
  a pinned address, its own card.
- A reload of an agent page on a phone also counts as an arrival and lands on the
  conversation; that is kept on purpose (it is where a phone reader wants to be) and the
  comment now says so, rather than adding a push-only marker the iOS app would also need.
- The Allow border comment says why --gold-edge (3:1 non-text contrast), not "same gold".
- The browser check says plainly that no committed check covers WebKit; the reveal result
  prints before the hold; the boot test counts exactly one openDetail(WANT_AGENT).

## Review iterations 8-9
- CLAUDE.md "Where to Find Things" row for phone notifications now names where a tap lands.
- Browser check (e): end to end from a page load (?tab=detail&agent= at 375, /api/status
  stubbed, real timers): present agent lands on its conversation; missing agent goes home
  after the grace and the address drops the link. Control: no settle call fails both.
- Any open agent marks the link spent (even the linked one, opened by hand in the grace).
- Comments: the trigger is any page load with ?agent=, whatever the tab; the hold's one gap
  (a scroll with no input event, e.g. a scrollbar drag or iOS's status-bar tap) is named.
- DETAIL_STACK_MQ names the 56rem stacking width; the Allow card's touch rule sits with its
  other rules.
