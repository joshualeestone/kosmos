---
pre_challenge: true
method: challenge-loop
branch: buildmarker-2066
diff_hash: cb8a9a2d0c7b1faa3323d75bdb7e9185f5091d3bf0caa3dd743c4460207ece09
validation: passed
subdir_audit: passed
timestamp: 2026-09-03T23:54:31Z
iterations: 2
converged: true
---

## [CHALLENGE-LOOP] Summary

**Iterations:** 2
**Converged:** Yes — the first blind sub-agent review (iteration 2) surfaced zero
BLOCKER/WARNING/CONVENTION findings (3 NITs, all deliberate-by-design or optional).
**Total findings:** 3 real defects (all in the 6.0 validation baseline) + 3 NITs.
**Fixed:** 3 | **Deferred:** 3 (NITs) | **Asked:** 0

Validation: `validation_log_run_or_skip` (JS suite + shell gate) green against final
HEAD; the committed browser check `render-build-marker-2066.js` passes and reds against
origin/main (control), including the computed-style staging-badge arm. `test-staging-
channel-2036` (Baron's staging work) still passes, so the read side does not disturb the
publish side. subdir audit: no changed subdir CLAUDE.md.

### Per-Iteration Breakdown

#### Iteration 1 — the 6.0 validation baseline: 3 real defects, all mine, all caught before a reviewer ran
- [BLOCKER] web.theme.test.js — the forced dark theme was out of sync: I hand-edited the
  `:root[data-theme="dark"]` `--stag`/`--stag-bg` tokens, but that block is GENERATED by
  tools/sync-forced-theme.js from the media-query source (the test pins `sync.build(PAGE)
  == PAGE`). --> FIXED: edited only the source (media block) and ran the generator; the
  twin now sits in the generated block in source order.
- [BLOCKER] browser-checks-reason-grep.test.js — two exact-count tripwires: my new browser
  check added one finding-emit site and one catch/launch-emit site. --> FIXED: bumped
  EXPECTED_SITES 35->36 and EXPECTED_CATCH_SITES 17->18, each confirmed quotable (same
  shapes as render-account-badge-1921; the `bad` arrays stayed empty).
- [BLOCKER] web.baked-version.test.js — my `paintBuildMark(null,'prod')` insertion broke the
  pinned `paintBuildLine(null); tick();` adjacency (baked line paints before the first
  poll). --> FIXED: ordered the marker call before the build line.

#### Iteration 2 — first blind sub-agent review: CONVERGED (0 blocking, 3 NITs)
Zero BLOCKER/WARNING/CONVENTION. Five STRENGTHs confirmed: sourceChannelNow() fails safe
(default prod, folds every non-'staging' value to prod, no path-traversal surface); no XSS
(only `esc(shown)` reaches innerHTML; version/channel/port live in the .title text);
baked-wins logic matches the sibling paintBuildLine; the browser check's computed-style
assertion is non-vacuous and genuinely reds on origin/main; both node tests execute the
logic (real server boot; paintBuildMark eval'd against a fake DOM) rather than pinning
source text.
- [NIT] server.js:2129 — sourceChannelNow() does a sync readFileSync on the 5s poll path
  --> DEFERRED: deliberate — a fresh read reflects a channel flip (staging->prod after an
  update) without a server restart, and the cost is negligible for a local single-account
  board.
- [NIT] web/index.html — `#buildmark` sets `user-select: none` --> DEFERRED: the design's
  "furniture, not content" choice; the version is copyable from the Settings build line and
  in the hover title.
- [NIT] web/index.html — no aria-label on `#buildmark` --> DEFERRED: not a defect — a screen
  reader already reads the visible text ("STAGING v0.6.27" / "v0.6.26"). Optional polish.

### Final Ledger

| # | Iter | Category | File:Line | Description | Status | Resolution |
|---|------|----------|-----------|-------------|--------|------------|
| 1 | 1 | BLOCKER | web.theme.test.js | forced-theme out of sync (edited generated region) | FIXED | regen commit |
| 2 | 1 | BLOCKER | browser-checks-reason-grep.test.js | two exact-count tripwires | FIXED | count commit |
| 3 | 1 | BLOCKER | web.baked-version.test.js | broke paintBuildLine/tick adjacency | FIXED | count commit |
| 4 | 2 | NIT | server.js:2129 | sync read on poll path | DEFERRED | deliberate (no-restart channel flip) |
| 5 | 2 | NIT | web/index.html | user-select:none | DEFERRED | design (furniture) |
| 6 | 2 | NIT | web/index.html | no aria-label | DEFERRED | visible text already SR-legible |

### Outstanding questions (ASKED, still unresolved when the run ended)
None.

### NITs (non-blocking, across all iterations)
- [NIT] server.js:2129 — sync readFileSync on the poll path (iteration 2)
- [NIT] web/index.html — user-select:none on the marker (iteration 2)
- [NIT] web/index.html — no aria-label on the marker (iteration 2)

### Strengths (across all iterations)
- Fail-safe server read (default prod, folds unexpected values), no path-traversal surface.
- No XSS: only the escaped version reaches innerHTML; channel/port are in the title text.
- Baked-wins version logic shared with paintBuildLine, so the two surfaces cannot drift.
- Non-vacuous computed-style browser check that genuinely reds on origin/main.
- Both node tests execute the logic rather than pinning source text.
