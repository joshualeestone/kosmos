# h8-disc-3828: render-assistant-hosted-3660 H8 accepts a guide drawn as its initial (#3828)

## Why
The 0.6.95 re-cut (b041227ae) stopped at 3b: render-assistant-hosted-3660 "H8 once a guide exists
the bubble moves to it, with its picture" failed twice. #3828 (asb-initials-3707) draws a guide with
NO picture as its initial on a disc (asbAvatar returns a data: SVG) instead of the /avatar URL; it
updated render-assistant-bubble-3034 but not this check, whose fixture guide has no picture.

## Call
H8 accepts either form: the /api/agent/josh/avatar URL (a guide with a picture) or a data:image/svg
whose text is the guide's initial J (a guide without). Both prove what H8 is for: the bubble moved
to the guide. The same predicate now also backs H19's precondition, which had the same URL-only
wait and was silently timing out (a 6 s sleep). Rejected: giving the fixture guide a picture (it would stop exercising #3828's path).

## Weakest premise
That the initial in face()'s SVG is the text node ">J</text>"; a markup change there would make
this arm fail loudly (safe direction).

## Proof
- Red: H8 on origin/main (Mortals): FAIL. Green: on this branch (Mortals): PASS.
- Full 3b set on this branch (Mortals, origin/main 81da9bc + this fix): all page checks passed
  (render-type-to-focus-3283 needed its retry: a flake, not a stale check).
