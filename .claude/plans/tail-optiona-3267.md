# tail-optiona-3267 -- message bubble tail: rebuild to the approved Option A + width cap

Addresses #3267. Refs #3244 (Josh picked Option A), #3247 (shipped, drifted). Josh 2026-09-18: the live tail "is not the right iMessage thing you showed yesterday" / "the graphic shows the messed up version"; also "message widths... too wide on both sides."

## Root cause
The approved Option A mockup (scratchpad/bubble-mockup.html, render bubble-options.png) draws the curved wing OVERLAPPING the bubble box, which is what makes the curve tuck under the corner and read as the Apple flick. The mockup used a SOLID tint, so overlap was fine. The app's user bubble was a TRANSLUCENT `--usermsg-tint`, so an overlapping wing double-composites into a darker triangle (#3130/6.72). #3247 dodged that by shrinking the wing onto a tiny no-overlap mask -> squished notch. My first #3267 attempt (bigger no-overlap) still rendered as a thin nub. Proven via the REAL-APP render tool: no-overlap cannot reproduce the curve; overlap-on-translucent shows the dark triangle.

## Fix (web/index.html)
1. **Option A overlap geometry** (matches the mockup): `.msg-bd::before` = colored wing 20x20 at left/right:-8, radius 18px 16px, z-index:-2; `.msg-bd::after` = --k-bg ground mask 14x22 at -14, radius 12px 16px, z-index:-1; tail-side bottom corner 6px.
2. **`--usermsg-tint` made SOLID** (pre-composited over each theme's ground) so the overlapping wing does NOT double-composite: light `#e8ebf5` (:120), dark `#141c2f` (:330 + the generated forced-dark twin :7661), navy `#1a2d58` (:7806). Visually identical over the standard ground; it only stops the tail's dark triangle. This reverses #2660's translucent choice for the tint (a design call, reversible; matches the approved solid mockup; imperceptible; flagged to Angel since her #3264 + the DM `.dm-b` also read the token).
3. **Width cap**: `.msg-bd { max-width: 52ch }` (:4758) so messages sit in from both edges. Previously only `#panel-detail .msg-b { max-width: 66ch }` (tab view) capped it; the consolidated view had NO cap -> full-width bubbles (Josh's "too wide on both sides"). The base cap covers both views.
4. **WHITE ground on BOTH views** (Josh 2026-09-18, verbatim "the background... whether it's on the consolidated view or the tab view, to be white"): tab `.thread` already --k-surface (#2711); consolidated `.pjmid` (:4196) + its `.composer` (:4231) flip --k-bg -> --k-surface (reverses #980's cream). BOTH tail carves `.msg:not(.you) .msg-bd::after` (:4819) AND `.msg.you .msg-bd::after` (:4820) are the GLOBAL rule at --k-surface, so carve == ground == --k-surface on both views and both themes -> no dark tail sliver (the operator's-own-tail sliver Angel's opus pass caught is covered here by construction).
5. **Agent cream** `--agent-msg` (light) #f9f7f1 -> **#f4f1ea** (:114), the approved-mockup Option A value. On the white ground the old cream dissolved; #f4f1ea reads as a clear subtle bubble. Dark/navy unchanged.

## Supersedes #3264/#3266 (Angel)
This unified change replaces Angel's consolidated-only #3264. The GLOBAL carve flip (both carves -> --k-surface) covers both views + both carves in one rule, and also fixes the latent tab-view dark tail sliver #3264's consolidated-scoped overrides left in place (at origin the tab ground was --k-surface but the global carve was --k-bg). Angel is off the message CSS (on tmux #3113), #3266 to be closed; her opus catch (the `.msg.you` carve) is carried. Confirmed clean with Splinter + Angel.

## Verification (REAL app render, not a mock -- Josh's explicit requirement)
Rendered the actual worktree web/index.html via `/tmp/shot-app-bubbles.cjs` (real pjRoomRow, real tokens): light /tmp/app-final-light.png + dark /tmp/app-final-dark.png. Confirmed: clean Option A curved wing on the blue bubble, NO dark triangle, agent cream reads on white, avatar bottom-baseline, bubbles capped with margin both sides, WHITE ground both themes. Sent to Josh for his cream GO before merge.
- Browser-check `render-room-msgbox-2806` all-passed light+dark: recalibrated the operator-blue floor 20->8 because the SOLID tint makes getComputedStyle return the true painted pixel (light lead 10, dark 19) rather than the old translucent raw-blue (~114); the >=20 distinctness guard is untouched; widened the no-seam pixel window to the Option A wing footprint. This check ships with the change.
- Unit tests: `web.consolidated-match-mock.test.js` updated (the .pjmid ground pin --k-bg -> --k-surface) + `web.consolidated-980/867` green; token-adjacent checks (render-agent-msg-gray-2805, render-mention-blue-2922, render-plus-blue-1615) green.

## Weakest premise
That solid tint is acceptable vs #2660's translucent (imperceptible over the standard ground, matches the approved mockup, but IS a reversal of a deliberate choice -- Josh can undo). That #f4f1ea is the right cream (it is his approved-mockup value; he was offered A/B/C/D and can go deeper). That 52ch is the right width (a render-judged value; easy to retune). That recalibrating the tail check's blue floor to 8 is correct and not weakening (it tracks the solid painted pixel and still rejects gray/cream; the distinctness guard stays strong) -- the reviewer should confirm the floor still reds a genuinely non-blue operator bubble.
