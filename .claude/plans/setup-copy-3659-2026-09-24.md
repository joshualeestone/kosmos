# Plan: #3659 -- the final setup page copy (April)

Author: April, 2026-09-24. Branch `setup-copy-3659`, off main.

## Josh's copy (card, verbatim)
- Eyebrow `SETUP COMPLETE`. Written as `Setup complete`: `.fc-eyebrow` uppercases, so the person
  sees SETUP COMPLETE. Both the static pane-9 markup (now `id="fr-fleet-eyebrow"`) and
  `frPaintFleet()` set it, so no path through the paint leaves the old "Your agents".
- Headline unchanged: "You're ready to start using Kosmos."
- ONE body line replacing both paragraphs: "Head to your dashboard to create or import agents,
  set up your projects, and start building your next big idea."
- Button stays "Giddy Up". One page serves Mac and Windows (web/index.html only), for 0.6.93.

## Decisions (reversible)
- The #2497 manual-import POINTER paragraph is retired: Josh's line says "create or import" and
  replaces both paragraphs. Its test is rewritten to pin the new line, and the stale #3575 and
  #2497 comments in frPaintFleet are removed so the source no longer claims the old copy.
- Weakest premise: that "both current paragraphs" includes the #2497 pointer. It is the second
  paragraph on the create path, so it does; restoring it is a one-commit revert.

## Verification
- Source tests pinning the copy: server.test.js, web.found-every-path-1493,
  web.firstrun-panecount-9screen (the new test scopes its no-old-copy check to the rendered
  statement, not the unreachable arms kept after the forced return).
- render-first-run browser check: body line exact, old copy absent, eyebrow reads SETUP COMPLETE
  (innerText, so the CSS uppercase is what is compared). A planted-old-copy control reddened all
  three assertions in both schemes.
