# Plan: win32-roadmap-r8b-570 -- record R8b and the v1 Windows release in the roadmap

Docs only. `.claude/plans/WINDOWS-ROADMAP.md` still said "installkosmos.com still
serves 0.6.37" and "#2754 not yet merged", and it had no record of the second real
reboot (R8b) or of the release. A new session reads that file first, so a stale
state there sends it to redo finished work.

## What changes

- **The one-line state:** v1 shipped. It gives the served version, sha and source
  sha, how the bytes were verified, and the four merged PRs. R8b passed. It lists
  what is still open.
- **§2, capability rows 4, 6 and 7:**
  - #2754 is merged;
  - R8b is the logon test on the zip install;
  - the Node-runtime anchor limit.
- **§2:** an R8b line under the 7c-6 R table.
- **§3c:** BLOCKER 5 is marked merged, and "what is still open" gives the served
  version.
- **§5:**
  - a second update by hand (Baron's bytes over the fbe246e0 install);
  - the Node-runtime hand-off limit, now with a measurement;
  - the named-world board-token gap;
  - a PE `kosmos` shim cannot by itself fix PowerShell 5.1's quote handling on
    native command lines.
- **§6 sequencing:** DONE, NOW, THEN and LATER, updated.

## Evidence (all measured on the Windows box, 2026-09-11)

- **R8b, logon 13:01 UTC:**
  - `\Kosmos\board` and 5 agent tasks were Running, with one
    `conhost --headless` supervisor each and no Kosmos windows;
  - `claude agents --json` listed 5 agents, and all 5 cards were idle;
  - the board answered `0.6.55+fbe246e024a7@default`;
  - `multiline-check.js reh-a` got the two lines intact on the board;
  - `msg-check.js reh-a reh-b` passed.
- **Baron's zip:**
  - `verify-candidate.js`: PASS (sha, clean manifest, 132/132 identical);
  - installed through Shell.Application CopyHere with ZoneId=3 on every file;
  - Kosmos.exe exited 0 in 3.2s;
  - e2e-zip.js e2e-7 passed Z0-Z6, and multiline-check and msg-check passed.
- **The site:** latest-win.json, both zips and the .sha256 were downloaded and
  hashed.
- **The Node-runtime lock:**
  - a running node.exe copy cannot be overwritten (`being used by another
    process`), but it can be renamed, and a new file can then take its name while
    the process keeps running;
  - the renamed file can be deleted only after the process exits;
  - control: overwriting an idle copy succeeded.

## Tests

None: docs only (CLAUDE.md exempts docs from the test requirement, not from the
plan or the review).

## Review log

(filled in as the rounds run)
