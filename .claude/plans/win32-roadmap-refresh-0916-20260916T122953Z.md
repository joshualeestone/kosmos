# Plan: win32-roadmap-refresh-0916

## Why
`.claude/plans/WINDOWS-ROADMAP.md` is the "resume here" doc a new Windows-lane session
reads first. Its RESUME block and one-line state are dated 2026-09-12 and are now stale:
they predate the updater arc S0-S5 completing+arming on main, the fresh 0.6.69 build, and
the resolution of several "open" items. A new session (this box BSODs often, so cold
resumes are frequent) would act on wrong state — e.g. re-chase the `.plist`/frClaude copy
bugs that are already fixed, or treat the `.ps1` exec-policy edge as a live blocker.

## What
Docs-only. Update WINDOWS-ROADMAP.md:
1. Refresh the ⚑ RESUME HERE block: add the S0-S5 updater-arc-complete state, the 0.6.69
   verified+boot-proven build, the staging-publish gate + V1-ready, the Thursday clean-box
   laptop, and DUNS->Azure signing in progress.
2. Refresh "The one-line state" with a dated 2026-09-16 entry (keep the prior ones as
   history — this file's convention is to append dated states, not delete).
3. Correct the "What is still open" list: clean-box first run is now scheduled (Thursday
   laptop, not the admin/reboot Windows Sandbox path); note the updater is no longer a
   pure fast-follow (arc complete on main, awaiting staging->V-checks); mark the `.ps1`
   exec-policy item as a measured non-issue for the agent path (not a blocker).

No behavior change, no code touched, no test needed (docs-only per CLAUDE.md). Still
requires this plan + a /challenge-loop.

## Risk
Low. If any claim is wrong it misleads a future session, so every updated fact is one I
verified on-box this session (build sha, boot proof, first-run certification) or read from
current code / live pointers.

## Verification
- `git diff` shows only WINDOWS-ROADMAP.md + this plan.
- Re-read the RESUME block for internal consistency (shas, versions, dates).
- /challenge-loop to convergence, then PR.
