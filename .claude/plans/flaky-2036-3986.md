# flaky-2036-3986: the #2036 hang tests stop racing a 300ms clock

Card: kosmos#3986 (mine: I wrote the test with #2036).

## What was actually wrong
The FLAKE was mechanism 2 (measured). Mechanism 1 is a real but narrow ordering issue found on the
way, exercised only by the new synthetic test:
1. PRODUCT (reasoned, then pinned by a synthetic test): after a board call times out, tools/plus-signin-fresh.js sent the cleanup
   `signin-cancel` on the SAME budget (KOSMOS_PLUS_CALL_MS). A timeout is the sign the board is
   slow, so that cancel was the call most likely to be abandoned, leaving a half sign-in. In
   production the budget is 15s and the 5s floor is inert; it matters only when the budget is set
   under 5s. Now `CLEANUP_MS = max(CALL_MS, 5s)` for all six cleanup cancels (two of them follow a timed-out
   call; the other four get the same floor for one rule).
2. TEST (measured; this was the flake): at 300ms, the runner's EARLIER calls (board identity, /api/remote) timed out on a loaded
   machine, so it exited as "setup, nothing recorded" before ever reaching the hanging call. That
   is the code the test expects, so only the final "cancelled?" assertion noticed. Seen directly:
   run 24 of a load sweep failed both hang tests in ~600ms with "never cancelled", far inside the
   new 5s floor. Now the hang tests use HANG_MS = 2000 and assert the hanging call was REACHED
   (signin-start / signin-verify / signin-register), so a run that stops early now says which call
   it never reached instead of passing or failing on a later assertion.

## Tests
- New: a fake board answering the cancel after HANG_MS + 1s; the runner must wait for it. Reverting
  the floor reds it ("abandoned the cancel"), measured.
- Load: 20 runs of the whole file alongside a full validation (load avg 16-28): 0 failures after
  the fix; before it, 2 of 40 (both hang tests together).

## Rejected
- Only widening the test budget: hides mechanism 1, which is a real ordering bug.
- Making the fake signal completion without a budget: the runner's budget IS what is under test.

## Weakest premise
2000ms is a margin, not a guarantee: a machine slow enough to take 2s for a local HTTP round trip
would still fail, but now with a message naming the call that was never reached.

## Iteration 1 (opus)
- WARNING: the register-timeout test (REGISTER_MS 300) could pass without the register arriving
  --> HANG_MS, a reached assertion, and forget must retire a real registration.
- WARNING: the late-cancel delay tracked HANG_MS while the floor is fixed --> a fixed 3000ms, with
  an assertion that it lands after HANG_MS and inside the floor.
- NITs: the plan and the comment overstated mechanism 1 --> reworded (the floor is inert at the
  default); "can never" --> reworded; no test bounded the cleanup --> a cancel that never answers
  must still end within HANG_MS + 5s + slack.
- The bound test's 10-minute late answer held the process open after the file finished (my
  slip, caught when a run hung): the fake board's timer is unref'd. A 20s cleanup budget reds the
  bound test ("waited 22107ms"), measured.
- Cost: the file runs ~24s (was ~3s). Plan-name timestamp: left as the repo's prevailing form.

## Iteration 2 (sonnet)
- WARNING: the comment said every cleanup follows a timeout; two of six do --> reworded, here too.
- WARNING: the bound test lacked the reached assertion its siblings have --> added.
- CONVENTION: NEVER_MS and BOUND_SLACK_MS named; NIT: the forget message covers a failed forget.
