---
branch: taskcard-agents-3131
---
# Plan: #3131 task-card stroke + agents-column red-! only (Josh 6.68, consolidated view)

## Goal
Josh 6.68 feedback, consolidated view:
1. Remove the stroke/border around task cards.
2. Agents column: do NOT show waiting/idle/busy text; show ONLY a red triangle + exclamation
   when an agent needs something.

## What changed (web/index.html)
1. `body.consolidated .tkcard, body.consolidated .tkcard:hover { border-color: transparent }`
   -- drops the task-card stroke in the CONSOLIDATED view only. The base `.tkcard` border
   (~6539) stays in the tab/grid project detail and the View-all-tasks screen, scoped the same
   way the container cards (.pjcard/.pjcol) already scope their borders by layout. border-color
   :transparent keeps the 1px box so nothing shifts; the focus-visible outline (a11y) is
   untouched. DECISION: consolidated-scoped per the card's "consolidated view:" framing; global
   is a one-line change if Josh wants it everywhere.
2. `pjMember(m, suppressTold, withMinus, hideState)` -- added an optional 4th param. The project
   Agents-column roster call (paintOneProject -> #pj-one-agents) now passes hideState=true, which
   OMITS the per-member STATE label `<small>` (Working/Idle/Waiting/Issue...). KEPT: the red
   needs-you triangle (`warn`, over the avatar), the state colour WASH (pjm-working/attn/idle),
   and the not-present "cannot see this agent" reason. The Settings members list
   (paintSettingsMembers -> #pjs-members, pjMember(m,false,true)) is UNCHANGED (still labels).
   DECISIONS: (a) removed TEXT only, kept the washes (the card names "text"; the washes are the
   #2711/#2699 Josh-approved colour system; "only red-!" could also mean strip the washes -> a
   one-line follow-up if Josh says so); (b) scoped to the Agents column via the flag, not a
   global pjMember change, so Settings keeps its labels.

## Backward-compatibility / blast radius
- The 4th param is optional, so every existing 3-arg pjMember call is unchanged. Node tests pass
  as-is: web.project-page 9/9, web.needsyou-dealarm-2808 6/6, web.consolidated-980 13/13. The
  needsyou-dealarm tests exercise cardStOf/stateCopyOf/CARD_ST/STATE_COPY (functions unchanged).
- The two needs-you browser-checks (render-project-needsyou-2699, render-needsyou-dealarm-2808)
  call pjMember with 3 args, so they still render the label and pass unchanged. No browser-check
  asserted the REAL roster's status label, so nothing broke.

## Test / browser-check added
- docs/browser-checks/render-project-needsyou-2699.js: added a 4th arm rendering a needs-you
  member with hideState=true (the way the real Agents column renders). Asserts the red triangle
  STILL shows while the state `<small>` label is DROPPED. Verified LOCALLY against the pinned-PW
  runtime. Also satisfies the #1720 gate (a docs/browser-checks assertion change for the web/ diff).

## Risk
- Isolated to the consolidated task-card border + the Agents-column roster label. No engine change.
- web/ change -> #1720 browser-check gate satisfied by the render-project-needsyou-2699.js arm.
- Merge-as-green once CI (browser-checks + test) is green.

## Weakest premise
That "Agents column" = the #pj-one-agents project roster (both tab + consolidated), and Josh wants
the labels gone there but kept in Settings. If he meant only the consolidated instance, or wants
Settings cleaned too, the flag makes either a small follow-up.
