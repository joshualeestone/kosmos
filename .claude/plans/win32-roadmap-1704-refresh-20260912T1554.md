# Plan: win32-roadmap-1704-refresh -- bring the Windows roadmap up to date (2026-09-12)

Documentation only. One file changes: `.claude/plans/WINDOWS-ROADMAP.md`.

## Why

The roadmap was last updated by #2823 (03f7169e, 2026-09-11). Since then, main has
landed the two items its §6 named NOW and NEXT, and a live check has run on the
Windows box. So the header's "What is still open" list, §5 and §6 now describe
open work that is done. Anyone resuming from the RESUME block would pick up
finished work. A roadmap line that asserts what the code does not do is the
convention-5 defect in prose, so every changed line below cites a commit, a PR
or a file:line on main (f120a6e2 at the time of writing).

## Claims changed, with evidence

1. **The anchor swap is on main, not in flight.** The header list, "Do this next"
   item 5, §2 row 7, the §5 Node-runtime bullet and §6 NOW all say
   `win32-anchor-swap-570` is in flight.
   - Evidence: 03b99146 "win32-anchor-swap-570 -- ... (#2825)", merged
     2026-09-11T20:47Z; `engine/win32anchor.js` `replaceInterpreter` (:259),
     `retireLeftoverInterpreters` (:303), called from `ensureAnchored`
     (:371, :373).
   - Limits, recorded rather than dropped: PR #2825's "Not done here" says the
     live swap on the box's real anchor was NOT run (Claude Code's permission
     check declined it pending Josh's go). The proof is
     `engine/win32anchor.test.js`, with a real running interpreter (341/341 win32
     suites on the box, per the PR). It is also not in any served Windows zip:
     `https://installkosmos.com/dist/latest-win.json` still names 0.6.55
     (fetched 2026-09-12).
2. **A named world's board token for agents is done.** The §5 bullet "A named
   world's board token does not reach its agents" and §6 NEXT describe it as
   open. The #1704 set closed it:
   - #2845 (a677a0b5), Windows world identity: the anchored boot shim applies
     `KOSMOS_WORLD` before the supervisor loads (`engine/win32anchor.js:175-180`,
     `worlds.applyAgentWorldEnv`, `engine/worlds.js:242`);
   - #2852 (fd19bb6e), a board can boot into a named world;
   - #2874 (1b3b81b0), Mac world identity: `bin/agent-supervisor.sh:318-324`
     calls `applyAgentWorldEnv` and hands each pane `KOSMOS_WORLD` (:450);
   - #2877 (e1e91030), the switch asks whether to pause or keep this Kosmos's
     agents;
   - #2879 (136172a7), a kept-running agent's sends go to its own Kosmos's
     outbox: `engine/outbox.js`, and the 421 `wrongWorld` refusal in
     `server.js:1401-1437`;
   - #2886 (e07ec774), lifts #2849's (718c72b8) named-world spawn guard
     (`server.js:281`);
   - #2892 (5aa75512), import at New Kosmos and from a settings cog.
   The bullet becomes resolved, with a short history line. The 📌 IMPORT note
   stays as it is.
3. **Live evidence, new.** `C:\Users\joshu\kosmos-scripts\live-1704-check.js`
   ran on the Windows box on 2026-09-12 (log last written 14:37 UTC) against a
   detached worktree of main at 5aa75512 (0.6.59, `package.json` at that sha).
   The log, `C:\Users\joshu\kosmos-scripts\live-1704-log.txt`, reads
   `RESULT: PASS`, with 18 PASS lines: 1, 2a-2d, 3a-3c, 4, 5, 6a-6c, 7a-7c,
   8a-8b. It is summarised in §5.
4. **New product gap: no way to delete a Kosmos.** `engine/worlds.js` exports
   `createWorld` (:340), `setActiveWorld` (:370) and `renameWorld` (:410), and no
   delete. The `/api/worlds*` routes in `server.js` are GET `/api/worlds` (:3083),
   GET `/api/worlds/list` (:3116), POST `/api/worlds` (:3125), `/active` (:3176),
   `/import` (:3308) and `/rename` (:3356). None deletes. A grep for
   `deleteWorld|removeWorld` over `engine/`, `server.js`, `web/index.html` and
   `install/kosmos` finds nothing. It is cross-platform. The live check's
   "Live1704" Kosmos (id `live1704`) stays on the box because of it (log, 67.2s
   "left behind"). Added to §5 and to §6 as a follow-up.
5. **Carried forward, still open:** the clean-box first run (waits on Josh:
   Windows Sandbox needs admin and a reboot), the real updater (a fast-follow),
   the `.ps1` execution-policy dependency, and a live Mac import-and-start
   (PR #2892's "Needs Angel (live Mac)": the Mac start path is proven only
   through the runner seam).
6. **New release rule.** Since 2026-09-12, every cut, Windows included, goes to
   STAGING first, is verified there, then goes to PROD. This is Josh's fleetwide
   policy, relayed by the coordinator; no file in the repo records it. It goes
   into the release handshake under "Do this next". Checkable caveat, recorded
   beside it: the staging-channel tooling (`docs/staging-channel.md`,
   `tools/promote-channel.sh`) moves `dist/latest-staging.json` to
   `dist/latest.json`, the Mac tarball's pointers. `tools/publish-kosmos-windows.sh`
   writes only `dist/latest-win.json`, and no Windows staging pointer exists in
   `tools/`. So a Windows staging cut has no pointer of its own yet.
7. **Header.** "The one-line state" moves to 2026-09-12, and the "already on
   main" list gains #2823, #2825 and the #1704 set.
8. **§3b's "Until `win32-kosmos-cli-570` merges"** becomes past tense, because it
   merged as #2754 (§3c). The rest of that parenthetical still holds:
   `engine/boardrestart.js:33-36` says `installedKosmosCli()` is not null on a
   Windows bundle, and `canSelfRestart` returns at its win32 check (:208) before
   asking.

## Not changed

Sections that are still accurate stay as they are. That covers §1, §3, §3a,
§3b's survey, §4, §7 and §8, and the 7c history. The "What is still open" note in
§3c (the served zip and the `.ps1` limit) is still accurate too.

## Checks

- Grep `docs/` and `.claude/plans/` for "anchor-swap", "board token" and "named
  world", to find any other doc that states a changed claim as current.
- `engine.reachable.test.js` and `one-derivation.test.js`, run with
  `node --test <explicit files>` on the box's runtime node, to confirm nothing
  else changed.
