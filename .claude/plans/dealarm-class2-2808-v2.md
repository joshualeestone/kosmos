# #2808 class 2: de-alarm an agent's own question (calm "Has a question", not red "Needs you")

Branch: `dealarm-class2-2808-v2` off origin/main. Card: joshualeestone/kosmos#2808. Ships 0.6.66 (top priority).

## The ruling (Josh, 2026-09-14 16:49, LOCKED)
Two classes wear the same red "Needs you" banner:
- **Class 1** (Claude Code's own permission/trust prompts): auto-approve + clear INVISIBLY. = PigeonPete (supervision layer, #2129, parked needs-operator).
- **Class 2** (an agent's OWN substantive question, e.g. an agent asking the operator to pick a visual direction): NOT auto-cleared. Keep the question, present it as a normal chat message, NOT a red "Needs you" alarm that reads as "app broken". Do NOT silently drop it (that stalls the fleet). = THIS BRANCH.

## What finished looks like
A `needs_you` an agent reported DELIBERATELY (self-report `by:'agent'`) renders as a calm "Has a question" card on the board (calm glyph, calm border, "Has a question" label), the question stays fully visible + answerable, and it is NOT the red "Needs you" alarm. A class-1 (`by:'auto'`) permission prompt, a scraped prompt, and an unknown-provenance/legacy report all STAY red (only the KNOWN agent case is calmed).

## Build (all in Angel's lane; disjoint from PigeonPete's class-1 supervision half)
1. **Plumb the classification** (`engine/status.js`): reconcileReport's self-reported needs_you return carries `by: reported.by || null` (5839); both card builds carry `stateReportedBy: status.by || null` (paneless 6024, pane 6539). Read-only passthrough of the `by` field selfreport.js already records (`auto`=class1 / `agent`=class2 / `operator` / null=legacy).
2. **De-alarm the render** (`web/index.html`): a new calm `question` treatment in the three shared derivations. `CARD_ST.question` = `{st:'question'}`, `STATE_COPY.question` = `{label:'Has a question', attn:false}`, `GLYPH.question` = a calm `.ask` "?" (not the red `.haz` "!"). `cardStOf`/`stateCopyOf` route a `state==='needs_you' && stateReportedBy==='agent'` card to `question` (predicate INLINED in both, byte-identical, so each painter is self-contained for the node tests that eval-extract it). `boardMods` gives it a calm `question` ground class. CSS: `.ask` / `.astate.st-question` / `.acard.question` in a calm blue (info), the forced-dark twin generated by `tools/sync-forced-theme.js`. The consolidated-view decorative-glyph hide rule includes `.ask` (so the status bubble Josh rejected 2026-08-26 does not reappear).
3. `answerBtn` is unchanged: a class-2 card KEEPS its inline "Answer" action (the operator should answer the question; it is functional, not an alarm).

## Rejected / deferred
- **Rejected: dropping class-2 from surfacing.** That is the #2146 false-calm regression (a real blocking question vanishing stalls the fleet). Josh: "do NOT drop these."
- **Rejected: reusing the `paused` treatment.** Semantically wrong (paused = the agent is stuck; a question = the agent needs a decision from YOU). It gets its own calm shape.
- **Rejected: treating `by===null` (legacy/unknown) as class-2.** selfreport.js:389 forbids it (absence is never "agent-typed"). De-alarm fires ONLY on the KNOWN `by==='agent'`; everything else stays red (the safe direction).
- **DEFERRED (weakest premise, documented on the card): whether the "Needs you" COUNT TILE and the PROJECT PILL should also drop a class-2 question from their red count.** This first-cut calms the CARD (Josh's literal QA complaint) and leaves the tile/pill counts UNCHANGED, because splitting `needsYou` reshapes hard-won #763 project-attribution + #1253's needs_you measurement, and the honest reading of the ruling on THAT is ambiguous (calm the card vs. calm every fleet surface). It is a small follow-up once Josh confirms the interpretation and Mona (design) + PigeonPete (#1253 owner) weigh in — both were unreachable/wedged during the build; Mona has since recovered.
- **DEFERRED: the exact VISUAL of the calm treatment is Mona Lisa's Kosmos-design domain.** This ships a defensible first-cut (calm, non-red, distinct from paused/unknown); the styling is one reversible block for her restyle. The plumbing + behavior beneath it will not need a refit.

## Verification
- `web.needsyou-dealarm-2808.test.js` (node): cardStOf/stateCopyOf return the calm `question` shape for `by:'agent'`; CONTROLS that `by:'auto'`, scraped (null), `by:'operator'`, and a non-needs_you `by:'agent'` all stay/behave red-or-unchanged.
- `engine/status.test.js` #2808 (node): reconcileReport carries `by` to the card; CONTROLS that `auto` stays `auto` and legacy stays `null`.
- `docs/browser-checks/render-needsyou-dealarm-2808.js` (Playwright, computed style): the class-2 card renders NON-RED (border color distinct from the alarm), calm `?` glyph, "Has a question"; CONTROLS that class-1 (auto) and scraped stay red. This is the browser-verify of Josh's visual complaint. PASS (headless; verdicts are computed DOM, not pixels).
- Golden card fixture re-captured (it now carries `stateReportedBy`); `web.consolidated-980` glyph count bumped 6->7 (the new `.ask` glyph); reason-grep EXPECTED_SITES 110->112 + EXPECTED_CATCH_SITES 78->80 (MEASURED); README indexes the new check.

## Process
Full gate: challenge-loop to convergence + PR, no human reviewer (Kosmos convention: agent merges own green). Coordinated on #2808: PigeonPete (class-1, disjoint), Mona (design restyle, post-cut), Splinter (PM, endorsed the first-cut). Note at PR time: Mona's #3075 (PR #3089) also edits web/index.html (S3 pane ~9757, disjoint from this change's regions ~1600/13800/14900); rebase/merge-check at create-pr.
