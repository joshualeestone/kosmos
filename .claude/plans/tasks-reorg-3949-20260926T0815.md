# tasks-reorg-3949: the Tasks page reorganised (Josh 2026-09-26 08:01)

Addresses #3949. Checklist and Josh's words are on the card. Mona owns the Tasks view (#3559) and reviews the design on the PR.

## Tile mapping (item 4, posted on the card before any code)
- **Needs Your Decision** (red): an open task whose holding agent (`claimWho`) needs the person right now. That is `status.needsPerson`: a question, a trust wait, or a connection given up on. For a question it also has to name this task's project (`stateProject`), which is the project page's needsYou rule (#763, #3726). Tied cards only (`isNamedOurs`), the same gate as the project members.
- **In progress** = `working`, **Assigned but not started** = `assigned`, **Unassigned** = `nobody`, **Completed** = `closed`.
- **Built but waiting**: not drawn. No task stores it. Follow-up is #3951.

## Change
### Engine
- `tasks.taskState(task)` returns `'decision'` for an open task whose `waitingOnPerson` is true, ahead of working and assigned. The view still renders the word and never computes it.
- The `/api/tasks?view=tasks` route sets `waitingOnPerson` from the roster it already reads: the card of `claimWho(t)`, tied, `needsPerson`, and for `needs_you`, `stateProject === t.projectId`.

### Page (web/index.html)
- The left Projects rail is removed, and the main body takes the full width.
- Projects becomes the existing `tsk-projsel` dropdown, shown at every width, on one line with **Created:**, a dropdown replacing the Added-in buttons (24h / 48h / 1 week / 1 month / All).
- Search is about half as wide, with the "N open tasks across M projects" line to its right.
- Five single-label tiles with no bylines. Josh named six; Built but waiting is not drawn (see the mapping). Needs Your Decision is red, and Completed is a tile again (Josh), where #3559 had Closed only as the fold.
- The "Tap a tile..." hint is gone. Tiles still filter on tap.
- **Group by** (a dropdown, default Status) and **Sort** move under the tiles.
- The group sections keep their one-line "why", and Needs Your Decision gets its own.

## Tests
- engine: `taskState` returns decision only when open and `waitingOnPerson`, and closed wins. The engine tests cover the rule arm by arm, with real fleet cards.
- route and browser: a real agent asks about the project (selfreport plus an asking pane). No stub. The route test also has a control agent asking about another project.
- page: the existing Tasks view tests are updated for the new structure. Checks: no rail; `tsk-projsel` and `tsk-win` are selects on one row; the tiles' order and labels, with no `.ts` byline; Completed is a tile; the hint is gone; Group by and Sort sit after the tiles.
- Browser: before and after shots, served.

## Weakest premise
A question belongs to the AGENT and names a project, not a task. An agent holding two tasks in one project lights both. That is the same granularity as the project page's red pill.

## Review round 1 (Opus)
- [WARNING] waitingOnPerson checked only the first open part's agent (claimWho), so a second agent's question on a shared task never lit the red tile. FIXED: every agent holding an open part counts, and a finished part's agent does not. Test (two parts, the second agent asking); it reds with first-holder-only.
- [WARNING] the Needs Your Decision line named a question or a go-ahead, but needsPerson also counts a connection Kosmos gave up on. FIXED: the line names it. The board's rule is unchanged.
- [WARNING] the Tasks view does not poll, so the red tile could go stale. FIXED: the board poll's roster hook (tskRosterChanged) re-reads the tasks when WHO needs the person changes (a question asked, answered, or moved to another project), and only while the view is shown. Test lifts the real functions; it reds without the re-read.
- [NIT] picking the Completed tile forced the fold open, and the toggle listener then kept it open after. FIXED: the listener ignores the fold while that tile is picked.
- [NIT] a comment still said "Assigned, not started". FIXED.
- [NIT] "PROJECT" has no colon while "CREATED:" does. DEFERRED: "Created:" is Josh's own label (#3949); the other labels are #3559's unchanged. Mona's design review is the place to settle it.
- [NIT] the require inside waitingOnPerson. Commented: status is loaded lazily from this layer, as projects.js does.

## Validation after round 1, and review round 2 (Sonnet)
Validation failed on two repo guards. Round 2 found both independently.
- [BLOCKER] browser-checks-selectors.test.js: the render check named `#tsk-projects` (to assert it is absent), and the guard reads any named id as one the page must have. FIXED: absence is now asserted by `aside, .tsk-rail`, since the rail was an aside.
- [BLOCKER] fixture-discipline.test.js: the new engine and web tests hand-built roster cards. FIXED: the cards are real, from test-support/fleet.
  - Questions come from selfreport.record, and the untied pane is fleet.stranger. Each fixture asserts the field it relies on.
  - The trust-wait and gave-up cases, and the web test's asking rows, are copies of a real card in the producer's own states.
- [WARNING] tskNeedsSig repeats status.needsPerson (only to decide when to re-read). Commented: it names needsPerson and the page's data-attn copy, where a new case also belongs. The count itself is the server's.
- [NIT] why a trust wait or a lost connection skips the project check. Commented at the line: only a question names a project.

## Review round 3 (Opus)
- [WARNING] neither the route test nor the browser check ran the real rule (both stubbed waitingOnPerson), and my stated reason (the fixture cannot make a question name a project) was false: selfreport.record plus an asking pane does it. FIXED:
  - The route test uses real agents, one asking about this project (decision) and a control asking about another (assigned), then both reporting working again (no decision). It reds when the route drops the rule.
  - The browser check's Max asks about Spring launch for real. Its comment and the README are corrected.
- [NIT] the "who needs the person" baseline was set on the first poll after the read, so a question asked between the read and that poll was missed. FIXED: tskLoad records it at the read.
- [NIT] a zero Needs Your Decision was drawn red. FIXED: red only when its count is above 0 (data-n). A web assertion checks the style; the browser check has a zero arm (a search leaves it at 0, and it is not red).
- [NIT] the re-read signature counted untied panes. FIXED: it skips `isNamedOurs === false`, as the server does.
- [NIT] every paint rewrote the three dropdowns' options, which could close one the person had open. FIXED: tskOptions rewrites them only when they change, and keeps the value in step.
- [CONVENTION] the "PROJECT" / "CREATED:" punctuation: already DEFERRED to Mona's design review.

## Review round 4 (Sonnet)
- [WARNING] CLAUDE.md's "Where to Find Things" row for the Tasks view listed four states and not waitingOnPerson. FIXED: it names `decision` and `tasks.waitingOnPerson`, and the route's added `waitingOnPerson` field.
- [NIT] the 480px row threshold in the browser check had no reason given. Commented: the pairs wrap by flex-wrap only; measured at 390 (wraps) and 760 (fits).
- [NIT] render-alltasks rebuilt a function from its source to evaluate it. Simplified to a plain evaluate.

## Review round 5 (Opus)
- [BLOCKER] renaming the fold Closed to Completed broke render-subtasks-3861.js, which found closed rows by matching /Closed/ on the heading. Two of its filters then silently stopped excluding closed rows, so assertions kept passing with weaker guarantees. FIXED:
  - The check reads the fold by its element (`folded: !!r.closest('.tsk-fold')`), not its label.
  - A new arm asserts the open order leaves out the finished rows.
  - render-tasks.js's fold messages now say Completed.
  - Swept every browser check for text that reads the old labels; only these two did. Re-run headless: subtasks 49 pass, render-tasks OK, tasks-view 147 pass.
- [WARNING] tskRosterChanged advanced the baseline even when it did not re-read (busy closing tasks), so a question arriving during a bulk close was never read. FIXED: the baseline moves only with a read (tskLoad records it). Test arm (busy, then free); reds with the old baseline.
- [WARNING] tskNeedsSig is a third copy of needsPerson on the page. DEFERRED: the page inlines this rule in seven painters on purpose (#3410, "its painters stay self-contained", and tests lift painters one at a time). The comment names every place a new case belongs.
- [NIT] "a question about this project" reads oddly on All tasks. FIXED: "its project".
- [NIT] stateProject is not filtered through knownIds as the project page does. Noted: the same result whenever the ids match, and a task's projectId is always a known project.
- [NIT] the plan did not list render-subtasks-3861 or render-tasks. Listed above.

## Rebase onto main, and the review after it
Rebased onto origin/main a4007bb (conflicts only in docs/browser-checks/README.md, twice, where main rewrote the render-agent-files-3614 row beside the render-tasks-view-3559 row: kept both). Main's other web/index.html changes do not touch the Tasks view. Re-run on the rebased branch: 77 unit/guard tests; browser checks tasks-view 147, alltasks 30, subtasks 49.
- [WARNING] Needs Your Decision counts tasks, not questions: one question can show as several. DEFERRED, and recorded on #3949 for Mona's review. It is the project page's granularity, and it keeps the tiles adding up to the open count.
- [WARNING] a zero decision tile kept a red dot (the inline --tsk-c beat the :not([data-n="0"]) CSS). FIXED in the painter: a neutral dot at zero. The browser check's zero arm also asserts the dot, and it reds with the old painter ({"redDot":true}).
- [NIT] tskOptions' comment overclaimed: a read that changes a count does rewrite the project list. Narrowed.
- [NIT] tskNeedsSig used isNamedOurs !== false where the server uses === true. Aligned.
- [NIT] render-alltasks header still said "rail item"; the palette comment used the old labels and had no red. Both corrected.
- [NIT] the forced-open Completed fold reopens on a repaint. DEFERRED: follows from the design.
- [CONVENTION] label casing and punctuation. Already DEFERRED to Mona's review.

## Mona's design review (PR #3983, approve), and what I changed
- Labels: "Project:", "Created:", "Group by:", "Sort:", in ordinary case (no letter-spaced capitals), so Josh's
  "Created:" reads as he wrote it and the four match. Weakest premise (hers): the colon on the other three.
- "All projects" carries no count: beside "N open tasks across M projects" it read as a number of projects.
  Each project's own option keeps its count. render-alltasks' #1346 arm now asserts the plain label.
- The Nobody yet chip is dropped inside a status group (the Unassigned heading says it); grouped by project,
  where states mix, it stays. The browser check asserts both arms.
- Phone (390): each pair still wraps. Measured from the shot: Project: plus its select and Created: plus its
  select need about 370px in a 296px row. Mona: wrapping is fine, do not shrink the text. Left as is.
- Needs Your Decision keeps counting tasks (her call on Q2).

## Review round 9 (Opus, after the design review)
- [WARNING] an unreadable roster made waitingOnPerson false for every task, so the red tile read 0 as a fact
  --> FIXED: the route sends rosterUnreadable, and the tile shows "?" with a reason ("could not read your agents
  just now"), neutral, not 0. Browser check drives the page's flag with Max's question still in the data (it
  would read 1 and red if ignored); reds with the flag ignored. Route test asserts false on a good read; reds
  when the field is dropped. Weakest premise: the unreadable arm of the route itself is not driven (safeRoster
  only fails when the snapshot throws).
- [WARNING] Created: can hide a pending decision --> DECIDED, kept: every tile counts inside the same filters,
  so the tiles add up to the count above them; Created: hiding an older task is what that filter means. The
  red attention on the project page and the agent list is unfiltered. Would change: Josh reading the 0 as
  "nobody needs me" while a filter is set.
- [NIT] the tskVisible comment sat above tskOptions; the All projects count comments were stale --> FIXED.
- [NIT] a test comment called hand-set states "real producer states" --> FIXED.
- [NIT] the Completed fold reopens on a repaint while its tile is picked --> DEFERRED (as before).
- [NIT] a finished task that never had an agent says "Nobody yet" in the Completed fold --> DEFERRED: it predates
  this change; the fixture has no such task.

## Review round 10 (Sonnet)
- [BLOCKER] the Needs Your Decision group heading kept a red dot at zero (round 7 fixed only the tile) --> FIXED:
  tskDot(g, n), one rule for the tile and the heading. Browser check reads the heading's dot at zero; reds with
  the old heading.
- [BLOCKER] with the agents unreadable, the heading said "0" with its usual sentence, and its list "Nothing here",
  under a tile saying "?" --> FIXED: the heading shows "?" and the cannot-tell sentence; with nothing listed the
  group is only that heading. Two browser arms (with Max's question in the data, and with a search leaving
  nothing); reds when the heading ignores the flag.
- [WARNING] counts tasks, not questions --> already DEFERRED and settled by Mona's review (keep).

## Review round 11 (Opus)
- [WARNING] "your go-ahead" was promised, but a trust wait never reaches this route's roster (the status route
  builds those rows offline), the gap projects.js already names --> DECIDED: keep the rule (it covers a trust wait
  if one arrives), drop "your go-ahead" from the page's sentence, and say the gap in waitingOnPerson's doc and the
  test. Rejected: building the route's roster the way /api/status does (a second derivation of the fleet, the
  habit CLAUDE.md warns against). Would change: a trust wait reaching snapshot().
- [WARNING] the unreadable roster was tested only with a hand-set flag --> FIXED: a server test makes the pane
  source throw and expects rosterUnreadable true and no decision (control: false after); a browser arm answers the
  route with rosterUnreadable through page.route and lets tskLoad read it. Reds when the page drops the field.
- [WARNING] the All projects check had weakened to "label and any rows" --> FIXED: every project option's "(N)"
  equals what picking it shows (#1346). Reds with an off-by-one count.
- [NIT] a failed reload kept the new baseline, so the tile stayed stale --> FIXED: a failed read sets a baseline
  no signature equals, and the next tick reads again. Test (lifted tskRosterChanged); reds without it.
- [NIT] the Completed fold snaps open while its tile is picked --> DEFERRED (as before).
- [NIT] other tiles do not caveat an unreadable roster --> DEFERRED: the decision tile and group carry it; the
  other states come from task data and claims, which the route reports on its own (cannot tell whether it started).
