# Plan: reframe the signed_in_unverified pill copy (#3136 copy piece)

Josh saw "Signed in - not recently checked" on his 5 CONNECTED Claude accounts and read it as
"not connected" though he was. ICK's #3145 landed an on-demand "Check now" probe. My lane (copy):
reframe the pill to a neutral "Signed in", move the nuance to the tooltip, nudge to Check now.

## Done-condition
- The signed_in_unverified pill reads a NEUTRAL "Signed in" (not scanning as disconnected).
- The "not verified live" nuance is in the tooltip (unverifiedWhy) and nudges toward Check now.
- 🛑 #874 preserved: the dot stays MUTED (acct-unknown), NEVER green; no claim the account "works".

## Approach (chosen)
Copy-only, two edits in web/index.html's signed_in_unverified branch: visible text
"Signed in - not recently checked" -> "Signed in"; unverifiedWhy reworded to carry the nuance +
"Use Check now to verify it". Class unchanged (acct-unknown, muted). Tests + the
render-account-badge-1921 browser-check updated to pin the reframe (neutral pill, old text OUT of
the pill, nuance IN the tooltip) while KEEPING the not-green honesty pin + Check now.

## Rejected
- Greening the pill / showing "Connected": reintroduces the #874 lie (a rejected token sat green).
- Auto-probe on load to verify: #1921 rejects a background probe; Check now is user-initiated.

## Weakest premise
That a neutral "Signed in" (muted dot) is distinguishable enough from the green working
"Signed in - active" state that a person does not mistake unverified for verified-live. Mitigated:
the working state adds "- active <age>" and a green dot; the unverified is plain + muted; the
tooltip + Check now disambiguate. If Josh wants a stronger visual distinction, that is a follow-up.

## Verification
- Node tests (web.badge-observed-1921, web.conn-live, server.badge-observed-1921): 21/21. The
  honesty pin (not acct-connected) + the reframe assertions pass and are non-vacuous.
- Browser-check render-account-badge-1921 (headless): unver@ renders cls=acct-unknown (muted),
  text "Signed in", title carries the nuance + Check now, checkNow present, honesty (not green).
- Challenge-loop to convergence.
- #3136 stays OPEN for the post-cut deployed-live verify (Check now greens a real account / reddens
  a dead one on the live Settings > AI Models panel).
