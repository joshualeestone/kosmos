# Plan: 6.68 margins + remove sub-project count (#3133)

## The ask
Josh, 6.68 feedback:
1. Consolidated "View all tasks": add more TOP margin (the project name is too close to the top).
2. Tab view projects-list: add RIGHT margin (projects run into the far-right border).
3. Tab projects-list: REMOVE the "X sub-projects" count (the sub-projects are listed right beneath it).

## Scope of this PR
Parts 1 and 3. **Part 2 is parked needs-browser** and NOT in this PR (see below).

## What finished looks like
- The consolidated View-all-tasks view has clear top breathing room above the project name.
- No "X sub-projects" count renders on projects-list rows (it was already hidden in the
  consolidated rail; this removes it from the tab list too).
- Tests + render-subprojects-1994 browser-check green.

## The change
- **Part 1 (web/index.html CSS):** add `padding-top: var(--space-8)` (24px, matching the existing
  right inset) scoped to `html[data-layout="consolidated"] body.consolidated #pj-alltasks-view`.
  Scoped to alltasks only, not the shared docs-view rule (Josh flagged only alltasks).
- **Part 3 (web/index.html JS + CSS):** `subCount = ''` in the projects-row render (the `.pjsub`
  span is no longer produced); `subs` is kept because the consolidated fold caret uses subs > 0.
  The now-dead `.pjsub` base CSS and its consolidated `display:none` are removed.
- **Part 3 (docs/browser-checks/render-subprojects-1994.js):** the parent-row assertions flip from
  "shows N sub-projects" to "shows no sub count"; `pjsub` dropped from the Browser-check-surface list.

## Verification
- render-subprojects-1994 browser-check run locally (pinned PW): all page checks passed.
- Full node suite via tools/run-tests.sh.

## Part 2 - parked needs-browser (not in this PR)
The "tab projects-list runs into the far-right border" symptom did not reproduce in a headless
render: at 1280px with a few projects the list rows sit ~936px from the view's right edge (grid
cards, left-aligned), so the collision needs a full grid row or a specific list-view/content state
I could not confidently reproduce or identify. Parked so a browser session (or Josh in-app)
resolves which view/state shows it and adds the right margin there. The projects-list container is
#pj-list-view / #pj-list (web/index.html ~11025/11111); its view spans the 24px page gutters
symmetrically at 1280px.

## Weakest premises
- Part 1: var(--space-8) (24px) is a directional "more top margin"; if Josh wants more/less it is a
  one-token nudge.
- Part 3: removing the count from the render (not just hiding it) is the complete removal; the
  sub-projects remain visible nested beneath each parent, so the count is redundant as Josh states.
