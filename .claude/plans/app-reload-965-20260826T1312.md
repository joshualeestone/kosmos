# app-reload-965: Cmd-R reloads the native Kosmos window (kosmos#965, native-shell half)

## Why

Josh, 2026-08-26 07:23 CDT, with a screenshot: the native app window (the #677 app shell)
has no way to refresh except relaunching the whole app. Cmd-R does nothing, and there is
no browser chrome to fall back to. "Sometimes I feel like I'm stuck in a spot and I need a
refresh to see something else or I'm stuck on a modal or something."

Issue kosmos#965 splits the work: the toolbar refresh icon is Mona Lisa's (web/index.html);
the keyboard/menu binding in the native shell is this branch. Either half alone satisfies
the issue per its "Done when"; this half is the one that works even when the page itself is
too wedged to show a toolbar.

## What

All changes in `native-app/main.swift`.

1. **A View menu with a Reload item, keyEquivalent "r"** (default modifier Cmd), inserted
   after the Edit menu, matching the standard macOS menu order (App, Edit, View). Action:
   a new `@objc func reloadBoard(_:)` on AppDelegate.

2. **`reloadBoard` picks the right kind of reload:**
   - If the web view has a committed page AND the last navigation did not fail:
     `webView.reload()`. The plain case: refresh the board page, clear any stuck page
     modal or stale screen.
   - Otherwise (never loaded, or the last navigation failed): re-run `loadBoard()`.
     `webView.reload()` on a nil/failed page is a no-op or repeats the failure without
     retrying the board; `loadBoard()` re-runs the full resolve-and-start path, so if the
     board process itself died, Cmd-R restarts it and reconnects. This is the actual
     "stuck screen" Josh described: the recovery is the board start, not just the page
     fetch.

3. **Track load failure state** for the decision above: a `lastLoadFailed` flag set in the
   existing `didFail` / `didFailProvisionalNavigation` delegate methods, cleared in
   `didFinish` and at the top of each `loadBoard()` attempt. The delegate methods already
   exist for logging; this adds one assignment to each, no new delegate surface.

4. **`logLine` instrumentation** on both reload branches, same style as every other
   decision point in the file, so a headed test (and any future field report) can prove
   which branch ran from the log alone.

5. **Test seam, matching the file's established env-var seam pattern**
   (`KOSMOS_APP_TEST_HOME`, `KOSMOS_APP_CONFIG` precedents, all documented testing-only):
   `KOSMOS_APP_TEST_RELOAD_AFTER=<seconds>` fires `reloadBoard` once via a timer after N
   seconds. This exists so the reload decision logic is drivable end to end without
   Accessibility permissions for synthetic keystrokes. The Cmd-R keyEquivalent itself is
   declarative AppKit wiring, verified once by hand in the headed check.

## Out of scope

- The toolbar refresh icon (Mona Lisa, web/index.html half of #965).
- Cmd-Shift-R / reload-ignoring-cache. Not asked for; add later if cache staleness ever
  shows up as a real symptom.
- Native NSAlert dialogs are app-modal and are not (and should not be) dismissed by
  reload; Josh's "stuck on a modal" reads as page-level modals, which a reload clears.

## Done when

1. `swiftc` compiles the file clean and `--kosmos-app-selftest` still passes.
2. Headed check A (happy path): app launched with `KOSMOS_URL` at a live local test
   server; reload fires; log shows the `webView.reload()` branch and a second
   `PAGE LOADED` line.
3. Headed check B (stuck path): app launched with `KOSMOS_URL` at a dead port; initial
   load fails (log: `PROVISIONAL LOAD FAILED`); server then brought up; reload fires; log
   shows the `loadBoard()` re-run branch and a `PAGE LOADED` line, proving Cmd-R recovers
   a window that started against a dead board.
4. Headed check C (the actual key): with the app frontmost, physical/scripted Cmd-R
   triggers the same path as check A, verified once by hand from the log. If scripted
   keystrokes are blocked by Accessibility permissions, this check is performed by the
   operator or deferred to the release walk, and the plan says so honestly rather than
   claiming it.
5. Full repo suite still green (`yarn test` — the shell/js tests do not touch
   main.swift, so this is a regression guard, not the proof).
6. Challenge-loop convergence + proof file, then PR per house convention.

## Checklist

- [x] View menu + Reload item + `reloadBoard` + `lastLoadFailed` in main.swift
- [x] `KOSMOS_APP_TEST_RELOAD_AFTER` seam with testing-only doc comment
- [x] Compile + selftest (`kosmos-app selftest ok`, port selftest 501 -> 16180)
- [x] Headed checks A and B via the seam; check C via menu-click AX verification
- [x] Full suite (green, exit 0, 65s; validation-log clean for the implementation diff)
- [ ] Challenge-loop + PR (in progress)

## Challenge-loop iteration 1 additions (2026-08-26 08:19-08:24 CT)

The blind review surfaced four WARNINGs, all fixed and re-proven headed:

1. `startBoard` now runs on a background queue with UI marshaled back, so a Cmd-R
   recovery of a genuinely down board no longer beachballs the app for the whole boot.
   A `boardStartInFlight` guard ignores a second press while one boot runs.
2. Benign cancelled navigations (NSURLErrorCancelled -999 in NSURLErrorDomain) no longer
   set `lastLoadFailed`, so a healthy page can't get silently flagged and rob the next
   Cmd-R of its plain-reload branch.
3. The "board died after a good load" case now recovers on ONE press: the reload branch
   arms a one-shot `recoverOnReloadFailure`, and a provisional failure right after a
   user reload falls through to `loadBoard()` once (consumed before the retry, so a
   still-dead board cannot loop).
4. `startBoard`'s child pipes can no longer deadlock `waitUntilExit` (stdout to the null
   device; stderr drained to EOF before the wait). Pre-existing, but Cmd-R made it
   reachable at arbitrary times.

New headed checks, both passed against a fake install whose `bin/kosmos start` is a
chatty shell script (200KB stdout, would have deadlocked the old pipe handling) that
also revives the test server:

- **Check D**: real resolve/start path, async boot, no deadlock, PAGE LOADED.
- **Check E**: good load, board killed, single seam reload: log shows reload branch,
  PROVISIONAL LOAD FAILED, "falling through to loadBoard() once", board restarted,
  PAGE LOADED. One press, dead board to live page.

Checks A and B re-run after the changes: both still pass unchanged.

## Challenge-loop iteration 2 additions (2026-08-26 08:28-08:31 CT)

Two WARNINGs and two NITs, all addressed, all checks re-run green (A, B, D, E):

1. The one-shot fall-through now also fires from `didFail` (a committed-then-failed
   reload is still the user's one press), is disarmed at the top of every `loadBoard()`
   attempt, so an armed flag can never leak into a later unrelated navigation failure
   and trigger a surprise board restart.
2. `boardStartInFlight` gained a 120s watchdog keyed to a start-generation counter: a
   hung `kosmos start` (reachable if a future child inherits stderr past the child's
   exit; cross-file dependency on install/kosmos's nohup fd redirection now named in a
   comment in `startBoard`) can no longer leave Reload permanently dead. A start
   resolving after its watchdog re-armed logs and reports nothing, so it cannot race a
   newer attempt's UI.
3. A Cmd-R dropped because a boot is in flight now beeps (the press registered) and the
   drop-not-queue choice is documented.
4. The test seam rejects non-finite values (`Double("inf")` no longer schedules a timer
   that never fires).

## Execution notes (2026-08-26 08:12-08:14 CT)

- **Check A (happy path) PASSED**: app against a live local server, seam fires at +5s;
  log shows `reload: webView.reload() of http://127.0.0.1:18968/` and a second
  `PAGE LOADED, document.title=kosmos-test-board`; server log shows the second GET.
- **Check B (recovery path) PASSED**: app launched against a dead port
  (`PROVISIONAL LOAD FAILED`), server brought up 3s later, seam reload at +6s took the
  other branch (`reload: no healthy page (url=<nil>, lastLoadFailed=true), re-running
  loadBoard()`) and recovered to `PAGE LOADED` with one GET on the server. This is the
  exact \"window started against a dead board\" scenario from the issue.
- **Check C (the real key) passed at the menu layer, honestly qualified**: synthetic
  keystrokes (System Events `keystroke`/`key code`) do not reach the app in this console
  state, so the literal Cmd-R keypress was not exercised. Instead, Accessibility
  attributes prove the binding (`AXMenuItemCmdChar=R`, `AXMenuItemCmdModifiers=0` =
  plain Command, i.e. Cmd-R) and CLICKING View > Reload through System Events fired the
  reload branch with a real second page load. The remaining gap (AppKit delivering a
  physical Cmd-R to a menu keyEquivalent) is standard OS behavior; worth one keypress in
  the next release walk.
- **Test-harness surprise, not app-related**: python3.14's `http.server` module hangs
  before serving on this machine (raw `socket` bind/listen/accept works fine, sandboxed
  or not). The headed checks use a minimal socket-based test server
  (`scratchpad/testserver.py`) instead. Noted so the next person doesn't burn the same
  half hour.
