# win32-roadmap-7c6-570: record the 7c-6 rehearsal in the Windows roadmap

Issue: #570. This is a docs-only change to `.claude/plans/WINDOWS-ROADMAP.md`, asked
for in the reboot handoff (step 3, "a docs PR recording the 7c-6 results").

## Why

The roadmap is the one document that answers "how far is Kosmos on Windows". It
still says 7c-6 is pending, the board's return at a real logon is "owed", and the
work lives on `win32-chat-arm-570` (merged and deleted). A new session reading it
would redo the rehearsal.

## What changes

- The RESUME block and "one-line state": 7c is done, and the rehearsal passed.
  What is left for v1 is shipping the build.
- §2, capability 4 (talk to it): R3 passed, and #2722 and #2728 are recorded.
- §2, capability 6 (survive a reboot): measured at a real logon on 2026-09-11, with
  the board included.
- §2 gains the 7c-6 R1-R8 results table, next to the 2026-09-08/09 one it
  supersedes. Every line is what the script and the reboot measured.
- §3's slice table: 7c-6 is DONE.
- "Do this next":
  - 7c-6 and the token defect are done;
  - the Windows build: what the live check of a main zip found, the launcher fix
    branch, and the handshake agreed with Baron;
  - what is still unmeasured: the pipe DACL, and a clean-box first run.

No code, so no tests. The numbers come from the handoff file (R1-R7, run
2026-09-10) and this session's R8 measurement (2026-09-11 02:50-02:57 UTC).

## Review log

- Round 1 (opus) found:
  - 6 [INACCURATE] lines this edit left stale, all fixed: the token defect
    marked fixed; win32-chat-arm-570 merged; both "STILL OWED: a real logon"
    notes; the §6 sequencing; §7's "agents cannot do this work"; the stale "does
    installkosmos serve a zip" question; the §2 caveat and the §5 update bullet.
  - It also caught that the one-line state overclaimed "what is left is
    shipping". `kosmos msg` is dark on the Windows zip (§3c, live since 7c-4),
    and a clean-box first run is owed. Both are now in the one-line state and in
    §6.
  - [NIT] The zip-check wording now says how it was unpacked (Explorer's shell,
    Mark of the Web set).
- Round 2 (opus) found:
  - [INACCURATE] The reply gap was filed as a degraded `kosmos msg` problem. It is
    BLOCKER 5 now: an agent's answer never reached the board. It was
    live-confirmed, is fixed by `win32-kosmos-cli-570`, and its verification is
    recorded (§3c).
  - [INACCURATE] R3 and capability 4 overclaimed. Both now say R3 read the
    transcript, not the board; the one-line state says "passed as defined"; and
    "talk" in the zip check is qualified.
  - [INACCURATE] §3 is marked historical (closed by 7c-4). §6's #2537 is moved to
    DONE, and the CLI branch is added.
  - [NIT] The stale 7c-2 row is gone from §7, the wrapped line is fixed, and the
    update timing is 8.8-9.6s over three runs.
- Round 3 (sonnet) found:
  - [INACCURATE] BLOCKER 5's header and its ✅ read as merged, like BLOCKERs 3
    and 4 (which cite commits). Both now say "fixed on branch
    `win32-kosmos-cli-570`, not yet merged when written".
  - [NIT] An over-indented continuation line under "Do this next" is aligned.
- Round 4 (opus) found:
  - [INACCURATE] BLOCKER 5 still described the CLI branch's first `kosmos.cmd`
    shim and its limit. Now it describes the `.ps1` (JSON in the environment)
    and its real limit (execution policy), and says why the `.cmd` was dropped.
  - [INACCURATE] "verified live" predated the `.ps1`. The `.ps1` has since been
    checked live, a two-line answer with quotes and `&` included, and the record
    says which shim each check used.
  - [INACCURATE] The clipath "no win32 in the file" claim now reads "no win32
    branch; win32 appears only in comments".
  - [INACCURATE] §3b's "installedKosmosCli() is null by construction" now notes
    what changes when the CLI branch merges.
- Round 5 (sonnet) found:
  - [INACCURATE] The `.ps1` live check had no record on the CLI branch. That
    branch's plan now records it (candidate `0.6.55+7fdaca1ba1c4`: the full check,
    and a two-line answer with quotes and `&`).
- After round 5, two facts changed, and the roadmap follows them:
  - the hand-off merged as #2752;
  - the CLI's PowerShell shim now passes its arguments in a temp file (CLI
    review round 2), not `KOSMOS_ARGV_JSON`.
