# reload-button-995: the footer offers the control that matches the state (kosmos#995)

## Why

Josh, 2026-08-26 11:26, with a screenshot:

> *"if we could put a 'reload' button so it would be like 'Reload' to update to Beta Version
> 0.5.66. Right now it tells me twice to reload but the only button is Check For Update. If we
> know there is an update available we dont need the 'Check for Update'."*

He is right and the cause is positional: `putBtn('Check for Update')` sat OUTSIDE the branch
that knows the page is stale, so the one state where Kosmos has already determined the answer
offered the one control whose job is to go and ask it.

## What

In `paintUpdateCard`'s reached-and-readable arm, the button branches on `pageIsStale(running)`:
`Reload` / act `reload`, else `Check for Update` / act `check`. The click handler gains a
`reload` arm that calls `location.reload()` with no confirm, because nothing is installed and
nothing is irreversible.

⚠️ **The button is deliberately NOT gated on `UPD_ASKED` while the sentence still is**, and that
asymmetry is the fix rather than an oversight. The `UPD_ASKED` gate exists because a VERDICT
answers a question, and until somebody asks there is none. A CONTROL is not a verdict: offering
the right button claims nothing. **And Josh's case is precisely the ungated one** -- he never
pressed Check; the news reached him from the build line above, which announces staleness
unasked. A button gated on `UPD_ASKED` would leave his screenshot unchanged, which is why the
never-pressed-Check case is the first assertion in the test.

## 🛑 The second half of #995 is NOT in this branch, and my claim on that card was wrong

The card also says the engine-stale notice should stop telling a person to type
`kosmos restart` into a terminal, and I wrote that #985 made this fixable because "the app can
already restart the board by itself".

**That is true of the NATIVE APP and not of the page, which is where that notice lives.**
Checked, both halves:

- there is **no board-restart route** -- `kosmos restart` appears exactly once in the whole repo,
  as the text of that very sentence;
- the page **cannot tell it is running inside the native app** -- no custom user-agent, no
  injected flag, nothing.

So the notice cannot honestly become a button, and it cannot even say "press ⌘R" without
knowing the reader is in the app. Making it fixable is a real change (a signal from the app, or
a restart route), not a copy edit. **Recorded on the card as a correction rather than silently
dropped**, because a card that says a thing is nearly done is worse than one that says it is not
started.

## Verification (done when)

1. The five tests below drive the REAL `paintUpdateCard`, lifted from the shipped page via
   `test-support/page`, so a drift in the function is a red test rather than a restatement that
   stays green.
2. ⭐ **Revert-proven, and asymmetrically on purpose.** Against the unfixed page: **three
   assertions FAIL and two controls still PASS.**
   ⚠️ An earlier version of this line said two and three. A reviewer re-ran it and got the split
   the other way round: the click-handler test is also revert-sensitive (its `indexOf` returns
   -1 without the fix), so it is real coverage rather than a control. The sentence the plan used
   as its done-when was wrong about the artifact it certifies, which is the one place a wrong
   number does real damage. That is the correct split -- the controls
   pin behaviour that must NOT change (a non-stale page keeps `Check for Update`; an offer still
   wins with `Update` and its confirm), so a fix that simply relabelled the button everywhere
   would take away the only control that exists when there is nothing to reload for, and those
   controls are what catch it.
3. Full suite; challenge-loop to convergence; PR per house flow.
4. Josh's own screen is the real proof: the state he photographed should now read `Reload`.

## Collision note

`web/index.html` has three of us in it. Measured rather than agreed: the consolidated restyle's
40 hunks span lines 70-25340 and none fall between 6432 and 10691; the confirm box work starts
at 26400. This change is at ~9490. Disjoint; last to merge rebases.


## Review pass 1, applied

- 🛑 **The new branch did not inherit the toast's ENGINE-STALE stand-down**, so the two surfaces
  could contradict each other: the toast says restart, the footer says Reload, and the reload
  re-delivers the same page so the button never clears. Reachable whenever an update replaced the
  files and the restart did not take. Gated, with a control proving the gate rather than a broken
  fixture.
- 🛑 **The press could relabel the button under a finger already on it.** `updCheckNowClick` ends
  with `btn.focus()`, and the paint it triggers can turn "Check for Update" into "Reload" and
  re-point the action, so the natural repeat of the press just made reloads the document. The
  offer arm has the same shape but a confirm absorbs a stray press; this one has nothing, and the
  page has no `beforeunload`, so anything typed and unsent is gone. Narrow exception rather than
  removing the refocus, because its accessibility reason still holds everywhere else.
- The #691 test's title still named the old control; the assertion beneath it had been re-pointed
  with both rulings quoted, and the title was left behind.
