# Plan: name the room's touch target (kosmos #3811, follow-up to #3809)

Owner: Kano. From #3809's final review round (39), CONVENTION: the 36px thumb target was a raw
number in two touch rules while the phone block names its sizes (--room-av, --room-av-gap).

## Finished looks like
- `--room-tap: 36px` is defined once on #pj-room in the (hover: none) block, with the reason it is
  36 and not 44; the bar's buttons and the reaction pill use it.
- web.room-phone-718.test.js pins the pill rule through the name.
- render-room-msgbox-2806 reads --room-tap from the page: every target is at least --room-tap, and
  --room-tap is at least 36 (the decided floor). Both engines pass.
- Controls: --room-tap lowered to 30px fails the arm; one rule back to a raw 30px fails it too.

## Decisions
- The floor 36 stays written once in the check (as the decided minimum), not read from the page:
  reading it would let a lowered value pass its own check.
- Only the two rules the finding named change; no other sizes are touched.
