# seed-doc-1591: the runner's seed text says a Kosmos-owned mailbox

Card: kosmos#1591 (revised 2026-09-26 06:45 after Splinter's review). The merged runner header (tools/plus-signin-fresh.js) and the #2036 plan still described the seed as a plus address on the mailbox the Gmail connector reads, which is another company's mailbox that the connector can read in full. Comment and plan text only: the code already takes the seed from --seed / KOSMOS_SEED_EMAIL with no default (#3940).
