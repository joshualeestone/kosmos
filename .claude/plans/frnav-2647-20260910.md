# kosmos#2647: move Check-again into the bottom nav, and fix the copy

Josh, product review 2026-09-10, from mortals. He **could not see the helper copy
at all before finishing the connection**, which is the whole reason the control
moves: the bottom nav is always visible, the mid-pane block was not.

## The four changes, as specified

1. Top copy becomes exactly: "To get the most out of your agents we need to
   ensure they can stay awake and access the computer."
2. Delete "This can take a few seconds after you flip the switch."
3. Move **[Check again]** into the bottom navigation, same area as **Next**, on
   the **far left**.
4. Keep the copy beside it, shortened to exactly: "Turned it on? Tap to check."

## 🛑 The obvious implementation is a DEAD BUTTON, and it looks correct

`.fr-acts` already has a far-left secondary slot, `#fr-alt`, driven by
`frActions(primary, alt)`. So the tempting move is to hand `#fr-alt` the
`fr-recheck` class and let the existing handler pick it up.

**It would never fire.** That handler is delegated on `#fr-pane-3`
(`document.getElementById('fr-pane-3').addEventListener('click', ...)`), and
`#fr-alt` lives in `.fr-acts`, **outside every pane**. The click never reaches
the delegate.

⭐ Nothing about that is visible from the button: it renders, it is focusable, it
styles correctly, and pressing it silently does nothing. **A moved control that
looks right and does nothing is worse than one that never moved**, because the
person concludes the check itself is broken.

⇒ The move goes through `frActions`'s `go` handler instead, which is the
mechanism that slot already has for exactly this.

## The design

- Extract the recheck body into one named function so the behaviour is not
  retyped at the new call site. **It has real logic worth not duplicating:** it
  refuses to clobber a `fr-msg-err` on the shared `#fr-s3-msg` line, both before
  and *after* the await, because a "Turn On" failure can land during it.
- Step 3 passes `alt: { label: 'Check again', go, hint }`.
- `frActions` learns `alt.hint`, rendered into a span in `.fr-acts`.
  **The hint must be CLEARED on every screen that has no hint**, or it becomes a
  sentence from screen 3 sitting under screen 5's buttons. That clearing is the
  part most likely to be forgotten, so it is asserted.
- Remove the now-dead `.fr-recheck` branch from the pane-3 delegate and the
  in-pane markup. `.fr-recheck` has exactly two occurrences (the button and the
  handler), verified, so removing both leaves nothing orphaned.

## Rejected

- **Duplicating the recheck logic at the new call site.** Two copies of the
  error-preserving rule, which is subtle and would drift.
- **A step-3-only hard-coded span in `.fr-acts`.** The nav is shared by nine
  screens; a hard-coded child is a thing every future screen has to remember to
  hide. Routing it through `frActions` means it clears itself.
- **Leaving the old in-pane button as well.** Two Check-agains is not what was
  asked, and the second would be the invisible one he complained about.

## Test plan

- A browser check on the real first-run screen: the Check-again control renders
  **in the bottom nav**, on the far left, with the hint beside it; pressing it
  actually fires a re-check (the arm that would have caught the dead-button
  design); the old in-pane control and note are **gone**.
- A negative arm: the hint does **not** appear on another screen.
- Copy asserted verbatim against Josh's exact strings, since three of the four
  items are exact wording.
- Mutation controls on every new arm.

## Weakest premise

That the bottom nav is the right home for a control that is only meaningful on
one screen. It is what Josh asked for and his reason is sound (always visible),
but it puts a screen-specific action in a shared bar, and the hint-clearing is
the maintenance cost. **What would change my mind: him saying it looks out of
place beside Next**, which is a layout call only he can make.


## Challenge-loop iteration 1: 1 BLOCKER, 3 WARNING, 1 CONVENTION, 2 NIT

### 🛑 BLOCKER: I moved a control and left an existing, wired check aimed at its old home

`render-gated-next.js:346` targeted `#fr-pane-3 .fr-recheck`, which now matches
nothing (verified: zero markup hits, controlled against the two COMMENT hits the
same grep returns). It is wired at `browser-checks.sh:1114`.

⭐ **Worse than a plain red.** `ok(!!btn)` fails, the label arm then compares
against `''`, and `if (btn) await btn.click()` **silently no-ops**, which turns
the assertion below it from a real signal into a coin flip on whether a 750ms
poll tick lands in its 80ms window. **A moved control degraded a sibling check
into flakiness rather than into an honest failure.**

⭐ **Writing a new check for the new home is not the same as finding the checks
that already watched the old one.** I did the first and not the second.
Repointed rather than deleted: that block is a live-board integration signal the
new hermetic check cannot provide.

### The rest, each real

- **The check passed green on a ONE-SHOT button.** It clicked once and never
  looked at `disabled` again, though "flip the switch, press again" is the
  control's entire purpose.
- **The move COST the control its adjacency.** The hint used to sit inside pane 3
  directly under the gate rows it refers to; it now lives in a footer shared by
  nine screens, where a rotor or tab lands on a button whose whole accessible
  name is "Check again". `aria-describedby` now ties them, toggled both ways.
- **`hintBesideAlt` was VACUOUS**, admitting any position in the ~350px between
  the two buttons. Measured: the span moved after `.fr-spacer` drifts 99px off
  its own button and every arm still passed.
- **🛑 My CSS comment was wrong about its own rule**, and this is the FIFTH
  instance across today's work of a confident claim about neighbouring code never
  re-opened. `margin-right: auto` is not load-bearing: geometry is byte-identical
  without it at five widths. The mechanism is the span's DOM position before
  `.fr-spacer`. ⭐ **A wrong comment and a weak assertion pointing the same way is
  how a defect walks through both**: the change the comment licensed (reorder the
  span, trust the margin) was exactly the one the check passed on.
- **The wrap note was backwards.** At 460px the hint stays on its button's row
  and NEXT wraps. Also recorded: this hint makes step 3's nav wrap below roughly
  490px, where origin/main did not wrap down to 400px. Cosmetic, but real and
  previously unstated.
- **`frRecheckPress` re-enabled a nine-screen shared button with no generation
  guard**, while its sibling `frRecheck` guards the SAME element with exactly
  that pattern.

### Two of my own mutations were invalid before they were valid

⚠️ **Both would have been mistaken for proof.** One anchor appeared three times,
so the mutation never applied and the "mutant" run was just the baseline. Another
left a dangling `else`, so the page threw `ReferenceError` and the check failed
for a syntax error rather than the arm firing. **A mutation that did not apply,
and a broken page, are both not-evidence that look like evidence.**

⚠️ **And my first control for the generation guard did not discriminate.** I
nearly shipped a guard I could not demonstrate: overlapping two presses where the
second had already finished shows nothing, because a stale re-enable to a value
already `false` changes nothing. The scenario that bites is a stale press
finishing while a NEWER one still holds the button.

### Controls at this point, all measured against an isolated copy

| mutation | result |
|---|---|
| baseline | exit 0 |
| `alt.go` replaced with a no-op | exit 1, ONLY the press arm, every visual arm still passing |
| drop the re-enable in `frRecheckPress` | exit 1, the one-shot arm |
| never SET the aria tie | exit 1, the describedby arm |
| never CLEAR the aria tie | exit 1, the stale-tie arm |
| move the hint span after `.fr-spacer` | exit 1, the adjacency arm, gap named |
| drop the hint-clear calls | exit 1, the leak arm |
| restore the old top copy | exit 1, the copy arm |
| drop the generation guard | stale press hands the button back (`disabledAfterStaleA` false vs true) |
