# Plan: move Sound from Settings > Automations to This computer (#3138)

Josh 6.68: move the master Sound control (new-message sound on/off) out of Settings > Automations
and place it under Settings > This computer, LAST in that section. No behavior change.

## Done-condition
- The Sound "Sounds" block (h3 + snd-row/snd-toggle) renders LAST in the This computer (s-sec-mac)
  section and is GONE from Automation (s-sec-automation).
- The toggle still works (ids preserved -> JS wiring intact).
- Automation section now reads [Auto-save, Prompter, Agent Communication, Daily report].

## Approach (chosen)
Pure markup move: cut the `<section class="dbox">` (comment + h3 + snd-row/snd-toggle) from
s-sec-automation, paste it before s-sec-mac's closing tag. ids unchanged (snd-row/snd-toggle) so
getElementById-based JS (paintSwitch/paintSettings, the click handler, soundMasterOn) is unaffected.
Comment updated to reflect the new location + rationale (per-device preference belongs with the
This-computer controls).

## Rejected
- Duplicating the control or adding a link: the card says MOVE, one instance.

## Weakest premise
That no JS scopes the toggle by DOM position/ancestor rather than by id. Mitigated: verified the
render-sound-master browser-check finds it by getElementById (section-agnostic) and passes in the
new layout; grep found no querySelector scoping the toggle to the Automation section.

## Verification
- Node: web.settings-nav.test.js (Automation order minus Sounds; Sounds LAST in mac) +
  web.bubblepop-2407.test.js: 20/20, non-vacuous (fails on origin/main).
- Browser-checks (headless, pinned Playwright): render-sound-master-2436 + render-prompter-label-1843
  both PASS in the new layout.
- Challenge-loop to convergence.
