---
pre_challenge: true
method: challenge-loop
branch: project-needsyou-2699
diff_hash: 1a4a5eb51996aecf7ca267b8d81612eac0b7b3d5bf2dced453d2fbcb03198b17
validation: passed
subdir_audit: passed
timestamp: 2026-09-11T04:32:16Z
iterations: 4
converged: true
---

## [CHALLENGE-LOOP] Summary

**Iterations:** 3
**Converged:** Yes (two-model witness on the final code: iteration 2 on Opus and iteration 3 on Sonnet both returned zero findings)
**Total findings:** 1 BLOCKER, 0 WARNINGs, 0 CONVENTIONs (all fixed)
**Fixed:** 1 BLOCKER (+ 2 self-inflicted test-infra breaks found by 6g and fixed) | **Deferred:** 0 | **Asked:** 0

kosmos#2699 (a Josh design-capture card): on the project view a needs-you member blended into the Idle/Working spot. The fix makes `pjMember` draw the red warning triangle (LROW_WARN) over the `.pj-face` avatar and red the status text, for needs_you ONLY, when present.

**subdir_audit is VACUOUS here:** the diff changes zero CLAUDE.md files, so the audit's subject was absent. Recorded `passed` because nothing failed, not because a guard engaged.

**validation note:** the full suite (`tools/run-tests.sh`) passes 5935/5936 on the final HEAD. The sole red across runs is `codex-report-bridge.test.js #1139` (a report-token/header TIMING test), which is UNRELATED to this project-view change (the diff touches pjMember, .pj-member CSS, the browser check, server.test.js's pjMember prelude, README, browser-checks.sh, reason-grep, and the plan - nothing in the report-token bridge). It is a contention flake: the g-mac-mini fleet is heavily loaded tonight and the test's timing runs stretched to 6-22s (vs ~5s normal), failing even in isolation. Every test relevant to this change passes (browser check, server.test.js 263/263, web.theme.test.js 13/13, reason-grep 5/5, pj/layout tests). `validation: passed` reflects that; the flake is documented rather than chased.

### Per-Iteration Breakdown

#### Iteration 1
**Reviewer model:** sonnet
**New findings:** 1 BLOCKER
**Self-generated:** 1
- [BLOCKER] web/index.html - the needs-you condition keyed on `stateCopyOf(m).attn`, but STATE_COPY.attn is true for SIX states (needs_you, rate_limited, auth_failed, blocked, stopped, unknown), so the red triangle + red text lit up on every paused/stopped/sign-in-broken/waiting/unreadable member, defeating the card (which is about making needs_you stand out FROM the rest). I had also asserted "attn is true only for needs_you" in the comment and plan without checking STATE_COPY --> FIXED: use `cardStOf(m).st === 'attn'` (CARD_ST maps only needs_you to st:'attn', the same key the org-node badge #2576/#2577 uses); gated on m.present; corrected the comment and plan. Strengthened the browser check's negative arm from idle-only to also a STOPPED member (STATE_COPY.attn true but not needs_you), the load-bearing discriminator, proven can-fail. (commit 9f94ec54)

#### Iteration 2
**Reviewer model:** opus (different model from iteration 1, per 6a)
**New findings:** 0
**Self-generated:** 0
Traced CARD_ST/STATE_COPY, the m.present gate (present is Boolean(card); an untied stranger is forced to state:'unknown'), CSS scoping/specificity, the generated dark twin, a11y, and mutation-tested the stopped negative arm. Verdict: ship it.

#### Iteration 3
**Reviewer model:** sonnet
**New findings:** 0
**Self-generated:** 0
Independently re-verified the condition (mutated it to also match 'stopped' and confirmed the check reddens), the scoping (mutated the show/position rule and confirmed red), the generated dark twin (web.theme.test.js), the server.test.js prelude (real CARD_ST/cardStOf via the tested extraction helpers, not a stub; 263/263), the browser check (ran it, mutated both the condition and the CSS to red it), the wiring counts, and em-dash. Checked every other pjMember lift site for a stale prelude. Verdict: correct, well-scoped, ship it.
**Converged** - two models returned zero on the final code.

#### Two self-inflicted test-infra breaks 6g caught (not challenge-loop findings, recorded for honesty)
- [BLOCKER] web.theme.test.js "forced theme is in step" - I hand-wrote the `:root[data-theme="dark"]` twin, but that forced section is GENERATED by tools/sync-forced-theme.js --> FIXED by removing the hand-written twin and running the generator (commit 9afbf834).
- [BLOCKER] server.test.js "pjMember suppressTold" threw `cardStOf is not defined` - the test lifts the real pjMember with an explicit dep prelude, and my new cardStOf call needed CARD_ST + cardStOf added to it --> FIXED (commit df798c82).

### Final Ledger

| # | Iter | Category | File:Line | Origin | Description | Status | Resolution |
|---|------|----------|-----------|--------|-------------|--------|------------|
| 1 | 1 | BLOCKER | web/index.html:33186 | BRANCH | needs-you condition lit up 6 states, not just needs_you | FIXED | 9f94ec54 |
| 2 | 6g | (infra) | web/index.html | SELF | hand-wrote the generated forced-theme dark twin | FIXED | 9afbf834 |
| 3 | 6g | (infra) | server.test.js | SELF | pjMember lift prelude missing cardStOf/CARD_ST | FIXED | df798c82 |

### NITs (non-blocking)
- [NIT] docs/browser-checks/render-project-needsyou-2699.js - the browser check exercises the 34px default-layout avatar, not the 28px consolidated path; the triangle CSS is layout-independent (overflow:visible + reused consolidated placement), so coverage is adequate. Noted by iteration 2 as informational.

### Strengths (across iterations)
- The needs-you condition converged onto the SAME key the existing org-node badge uses (cardStOf.st === 'attn'), so the two needs-you surfaces cannot drift.
- The negative arm is the correct discriminator (a stopped member, whose STATE_COPY.attn is true but is not needs_you); an idle-only arm could not have caught the original BLOCKER.
- Model alternation held (Sonnet, Opus, Sonnet); the BLOCKER came from the first Sonnet pass.

#### Merge with origin/main (after iteration 3) + iteration 4
Main advanced (my own #2697 render-profile-field-widths merged as cb77290f, touching the SAME guard
files). Merged origin/main in (commit 65dd7153). web/index.html AUTO-MERGED cleanly - #2697's change is
the detail-form field CSS + found-desc, mine is pjMember + .pj-member CSS, different regions, no semantic
overlap. Three guard-file conflicts, all mechanical: reason-grep counts stacked to 99 SITES / 69 CATCH
(base 95/65 + #2697 +2/+2 + #2699 +2/+2, verified by the counting test), and the README rows +
browser-checks.sh loop entries were unioned. After the merge: reason-grep 5/5, both browser checks PASS,
web.theme.test.js 13/13, server.test.js 263/263. diff_hash regenerated for the merged HEAD (my net change
vs the new main). Iteration 4 is a blind re-witness of the merged state.

## Release-cut note
Baron's 0.6.55 cut is DONE and main is unfrozen, so this can merge when green. Re-check for a NEW active cut before self-merge (render-surface change), per the merges-pause-during-a-cut rule.
