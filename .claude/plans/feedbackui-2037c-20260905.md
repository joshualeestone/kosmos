# Plan: #2037 PR-C1 - feedback report opt-in goes live (Settings switch + route + board trigger), default OFF

Branch: `feedbackui-2037c`. Card: kosmos#2037 (agent-authored daily product-feedback report, default-on, sent not collected). Prior slices merged: 2a `kosmos feedback` CLI (#2158), 2b PM daily instruction (#2161), transmit engine `engine/feedbacksend.js` (#2277, off by default, scrub + #2246 contract, maybeSend unwired).

## The slice decision (documented, my call under Josh's "decide + keep going")

The card's remaining "PR-C" is: Settings + setup opt-in UI + default-ON flip + the board-side send trigger. That is four surfaces plus a scheduler plus a **default-ON content phone-home**. The split:

- **PR-C1 (this branch):** the opt-in send goes live END TO END - `/api/feedback-setting` route + Settings > Automation switch + a board daily-send trigger + once-per-day dedup + **the default-ON flip**. All THREE of the launch requirement (trigger, control, default-ON).
- **PR-C2 (fast-follow):** the install-time disclosure checkbox (first-run "Create your first agent" step, Mona Lisa's approved Placement A copy).

**UPDATE 2026-09-05 (during challenge-loop): default-ON folded INTO PR-C1.** I first staged the default-ON flip to PR-C2 (to land with the install-time disclosure). Splinter, relaying Kitty's end-to-end verification and Josh's "baked in day one", made PR-C the launch critical path and required all THREE in it (trigger + control + default-ON). That falsified this plan's original weakest premise ("Josh is fine with default-ON one PR later"), so the flip moved into PR-C1. The Settings switch is the disclosure surface for launch; the install-time checkbox is the immediate fast-follow (PR-C2). Flagged to Splinter so he/Josh can hold the flip for C2 if the install disclosure must precede it; proceeding all-three unless told to hold.

**Why this split, and the weakest premise.** Josh ruled default-ON with a "default-checked checkbox in the install process" as the disclosure surface. Shipping default-ON content phone-home BEFORE that install-time surface exists means a person's agent starts sending daily report bodies with the disclosure only in Settings (which they may never open). So the default-ON flip lands WITH the install disclosure (C2), never before it (#2013: a default and its disclosing control land together). C1 ships the mechanism + a Settings opt-IN so nothing sends by default and an informed user can turn it on now. This mirrors exactly how the sibling ping feature staged (Settings control first under #2020 step 1, point-of-act checkbox + default later). **Weakest premise:** that Josh is fine with default-ON arriving one PR later (with the install checkbox) rather than now. If he wants default-ON immediately, C2's flip can move up - but it should still ship with the install disclosure, not naked. Splinter/Josh can override; documented on the card.

## What "finished" looks like (C1)

A person can turn the daily feedback report ON in Settings > Automation (it is OFF by default). When on, the running board sends that day's local report once per day (scrubbed, #2246 contract, via the merged `feedbacksend.maybeSend`). The switch carries on/off/could-not-read with the PRIVACY could-not-read treatment (#2047 + Mona Lisa's 2026-09-03 privacy ruling: row present, knob hidden, actionable line, never a false Off). Nothing sends by default; nothing sends under test (the underTest guard in feedbacksend).

## Changes

1. **`engine/feedbacksend.js`** - once-per-day dedup so the board sweep cannot re-POST the same day's report every tick.
   - Persist a `sent` date in `feedbacksend.json` (alongside `on`). `write`/`read` already round-trip the object; extend `read` to surface `sent` and add a `markSent(date)` + `sentOn(date)` (or fold into a new `sendDailyOnce(date)`).
   - New `sendDailyOnce(date)`: if `!read().on` return; if `read().sent === date` return; else **mark sent first** (so a failed POST does not re-send all day - best-effort daily, next day retries), then call the existing `maybeSend(date)` send path. Keep `maybeSend` unchanged for the direct/test callers.
   - Default stays OFF (ENOENT -> `{on:false}`). Do NOT flip here; that is C2.

2. **`server.js`**
   - `require('./engine/feedbacksend')` near line 316 (beside ping/notify/feedback).
   - `/api/feedback-setting` GET/HEAD/PUT, mirroring `/api/ping-setting` (3487-3505): GET returns `{on, ok}` from `feedbacksend.read()`; PUT reads `{on}`, calls `feedbacksend.setOn`, returns `{on, ok}`.
   - A board daily-send sweep in the start block (mirror the #185/#1724 sweeps ~9043-9083): `setInterval`, `unref()`, best-effort try/catch, calling `feedbacksend.sendDailyOnce(feedback.today())`. Cadence hourly (`AGENT_WORKFORCE_FEEDBACK_SWEEP_MS` env test seam, default 60*60*1000); dedup makes the exact cadence non-critical. It fires nothing when off (read().on false) and nothing under test (no sender + underTest).

3. **`web/index.html`** - Settings > Automation "Daily report" block, inserted before the automation section close (~10142, the placeholder note at 10111-10113 marks the slot). Mirror the tell-row/tell-toggle shape and the tellPaint/refreshTell/tellToggleClick JS.
   - Markup: `feedback-row` / `feedback-toggle` (role=switch, hidden until painted) / `feedback-msg`. Heading + copy VERBATIM from Mona Lisa's approved design (2026-09-03 20:58, Josh's decision): label "Make Kosmos work better for everyone.", subtext "Allow an agent to send a daily note with any system issues or improvement suggestions."
   - JS: `feedbackPaint(r)` (paintSwitch('feedback-toggle', unread?null:r.on===true) + the privacy could-not-read message "We could not check this setting here. Open Kosmos from its icon and you can change it."), `refreshFeedback()` (epoch-guarded, 403-safe: non-ok GET -> null paint), `feedbackToggleClick()` (PUT /api/feedback-setting). Call `refreshFeedback()` where the other Automation switches refresh.

4. **Tests**
   - `engine/feedbacksend.test.js`: sendDailyOnce sends once for a day and skips the second call same day (with an injected sender); off -> sends nothing; a new day sends again. Positive controls.
   - `server.test.js` (or a focused test): GET/PUT /api/feedback-setting round-trips; the sweep, driven once, calls sendDailyOnce (inject a sender, assert one POST, assert a second sweep tick same day does not re-POST).
   - `web.*.test.js`: the feedback-toggle row is present + wired (refreshFeedback in the refresh path, the PUT in the click), the could-not-read treatment hides the knob + shows the message, and the copy matches the approved strings.

## Traps

- web/ change trips the browser-check gate chain (test:shell + reason-grep). Re-run the FULL suite.
- Default MUST stay OFF in C1 (the default-ON flip is C2, with the install disclosure). A test should pin `feedbacksend.read().on === false` on a fresh machine so C2's flip is a deliberate, reviewed change.
- No em dashes (5 spellings).
- `gh pr create` needs a LITERAL `cd <abspath>`; `--body-file` not `--body "..."`.
- Kosmos beta: no reviewer, merge on green; respect any active freeze.
- The sweep must be unref'd and best-effort, and must never POST under test (underTest guard) or when off.
- markSent-before-POST is deliberate (no all-day retry spam on a failed send); document it.

## Verify + ship

Unit tests green; full suite (web change) green; challenge-loop to convergence; screenshot the Settings switch; PR (no reviewer), merge on green. Leave #2037 open for PR-C2 (install checkbox + default-ON).
