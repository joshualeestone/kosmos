# Plan: #2020 restore the created-agent ping consent (create-page checkbox + default-ON)

Branch: `createping-2020`. Card: kosmos#2020 (create-ping consent) + kosmos#2013 (default-ON ruling). Authorized by Josh's ruling TODAY (2026-09-05), relayed by Splinter: *"we need that back in for sure"* + *"I've never said flip it off"* - which REVERSES the 2026-08-26 removal.

## Goal / what "finished" looks like

The created-agent ping consent control is back on the Create-an-Agent page (a default-ON checkbox, Josh's exact copy "Let the Kosmos team know you created an agent."), the ping default is ON, and the guard that currently pins the control ABSENT is inverted to pin it PRESENT + default-ON. The homepage agents-created counter (which has been undercounting while ping was off) starts counting again.

## Why this is now buildable (it was NOT, until today)

- `engine/ping.test.js` has a guard `'the created-ping CREATE-PAGE control stays absent, and it does not default ON'` that pins `create-tell*` and the copy ABSENT and `ping.read().on === false`. That guard encoded the 08-26 removal (Josh: "they both need to be removed").
- Josh reversed that TODAY. So the guard now pins a SUPERSEDED decision (the `a-guard-keyed-on-a-superseded-artifact` shape). Inverting it is correct, not a weakening - the reversal is the new authority.
- The Settings opt-out control (`tell-row`/`tell-toggle`) ALREADY exists (restored earlier under #2020 step 1). This card adds the CREATE-PAGE control + flips the default.

## Scope decisions

1. **Default-ON is Josh's explicit, low-privacy ruling.** The ping payload is event-only: `event, installId, at, version, os` - nothing about the agent, its name, role, instructions, or anything typed (enumerated + tested by name in ping.test.js). Josh wants this for the homepage counter. So default-ON here does NOT carry the #2037 disclosure/scrub concern (that was about the feedback REPORT body; this is an event ping). This is why #2020's step-3 hold is lifted by his ruling.
2. **The create-page checkbox + the default flip land together (#2013: a default and its control are ONE decision).** The inverted guard asserts both, so they cannot drift apart.
3. **Exact copy is Josh's:** "Let the Kosmos team know you created an agent." (bottom of the Create-an-Agent page, Create button to its right, default checked). #2020 is needs-release on the FINAL wording, so ship this exact string and flag that Josh owns the final wording.

## Changes

- **`web/index.html`**: restore the create-page consent control at the bottom of the Create-an-Agent form (`id="create-tell"` checkbox, default checked, label = the exact copy; `id="create-tell-note"` / `id="create-tell-wrap"` per the guard's expected ids). Wire the create flow to pass the checkbox state as the `wanted` flag to the ping (`ping.agentCreated({ wanted })`). Confirm the Settings `tell-row` copy matches.
- **`engine/ping.js`**: flip the default to ON. `read()` currently returns `{on:false}` on ENOENT (the 08-26 change); restore ENOENT -> `{on:true}` (never-asked = on, per Josh). Keep the fails-to-off-on-UNREADABLE behavior (a corrupt file still reads off, the safe direction for a phone-home we can't read).
- **`engine/ping.test.js`**: invert the guard. Assert the create-page control is PRESENT (`create-tell*` + the copy present, `create-go` still present) and `ping.read().on === true` (ENOENT default). Update the other ping.test.js assertions that pin default-off (e.g. line ~38 `ping.read().on === false`, line ~209-210) to the new default-on reality, changed TOGETHER with the control per #2013. Document the provenance in the test comment: Josh's ruling 2026-09-05 reverses the 08-26 removal.

## Traps (from PR-A + the repo)

- web/ change trips the browser-check gate chain (test:shell #1720/#1387/#1864 + reason-grep counts) and needs the box + a browser-quiet window. Do NOT build during an active release cut.
- Write this plan file BEFORE the challenge-loop (the pre-challenge-gate requires a plan file; PR-A hit this).
- CSS: use defined tokens; a `var(--x)` with no fallback undefined at :root is dropped (server.test.js catches it).
- No em dashes anywhere (5 spellings).
- `gh pr create` needs a LITERAL `cd <abspath>`; `--body-file`, never `--body "..."`.
- Kosmos beta: no reviewer, merge on green (but respect any active site-merge / kosmos-main freeze - hold the merge on CLEAR).

## Weakest premise

That the create-page checkbox ids the guard expects (`create-tell`, `create-tell-note`, `create-tell-wrap`) are the right ids to reuse. They come from the guard's own absence-assertion, so re-using them makes the inverted guard symmetric. If the historical control used different ids, prefer the guard's ids (the guard is the contract).
