# mask-words-3935: a held key with WORDS between its pieces is masked

Addresses #3935 (the gap #3938 / mask-separators-3769 recorded and left open). Built on
mask-separators-3769 at 4e2d987b, rebased onto main after #3938 merged (426c7711, identical content).

## Problem
The guide's output mask joins a held key's pieces only when the noise between them is characters
no key uses (| , ; . spaces) or runs of three or fewer key characters. Any run of four or more
between pieces defeated it and the whole key showed: a word row label (`| Charlie | Ab3dEf7h |`),
another filled column, a bullet's description, words around bold or backticked chunks.

## Change (engine/secretmask.js, wordSkippingSpans)
- Cut the reply into runs of key characters, with positions.
- A held form is STARTED by a run that ends with the form's opening, four characters or more.
  Ending, not only starting, so `KEY=Ab3d` starts it too.
- Walk the runs after it in order: a run that is exactly the form's next part advances it, and any
  other run is skipped as noise. EVERY reached position is kept, not only the latest, so a short
  noise run equal to the next part ("Part 1" before a piece starting with 1) cannot derail it.
- The bound is the one #3769 uses: at most 4x the form's length in non-whitespace characters from
  the first piece.
- A hit is masked piece by piece (each matched run), reported as split_secret (round 18 changed this from one span first-to-last).
- Forms are walked with their non-key characters removed (round 3); forms made only of words and
  numbers, over 1,024 characters, or opening with no letter or digit are not walked (rounds 1, 3, 5).

## Cost
- Candidates come from a 4-character opening index and are grouped per opening by the character
  the next piece must start with, so a form is walked only if some run in reach begins with that
  character.
- A step budget (1,250,000 checks since review round 20) bounds the walk. On a reply built to keep 2,000 held forms alive,
  the unbounded walk cost 9.6s of CPU; bounded at 250,000, it cost about 18ms. At 1,250,000 an exhausted
  search costs about 1 to 1.3s (review round 23), paid once per stored reply thanks to mask()'s cache.
- A reply that exhausts the budget is WITHHELD whole (split_search_limit): a search cut short may
  have missed a key, and this file errs toward masking. The ordinary cost tests assert they do not
  reach it.
- Found, NOT this change's: on that same adversarial reply the #3938 separator copies' knownFormsIn
  costs about 600ms, the same with or without this change (805ms measured on the base branch).
  Reported on #3938.

## Tests (engine/secretmask.test.js)
- Words between pieces: word row labels, another filled column, bullets with a description,
  prose around bold and backticks, and a first piece glued after `KEY=`. No piece readable, the
  text around it kept, reported as split_secret.
- Held values only: an ordinary word-labelled table, a bullet list, and an answer naming a held
  key's known opening (sk-ant-api03) are untouched. CONTROL: the held key in a word table is
  masked.
- Derail: "Part 1" before a piece that starts with 1.
- Budget: the adversarial reply is withheld whole, in under 1500ms.
- Mutations, each on a scratch copy:

  | mutation | result |
  |---|---|
  | keep only the latest position | 3 tests red |
  | no budget | the budget test red (4.8s) |
  | opening only at a run's start | the words test red (glued case) |
  | walk removed | 4 tests red |

- server.guide-secrets-3769.test.js and engine/secretmask.test.js: 43/43 at the first commit (see
  each round below for the tests added since).

## Review round 1
- [WARNING] the budget did not cover the look-ahead scan, and its reach grows with the form's
  length: the board holds whole files (knownsecrets.js, up to 64KB), so one 40,000-character held
  value made a reply repeating its opening cost 1.3s --> FIXED: forms over 1,024 characters are not
  walked (still masked whole by known_secret), and every run the look-ahead visits is charged. Two
  tests: the long held value (reds without the cap), and a held value at the cap whose opening
  repeats 10,000 times, withheld at the budget (reds without the per-run charge).
- [WARNING] key characters glued to a later piece hid it: a label with = (part2=Ab3d), italics
  (_Ab3d_), a trailing slash --> FIXED: each run is also tried with leading/trailing _ , a trailing
  /, and anything up to an inner = removed; the opening is looked for with trailing _ and /
  removed. Test with the reviewer's four cases; reds before the fix.
- [NIT] the budget test's margin --> the adversarial input is now 1,000 rows (it already exhausts
  the budget).
- [NIT] the non-space count was built for every reply --> built on the first opening found.
- [NIT] one opening can complete two forms, giving overlapping spans --> DEFERRED: the rebuild
  merges them; only the split_secret count can read one higher.

## Review round 2
- [BLOCKER] two held keys that share an opening (every Anthropic key opens sk-ant-api03-), both
  split in one reply: the second key's walk started at the FIRST key's opening, skipped the first
  key's pieces as noise, and completed on its own pieces, so one span masked everything between
  them --> FIXED: a walk stops at a run that opens the same form again at least as fully as its own
  opening; that later start walks it. Test (two keys, word rows, a header between) reds without it.
  The "at least as fully" half has its own test: a short prose mention (sk-ant) between the pieces
  does not cut a real walk short (reds if any 4-character reopening stops it).
- [WARNING] overlapping spans printed two masks side by side --> FIXED in the merge: a span that
  overlaps the previous one widens it. No known input reaches a partial overlap now (forms are
  walked longest first, so a shared start's longer span always contains the shorter), so this is
  defensive and has no test that can fail on the old merge; a test that could not fail was dropped.
- [WARNING] no test with two held keys sharing an opening --> the BLOCKER's test.
- [NIT] the = rule takes the last = --> the comment says so.
- [NIT] the trailing _ / strip is written twice --> DEFERRED: two one-line regexes, different
  inputs (the opening run vs every piece).

## Review round 3
- [BLOCKER] (SELF, round 1's code, a code line) the opening was looked for only with a trailing _ or
  / stripped, which also strips the key's OWN last character when a chunk ends in one (_ is in
  base64url keys), so the whole key leaked --> FIXED: the opening is looked for both as written and
  stripped. Test with a first chunk ending in _ and in /; reds before.
- [WARNING] held values with a character outside the key set (a password's ! or #) were left out of
  the walk, and the comment said only the spaced hex was --> FIXED: the walk uses the value with
  those characters removed (they sit between runs in the text, not inside them), still 12 or more.
  Test with a random password with symbols, pieces of 5; reds before.
- [WARNING] groupsFor was not charged: 2,000 held sk-ant-api03- keys and 10,000 distinct words
  opening sk-a cost 1.7s --> FIXED: each distinct opening is charged the forms it scans. Test asserts
  the reply is withheld at the budget; reds without the charge (mutant: 600ms and not withheld).
- [WARNING] a held value made of words (Administrator1, Settings2024) was assembled out of ordinary
  sentences and masked them --> FIXED: such values are not walked (madeOfWords: every piece at the
  case changes, digits and key punctuation is a word or a number). They are still masked whole.
  Test with both of the reviewer's sentences plus a whole-value control; reds before. The cost
  tests' held values were word-shaped (held-value-...-xyz), so the budget test now uses
  random-looking ones, as keys are.
- [NIT] hyphen and other glue --> named in the code's Not covered comment.
- [NIT] the tests that assert WITHHELD now point at WORD_WALK_BUDGET.

## Review round 4
- [CONVENTION] the 4-character opening was a raw literal at three sites --> FIXED: OPENING_LEN, with
  why it is 4 and what that costs.
- [CONVENTION] the first commit's subject is `kosmos#3935: ...`, neither `#N:` nor `<branch> -- `
  --> DEFERRED: the repo squash-merges (#3938 landed as one commit), so the PR title is the subject
  that reaches main, and it uses the branch form. Rewriting pushed history buys nothing.
- [NIT] the reopening check is not charged --> commented: bounded by the reach, not by the budget.

## Review round 5
- [WARNING] (SELF, round 2's stop, a code line) a sentence naming the key's longer prefix
  (sk-ant-api03) between two pieces stopped the only walk that could complete, so the key showed
  --> FIXED by replacing the stop: every walk runs, and when the same form completes at the same
  place from two starts the LATER start is kept. The two-keys case (round 2) is still handled that
  way: the second key's own start ends where its walk from the first key's opening ends, and wins.
  Test with the reviewer's sentence; reds before. Mutant keeping the earliest start reds the
  two-keys test.
- [WARNING] (same stop) a key whose later chunk repeats its opening stopped its own walk --> FIXED
  by the same change. Test (Qz7kQz7k...); reds before.
- [WARNING] the budget comment overstated how hard it is to reach, and a held PEM's walked form opens
  with ---- so every markdown rule started a walk --> the comment now states the measured trigger;
  forms whose opening has no letter or digit are not walked (still masked whole, and across lines).
  Test: a held PEM and 3,000 markdown rules, unchanged; reds without the skip.
- [NIT] two spans touching exactly (from === last) print two masks --> DEFERRED: pre-existing merge
  code (#3938), no known input reaches it.
- [CONVENTION] the plan file had no timestamp --> renamed to mask-words-3935-20260926T0555.md.
- [CONVENTION] the first commit's subject --> already DEFERRED in round 4.

## Review round 6
- [WARNING] charges counted comparisons, not the characters they read: an opening or a piece can be
  up to 1,024 characters, so 2,000 held values sharing a 1,000-character prefix and 120 near-miss
  runs cost 1.8s without reaching the budget --> FIXED: groupsFor and the walk charge one unit per 16
  characters a comparison can read (chunksOf). Test with that shape asserts the reply is withheld at
  the budget; reds on round 5's code (ran to the end). Most of that reply's remaining ~1s is the
  separator copies' knownFormsIn (#3938), profiled: the walk itself is 18ms. Reported on #3938.
- [WARNING] no test with held values sharing a long prefix --> that test.
- [NIT] two spans touching exactly --> already DEFERRED in round 5.
- [NIT] the inner `run` beside `runs` --> renamed `head`.

## Review round 7
- [WARNING] a key's own - or _ left out where the reply splits it (Prefix sk-ant-api03, then the
  rest: ...) ended the walk, and the key showed --> FIXED: at a - or _ of the key, the next piece is
  also tried one character on (charged per position), and the look-ahead files such a form under
  both characters. Test with the reviewer's reply; reds before, and reds with either half removed.
- [WARNING] "later start wins" left an abandoned first try at the same key readable --> FIXED: an
  earlier start that completes at the same place is kept too, unless a DIFFERENT held form completes
  between it and the latest start (the two-keys case). Test with the reviewer's reply; reds without
  keeping earlier starts, and the two-keys test reds without the sibling check.
- [WARNING] a word-shaped password with a short capital piece (Admin.2024.DB.prod) counts as not
  made of words, so a matching sentence is masked --> DEFERRED, measured: judging by the letters the
  word pieces cover (digit groups neutral) catches it, but at the threshold that does (0.8) it also
  stops walking 0.4% of random 32-character hex secrets and 1.3% of random 16-character passwords,
  against 0.005% and 0.08% today (20,000 of each). That trades a leak for readability, and this file
  masks when in doubt. The false positive needs the whole password's pieces, in order, within 4x.
- [NIT] an opening longer than any walked form was still looked up and charged --> skipped.
- [NIT] the whole-value CONTROL used "the password is ...", which SPOKEN masks anyway --> now
  "use Administrator1 here", asserting known_secret fired.
- [NIT] the Change section and the 43/43 line were stale --> corrected.
- [NIT] the round-6 cost test is the slowest in the file (~1.2s, mostly #3938's knownFormsIn) -->
  kept: it is what pins the length charge.

## Review round 8
- [BLOCKER] (SELF, round 7's code) deciding which earlier starts to keep compared every completion
  with every other: quadratic, outside the budget, and a 1.5MB reply repeating an opening 20,000
  times cost 1.5 to 4.3s and still came back --> FIXED: one sweep over completions in order of where
  they end, keeping the latest start of the two most recent different forms, answers each question
  in constant time (O(n log n) overall). Test compares the cost with the same reply holding no match:
  1.3x now, 4x before (reds). The two-keys and abandoned-try tests still pin what it decides.
- [NIT] the first commit's subject --> already DEFERRED in round 4.
- [NIT] a leading / was not taken off a piece --> it is now, as a trailing one is. Added to the glue
  test; reds before.

## Review round 9
- [WARNING] more glue let the key through: a trailing - on each piece, a label after = (Ab3d=part0),
  pieces wrapped in + --> FIXED: each run is also tried with - + _ / taken off both ends and with a
  label on either side of an inner = removed (not base64 padding); the opening is looked for in the
  same variants. Test with the reviewer's three replies; reds before.
- [WARNING] keeping earlier starts masked explanation: "Every Anthropic key begins sk-ant-api03- like
  this. Here is yours, split: <key>" lost the sentence, the bare mention counting as a first try -->
  FIXED: an earlier start is kept only if its walk matched a piece before the latest start (a try
  that got somewhere). Test; reds before. The round-7 abandoned-try test still passes (that try
  matched three pieces first).
- [NIT] the header's glue list and the budget figure were out of date --> corrected ("a few hundred").
- [NIT] the new tests are slow (the file takes seconds) --> DEFERRED: the timing asserts are CPU
  time, and most of it is #3938's knownFormsIn on the cost tests' large replies.
- [NIT] comments carry review round numbers --> DEFERRED: that is this file's existing idiom (#3938
  wrote eight of them); the reasoning each one carries stays at the code.

## Review round 10
- [BLOCKER] an abandoned first try at a key stayed readable when an unrelated key's table sat between
  it and the retry. The reviewer attributed it to the sibling rule; measured, the cause was the reach
  bound: the other key's table put the retry past 4x the key's length, so the first try's walk never
  completed --> FIXED: a walk that ends without completing, having matched its opening and at least
  one more piece (12 characters or more of the held value), masks the pieces it matched, each on its
  own (not the text between). It also covers a key given only in part. Test with the reviewer's reply;
  reds without it.
- [WARNING] the comment said the sibling exclusion was for keys SHARING AN OPENING, and the code
  excluded any other form --> the exclusion is gone. Measured: the round-9 rule (an earlier start is
  kept only if its walk moved before the latest start) already drops the two-keys case, because the
  second key's walk moves only on its own pieces, after its own start. Both versions of the sibling
  check were mutants no test could see. What remains is one pass over the starts, and each part is
  pinned: without the moved-before rule the two-keys, bare-mention and words tests red; without
  earlier starts the abandoned-try test reds.
- [WARNING] two cost tests compare CPU time --> already DEFERRED (round 9): CPU time, not wall.
- [NIT] a dense three-declaration let --> gone with the sweep.

## Review round 11
- [BLOCKER] (SELF, round 10's rule, a code line) the partial-walk rule counted the opening toward its 12
  characters, and a piece could be one character, so a reply naming a key's PUBLIC prefix (sk-ant-api03 is
  12 on its own, github_pat_ 11) followed by a bullet "-", the word "a" or a step number was masked
  --> FIXED: only pieces of OPENING_LEN or more after the opening count, and they must come to 8 or more;
  then the opening and those pieces are masked, each on its own. Test with the reviewer's five replies
  (none changed) and a control (a real two-piece try is masked); reds with the old threshold.
- [WARNING] the prefix-naming test could not catch that --> the round-11 test covers it.
- [WARNING] a first chunk said once, then repeated before the full key, stays readable (8 characters)
  --> DEFERRED: that is the same exposure as a lone 8-character fragment, which this file leaves alone
  below its 12-character minimum; masking it would mean masking any repeated key-opening run.
- [WARNING] the Not covered list did not describe the partial rule's envelope --> it does now.
- [NIT] the suite's runtime (#3938's knownFormsIn on the cost tests) --> already DEFERRED.
- [NIT] the overlap-widening branch has no failing test --> already DEFERRED (round 2's note).

## Review round 12
- [BLOCKER] (SELF, round 7's code line) the dropped-separator skip went exactly one character past a - or
  _, so a key with a doubled "--" or "__" at the split leaked whole. The reviewer measured about 3% of random
  secrets' base64url forms (a form the board holds for every value) containing a doubled separator
  --> FIXED: skipsFrom skips a run of up to SKIP_RUN (3) separators, in both the look-ahead filing and the
  walk, charged per position. Test with "--" and "__"; reds with the run capped at one.
- [NIT] the two slowest cost tests --> already DEFERRED (round 9).
- [CONVENTION] the first commit's subject --> already DEFERRED (round 4).

## Review round 13
- [WARNING] a URL-shaped held value's public scheme and host split into several runs, so a reply naming
  them (https://discord.com/api/webhooks/) passed the partial rule --> FIXED: only pieces that are not made of
  words or numbers count toward it. Test with the reviewer's three replies; reds without the filter.
- [WARNING] the budget withholds an ordinary-looking reply with ten held Anthropic keys and 200 prefix
  mentions --> DEFERRED as the stated trade-off (fail closed), with that measurement now in the budget comment
  instead of "hard to reach".
- [WARNING] a key in 4-character chunks joined by hyphens (licence-key style) leaked whole, pre-existing
  --> FIXED: a third copy drops - and _ as well (only forms without them can occur in it). Test (hyphens and
  underscores), with a control: a key with its own hyphens, split by words, is still masked. Reds without the
  copy. A filter restricting it to such forms was dead code (it could never change the answer) and is gone.
- [NIT] the partial walk follows the first way each position was reached; a contrived run of short matches
  can hide a real piece --> DEFERRED: contrived input, and the completed-walk path is unaffected.
- [NIT] a partial try whose piece had a label glued before it masks the label too --> DEFERRED: it only
  masks more.
- [NIT] a partial try counts one split_secret per piece --> DEFERRED: the count is a log hint, never shown.
- [NIT] test runtime --> already DEFERRED.

## Review round 14
- [BLOCKER] licence-key chunks with words between the groups (Qw8e-Rt2y and then Ui9o-Za3s ...) leaked whole:
  the walk saw each hyphen-joined pair as one run it could not match, and the no-separator copy still held the
  words --> FIXED: pieceVariants also tries a run with its internal - and _ removed, one piece of the key; the
  opening is looked for in the same variants. Test (hyphens, underscores, sentences between); reds without it.
- [WARNING] the no-separator copy repeated a full search when the text has no - or _ (then it equals the first
  copy): +38% CPU on the round-6 cost reply --> FIXED: skipped when identical.
- [CONVENTION] the partial-try threshold was a raw 8 --> PARTIAL_MIN, with why.
- [NIT] the Not covered comment --> the header now names the separators it handles; chunks joined by other
  punctuation in the middle of a run stay the listed gap.

## Review round 15
- [WARNING] a key given without its public prefix (the part after sk-ant-api03-, split by words) started no
  walk, pre-existing --> FIXED: the part after the last - or _ in a form's first 16 characters is walked as a
  form of its own (addWalked's usual checks: 12+ characters, not word-shaped). Test (words, a table) with a
  prose control; reds without it.
- [WARNING] a label joined by a hyphen (chunk-2-Rt2mNp9b) hid the piece --> FIXED: each tail after a - or _
  (up to four, and only OPENING_LEN or longer) is tried as a piece. Test; reds without the tails. A first
  version with no length floor let "Wor" (the tail of pi03-Wor) complete a walk from a prose mention of the
  prefix, which then won as the later start and left the real first chunk readable: caught by the round-5
  test, fixed by the floor.
- [WARNING] the budget comment gave one shape's measurement --> it now says what fills it: (held forms sharing
  an opening) x (mentions of it in the reply).
- [NIT] a completed walk masks the words between its pieces; the partial path does not --> DEFERRED: only
  over-masks.
- [NIT] a span can start at a glued label --> DEFERRED: only over-masks.
- [NIT] absolute CPU bounds in the tests --> already DEFERRED (round 9).
- [NIT] the Not covered block reads densely --> DEFERRED: it is accurate; the plan's list is the readable one.

## Review round 16
- [BLOCKER] (SELF, round 3's madeOfWords) "three letters with a vowel" called 98% of random single-case
  secrets words (zvqxrpldkinaeout), so they were never walked and leaked split by a word --> FIXED: wordLike
  needs a quarter vowels and no run of four consonants. Measured over 20,000 each: random lower/upper
  16-character secrets 98% -> 7% taken for words (12 characters: 15%), random keys and hex 0%, and
  Administrator1, Settings2024, Password123, correcthorsebatterystaple still words. Test (three single-case
  secrets split by a word, with round 3's sentences as the control); reds with the old rule.
- [WARNING] a retry within reach of an abandoned try completed from the earlier start too, and that start's
  whole span hid the prose between ("Sorry, again:") --> FIXED: a completing walk records the runs it matched,
  and an earlier start is masked piece by piece. Test; reds with the whole-span version.
- [NIT] a span can start at a glued label --> already DEFERRED (only over-masks).

## Review round 17
- [BLOCKER] (SELF, round 12's skip) a piece matched past a skipped separator linked back to the SKIP
  position, which is never recorded, so the trace back stopped there: every piece before it in a partial or
  abandoned try stayed readable (an Anthropic key body holds - and _) --> FIXED: a piece links back to the
  reached position it came from and records where it really started; its length counts from there, so a
  skipped separator is never counted as matched. Test (three shapes, every piece asserted); reds with the
  old link.
- [WARNING] no test covered several dropped separators in one try --> that test.
- [WARNING] a label with more hyphens than the four tails hid the piece --> FIXED: tails are taken from the
  end. Test with six hyphens; reds when taken from the start.
- [WARNING] the budget could withhold a long link-heavy reply --> DEFERRED as the stated fail-closed trade-off;
  split_search_limit is logged, so the rate is observable after release.
- [NIT] the Not covered block was dense and one sentence garbled --> rewritten as a list.
- [NIT] the split_secret count / absolute CPU bounds --> already DEFERRED.

## Review round 18
- [WARNING] a completed walk masked one span from the first piece to the last, so a word-labelled table
  collapsed to one row and a bullet list to one bullet --> FIXED: the latest start is masked piece by piece
  too, as earlier starts and partial tries already were; the row labels and bullet text stay. Test (six
  labelled rows keep their shape); reds with the whole span. Two older tests asserted the old shape: the
  two-keys count is now one split_secret per piece (at least two), and the round-5 test no longer demands the
  public prefix its own sentence names be hidden.
- [WARNING] wordLike (a held value is words, so do not walk it) and looksRandom's wordish (a long name is
  words, so do not mask it) look like two copies of one rule --> DEFERRED, and said at the code: they answer
  different questions in opposite directions, and changing main's catch-all is not this card.
- [WARNING] every run's variants were built even when nothing matched: 2.5x on text dense with - _ = -->
  FIXED: a run's variants are built on first use, and a run holding no indexed opening (checked on it and on
  it without - and _, of which every variant is a substring) starts nothing. Measured 118ms -> 76ms on a
  100KB glue-dense reply with 50 held keys. A cost test sets a ceiling; a timing ratio could not tell the two
  versions apart reliably, so the laziness itself is measured, not pinned.

## Review round 19
- [WARNING] a label glued AFTER a piece with - or _ (Zq8vLm3p-part1) hid the whole key: only tails were
  offered --> FIXED: heads before each - or _ (up to four, OPENING_LEN or longer). Test; reds without them.
- [WARNING] a first chunk under OPENING_LEN left the rest readable as one run (22 of 24 characters) --> FIXED:
  fragmentsIn masks any run holding FRAGMENT_LEN (12) consecutive characters of a held value as given. Not its
  public prefix (slices start after the last - or _ in the first 16 characters), and not a run that is itself
  words (a webhook URL's com/api/webhooks/). Test with the reviewer's two replies and a prose control; reds
  without it, and the round 5/11/15 prose tests red if the prefix is sliced too, and the URL test reds without
  the run check.
- [WARNING] the budget comment called one measurement "well inside" --> restated with the reviewer's measured
  rates (6 of 20 at 400 mentions, 1 of 20 at 160, 0 at 80 or fewer).
- [WARNING] a reply the budget stopped said "Kosmos removed a password or key", which is false when none was
  found --> FIXED: UNCHECKED ("could not finish checking ... so it is not shown"). Nothing compared the string.
- [NIT] from may start at a glued label --> commented at the line (only ever masks a little more).
- [NIT] a repeated short piece's later copy is not traced --> DEFERRED: a repeat exposes no new key material.

## Review round 20
- [BLOCKER] (SELF, round 19's fragmentsIn) each run was read only to its first 4,096 characters, so a held
  fragment past that in one long run was never looked at, and nothing said so --> FIXED: the whole run is
  read (one Set lookup per position, linear). Test with the fragment 4,200 characters in; reds with the cap.
- [WARNING] the fragmentsIn comment overstated what the code did --> true once the cap is gone.
- [WARNING] no test at the cap boundary --> the round-20 test.
- [NIT] the inline 4096 --> gone with the cap.

## Review round 21
- [WARNING] a held NAME=value line (knownsecrets.js holds whole lines from secrets files) put slices of
  the public NAME into the fragment index, so a reply naming KOSMOS_CF_ACCOUNT_ID_FOR_DEPLOYS was masked
  --> FIXED: NAME=value values are left out of the walk and the fragment index (the value is held on its
  own). Test with a value-split control; reds without the exclusion.
- [WARNING] the budget withheld ordinary dense prose (9 of 20 at 80 mentions with five keys) while an
  exhausted search had cost only 37 to 50ms --> FIXED: WORD_WALK_BUDGET 250,000 -> 1,250,000; every
  adversarial test still ends at it within its CPU bound (all pass unchanged). The comment records it.
- [WARNING] overlapping pieces (a character given twice) leave half the key --> DEFERRED, named in Not covered.
- [NIT] the fragment index counted values differently from the forms, with no memory bound --> the same
  taken values, and MAX_GRAMS (400,000).
- [NIT] the header said two copies and one span --> corrected; the plan's Change line too.
- [NIT] the budget-message test title claimed behaviour --> retitled (the behaviour is asserted by the budget
  tests).
- [NIT] the round-20 test could pass through the long-token catch-all --> it asserts split_secret fired.
- [NIT] CPU on large replies with 50 held keys roughly doubles --> DEFERRED: within bounds, measured.
- [CONVENTION] the first commit's subject --> already DEFERRED (round 4).

## Review round 22 (Sonnet)
- [BLOCKER] the fragment index stopped adding slices at MAX_GRAMS, so values past the cap had no fragment
  coverage and nothing said so (the rest of the file fails closed) --> FIXED: over the cap every value is
  indexed at one stride, so any 12 + stride - 1 consecutive characters of any value hold an indexed slice.
  Test: 2,000 distinct 1,000-character values (stride 5), pieces of the first and the last value at every
  offset in a stride, found by the fragment search (split_secret). Reds with the old cut-off and with a stride
  one too wide. (A first version used a small LCG that repeated slices across 187 values and passed against
  the old code; the test now asserts the index actually fills.)
- [WARNING] the NAME=value exclusion checked each form, so the hex and base64 of such a line were still
  walked --> FIXED: forms of an env line are never walked (still masked whole). Test: the name's hex split
  in two is left alone; reds when those forms are walked.
- [NIT] the setup guide log said "masked" for a reply withheld by split_search_limit --> FIXED: "caught".
- [NIT] this plan's cost section said 250,000 --> FIXED.

## Review round 23 (Opus)
- [BLOCKER] the NAME=value test matched padded base64 (Zq8v...Q1==), so such secrets were never walked or
  fragment-indexed, a leak class introduced in round 21 (about a quarter of openssl rand -base64 output) -->
  FIXED: a line counts only when its value after = is long enough (12) to be held on its own by
  engine/knownsecrets.js; padding never is. Tests: padded value held alone and with its AUTH_SECRET= line, split
  and fragment; control, a real env line's name stays readable. Reds when every such shape counts as a line.
- [WARNING] an exhausted walk costs about a second, and the board masks every served row on every read -->
  FIXED: mask() caches results per held set (1,000 entries, texts to 64KB, cleared on any change; callers get
  copies). Comment now gives the measured cost. Tests: cache cleared when the set changes; a mutated result
  does not leak into the next. DEFERRED: not starting walks from every variant of one repeated opening run
  (a cost refinement; the cache removes the per-poll multiplier).
- [WARNING] a repeated opening that is not public left its earlier copy readable --> FIXED: an earlier dropped
  start's opening run is masked when it has PARTIAL_MIN characters past the public head that are not words,
  and only when it is a separate copy (a label glued onto the latest start's run, KEY=sk-ant-a, is not).
  Tests with a public-prefix control; the glued case is pinned by the existing WORDS test.
- [WARNING] a held webhook URL's public path slices masked a placeholder URL --> FIXED: URL-shaped values are
  sliced only after their last /. Test: the placeholder stays, the token's fragment is still found.
- [WARNING] setKnownSecrets rebuilt the whole index every reload (about 2s at the cap) --> FIXED: an unchanged
  held set keeps its index. A changed set still rebuilds in full (DEFERRED: incremental build).
- [NIT] the fragment comment claimed more than the code --> FIXED: it lists its limits.
- [NIT] a test comment's 18ms predated the larger budget --> FIXED.

## Review round 24 (Sonnet)
- [WARNING] the unchanged-set check compared the held list in order, and the files are read in directory order
  --> FIXED: the signature is taken over a sorted copy (the index is still built from the list as given). Test
  with a rebuild counter; reds with the unsorted signature.
- [WARNING] the reach bound 4 was written in three places --> FIXED: SPLIT_REACH, one constant for the
  separator copies and the word walk.
- [WARNING] an exhausted walk holds the event loop about a second --> DEFERRED, documented: the measured cost
  of the budget; mask()'s cache pays it once per stored reply.
- [NIT] a test that checks only the withheld wording was named like a behaviour test --> FIXED: renamed.
- [NIT] short identifiers in wordSkippingSpans --> not changed: the file's existing style.

## Review round 25 (Opus)
- [WARNING] a key spaced one character at a time WITH words between its chunks leaked whole (the word walk and
  fragment search read only the reply as written) --> FIXED: both also search a copy with single-character
  spacing closed up (not line breaks: that joined the next line's words into a span, caught by two existing
  tests), only when the copy differs. Test with a spelled-out-letters control; reds without the copy.
- [WARNING] a whole env file held as one value was walked, so its second and later NAMES were masked in prose
  --> FIXED: a held value made of lines each held on its own (at least one a NAME=value line) is not walked or
  sliced, as a single line is not; its forms are still masked whole. Test through knownsecrets.collect on a real
  file; reds without it.
- [WARNING] the 40KB table timing test timed a cache hit --> FIXED: timed on the first call.
- [CONVENTION] repeated literals --> FIXED: publicHeadCut / PUBLIC_HEAD_MAX, MIN_VALUE_LEN, FRAGMENT_RUN from
  FRAGMENT_LEN, PIECE_VARIANTS_MAX.
- [NIT] the signature joined on NUL (collides, and kept every file) --> FIXED: sha256 of the sorted list as JSON.
- [NIT] the cache could hold 128MB --> FIXED: also bounded at 4,000,000 characters.
- [NIT] a NAME=value line whose value fell past MAX_KNOWN_VALUES was exempt anyway --> FIXED: exempt only when
  its value is held. Test at the cap; reds without the check.
- [NIT] comment debris in the budget comment --> FIXED.

## Review round 26 (Sonnet)
- [BLOCKER] once long values thinned the fragment index, a bare 12-character excerpt could be missed --> DECIDED
  and narrowed: the stride is the stated trade-off for whole files, but keys should not pay for files. Values up
  to KEY_LEN_MAX (256) keep every slice while they fit; only longer values are thinned, with what is left. Test:
  1,000 file-sized values plus a key, every 12-character excerpt of the key found; reds when keys are thinned
  with the files. Weakest premise: a secret longer than 256 characters is treated as a file.
- [BLOCKER] a label glued after a piece (=words, -part1) was masked with it, to the run's end --> FIXED: only the
  matched piece is masked (pieceSpan: the run's end if the piece ends it, else its place in the run, else the
  run). A run holding 12 characters of the key is still masked whole by the fragment search, on purpose. Test;
  reds with whole-run spans. The earlier-opening rule now also accepts the head without its last - or _.
- [NIT] "caught split_search_limit" when nothing was caught --> kept: the kind names what happened; the round-22
  change was from "masked", which was wronger.

## Review round 27 (Opus)
- [WARNING] a key grouped with / + or = between chunks shows in full --> DEFERRED to #3995: not a regression (main
  shows it), and outside this card (words between pieces).
- [WARNING] (regression) a hand-placed secrets file's comment lines and export / YAML / JSON names were walked, so
  prose naming them was masked --> FIXED: one parser, knownsecrets.assignedValue (NAME=value, export NAME=value,
  "name: value", "\"name\": \"value\""), holds each value on its own in the collector and decides which lines the
  mask does not walk; a comment line (#) is never walked or sliced. Test through collect on real files: the names
  and the comment's words stay readable, all four values still masked. Reds without the comment rule and without
  the YAML form.
- [WARNING] pieces spread past the reach limit leak --> DEFERRED to #3995 (main shows it; the reach bound is the
  documented limit).
- [WARNING] the round-8 timing test failed under a loaded suite --> FIXED: median of three interleaved runs.
- [NIT] the signature was recorded before the build --> FIXED: recorded after it completes (no test: forcing the
  build to throw needs a fault hook this file does not have).
- [NIT] past the cap, reordering kept a different subset --> FIXED: values taken in sorted order. Test; reds
  without it.
- [NIT] the spaced copy has its own budget, so the worst case is about twice the stated second --> noted here; the
  copy is searched only when it differs from the reply.
- [CONVENTION] a garbled fragment comment and a stale "the whole message is withheld" comment --> FIXED.
- [NIT] WITHHELD is exported and never returned, so one assertion is vacuous --> pre-existing, left.

## Review round 28 (Sonnet)
- [BLOCKER] (self-generated in round 27) a key in a comment line was never protected: "# OLD_API_KEY=value" held
  only as the whole line, and the line was not walked --> FIXED: assignedValue reads an assignment after a leading
  #, and the collector also holds each comment token of 12+ characters with a letter and a digit, so a key written
  bare in a comment is held and walked. Test through collect: both keys masked split by words; the comment's words
  and the names stay readable. Reds without the # strip and without the token rule.
- [WARNING] the YAML form read "Note: see the wiki ..." as a value --> FIXED: YAML and JSON values with no space.
  Test; reds with the old form.
- [WARNING] the spaced copy's own budget doubles the worst case --> restated in the budget comment (up to twice
  1 to 1.3 seconds); fail-closed per pass. Not changed: a shared budget would let the first pass starve the second.
- [NIT] WITHHELD is dead (pre-existing) --> left. [NIT] "caught" log wording --> kept (round 22).

## Not covered (still open, named on #3935)
- pieces out of order or reversed;
- an opening piece shorter than four characters that also has words after it;
- glue in the middle of a run other than a label joined with = and - or _ between chunks;
- a random single-case secret the word test still takes for words (about 7% at 16 characters, 15% at 12);
- a held value made only of words and numbers (Administrator1): the walk cannot tell it from prose,
  so it is not walked; such a password split by words stays readable;
- held forms over 1,024 characters split into pieces (whole files; not a key);
- a held value whose opening has no letter or digit (a PEM's -----BEGIN) split by words;
- a partial try with fewer than 8 characters after the opening in runs of 4 or more (left alone);
- overlapping pieces (a character given twice);
- a key split across two replies (the mask is per message).

## Weakest premise
That the first piece carries the key's opening, four characters or more. A key cut into 3-character
chunks with word labels between them still shows.
