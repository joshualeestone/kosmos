# runnable-dir-1592: one definition of "is this runnable", so a directory can never pass again (#1592)

`fs.accessSync(p, X_OK)` SUCCEEDS ON A DIRECTORY. Every hand-rolled runnable
check in engine/ therefore accepted a folder as an executable.
`engine/runners.js`'s `isRunnable` is the correct form and differs by one line,
`statSync(p).isFile()`. This exports it and makes it the single definition.

## The sites, measured rather than taken from the card

**DEFECT lines, at the merge base `ba85acf5`, because that is where the defect exists:**

    engine/devicedoor.js:34      lambda
    engine/githubdevice.js:109   byte-identical twin of the above
    engine/connect.js:434        willInstall presence check
    engine/connect.js:2147       canRunClaude

At HEAD these three files contain ZERO raw `accessSync(X_OK)` calls, which is the
branch's entire claim. The FIX sites are NOT given as line numbers. An earlier draft said "near 36 / 175 / 443 / 2229" and they had drifted by up to 119 lines, while the DEFECT table above reproduces exactly against the merge base, which is what made the fix row look equally checked. Anchor by function name, as this section already instructs.

🛑 **CORRECTED A THIRD TIME, AND THE SECOND CORRECTION WAS ALSO WRONG.**
This table previously carried the HEAD numbers under a heading that frames them as
naming the DEFECT. They name the FIX. A reader following them to see the bug arrives
at the repair and has no way to tell, because both look like ordinary code.

⚠️ **And the note that replaced them was false in the other direction.** It said THESE
LINE NUMBERS MOVED IN THE REBASE, of all four. Measured at both bases, exactly one did:

    devicedoor.js    34 -> 34     unmoved
    githubdevice.js 109 -> 109    unmoved
    connect.js      434 -> 434    unmoved
    connect.js     2082 -> 2147   MOVED, and the only one

⇒ So the section written to correct a stale table has now been wrong three times:
two phantom sites, then a blanket claim that everything moved, then fix-numbers
presented as defect-numbers. **Every one of the three was a correction issued without
measuring, because the CLASS was familiar and the instance therefore felt obvious.**
That is the reusable part: knowing the failure by name did not prevent it, and being
the person who named it made re-checking feel redundant.

🛑 CORRECTED AFTER REVIEW, AND THE ERROR IS THE ONE THIS SECTION EXISTS TO FIX.
An earlier version of this table listed six sites, adding `connect.js:946
haveBinary` and `connect.js:1218 post-install gate`, and put `canRunClaude` at
1905. Measured against the PRE-REBASE base `763d41f5` (the current merge base is `ba85acf5`, used by the table above; these two numbers belong to the old base and are kept for provenance), connect.js contains exactly
TWO raw `accessSync(X_OK)` calls, at 434 and 2082. Lines 946 and 1218 already
used `resolveBin().present`, and the file's own comment named the two that
remained.

⇒ So the table headed "measured rather than taken from the card" carried two
phantom sites and one wrong line number: it committed the same error class it
was written to correct, one layer down. The card's count of five was closer to
the truth than my correction of it.

One correction to the card still stands: it lists `engine/machine.js:412`, which
is already correct: it guards with `st.isFile()` two lines above and says why.
Left alone, and the sweep accepts a self-guarded site rather than forcing a
change into a file a live branch is editing.

Excluded on measurement: `projects.js:525` is `R_OK`, not a runnable check, and
four apparent hits are COMMENTS mentioning accessSync rather than calls.

## Why the guard is a source sweep and not six mocked arms

Fixing a subset is a route change rather than a behaviour change: harden the
entry check, leave the post-install check weak, and a directory takes a
different path to the same wrong answer. #1580 demonstrated that. A per-site
mock proves one site and says nothing about the next one added, so the sweep is
repo-wide rather than per-site.

🛑 **BUT IT ASSERTS A TOKEN, NOT THE CLASS, AND THIS PARAGRAPH SAID OTHERWISE.**
It read "the sweep asserts the CLASS: no file in the repo may ask the weak
question". It does not. It asserts one token, `accessSync|access(..., X_OK)`.

**The CLASS is "a directory reads as an installed binary", and that class is LIVE
TODAY through a spelling the sweep cannot see.** `fs.existsSync` is a
runner-presence decision in four places, named by MECHANISM because the numbers
this paragraph used to carry were all wrong:

    engine/create.js         setProvider        !fs.existsSync(runnerBin)
    engine/create.js         installJob         !fs.existsSync(runnerBin)
    engine/create.js         createAgentInner   !fs.existsSync(codexBin), and
                                                !fs.existsSync(bin) with codexPresent
    engine/openaiaccounts.js                    !bin || !fs.existsSync(bin),
                                                returning MISSING_RUNNER_SENTENCE

A folder at those paths passes every one of them exactly as it passed
`accessSync(X_OK)`, and the flow proceeds to a spawn that fails later. Those sites
pre-date this branch and are outside this card's scope.

🛑 **THIS PARAGRAPH CITED `create.js:763`, `:1602`, `:1777`, `:2159` AND
`openaiaccounts.js:281`. ALL FIVE WERE WRONG**, on this branch and on `main`,
landing on unrelated prose and on a `const bin = String(codexBin || '')`. **The
`install/setup.sh` citations in the very next paragraph are CORRECT**, which is
what made the bad set hard to see: a reader spot-checking one citation from this
section has an even chance of confirming the wrong half.

⇒ Whoever picks up #1616 would have inherited five dead pointers **from a plan
that argues at length against line numbers as keys**, which is the third distinct
place on this branch where that argument was made and then not applied to the
document making it.
**Carded as #1616 so the gap has an OWNER rather than a caveat.**

🛑 **AND THE CONSEQUENCE ARGUMENT #1616 NEEDS, MEASURED HERE WITH A CONTROL, because
"the same class exists elsewhere" understates it: THE TWO PATHS NOW DISAGREE ABOUT
THE SAME MACHINE.**

    a DIRECTORY at the bin path:
      fs.existsSync(dir)       true    <- the CREATION path accepts it
      runners.isRunnable(dir)  false   <- the first-run screen refuses it
    CONTROL, a real executable:
      existsSync true, isRunnable true

⇒ **A folder at `~/.local/bin/claude` is refused on the screen that only REPORTS,
and accepted by the path that actually LAUNCHES an agent.** That is the
higher-consequence route, and this branch made the disagreement possible by fixing
one side of it.

⚠️ **Still out of scope and still #1616's**, for the reason already given: widening
this card into `create.js` pulls in a file a live branch is editing. **But the card
should carry the asymmetry rather than the class**, because "existsSync also accepts
a directory" reads as tidy-up and "the two screens now contradict each other about
one machine" does not.

⚠️ **AND THERE IS A THIRD SPELLING THIS PLAN NEVER NAMED: THE SHELL FORM.**
`install/setup.sh` asks the same question with `[ -x "$p" ]` at :561, :641, :642 and
:647, gating whether a staged download is usable. Measured, with a control:

    [ -x <directory> ]   TRUE     <- same defect as accessSync(X_OK)
    [ -x <missing> ]     false    <- control, so the test can say no

⇒ So the class has THREE spellings, not two: `accessSync(X_OK)` in engine JS,
`existsSync` (#1616), and `[ -x ]` in the installer. **Out of scope for this branch and
pre-existing**, and recorded rather than fixed because the installer is not a file this
branch should be editing. But a plan that cards one sibling and is silent on another
teaches the next reader that the sweep was complete when it covered one language.

⇒ **SO IT IS CARDED: kosmos#1716**, which holds the measurement (27 bare against 2
same-path guarded, across 3 files) and the command that produced it.
⚠️ **THIS PARAGRAPH STATED THAT RULE AND THEN DID NOT FOLLOW IT.** Measured: #1616
appeared FIVE times in this plan and #1716 ZERO times, so a reader of the plan alone
believed the shell gap was uncarded, which is precisely what the sentence above warns
about. The card existed the whole time; the plan just never named it.

⭐ **A guard narrower than the class it names is the failure it was written to
prevent, arriving by a different door.** The behavioural arms assert the class at
the FOUR SITES THIS BRANCH TOUCHES; the sweep asserts a token repo-wide. Neither
is "a directory can never pass again", which is how the branch headline reads and
is why that headline is wrong.

📌 Found by a BLIND reviewer on the first pass of the real challenge loop. Eighteen
non-blind passes had read this plan and this guard repeatedly and none of them
questioned the word CLASS.


## What went wrong while building it, because the guard nearly shipped as decoration

The sweep stripped comments by DELETING them, which shifts every line number
below, so it reported a real defect at `machine.js:191` when the call is at 412.
Comments are now blanked and numbering survives.

My first planted arm DID NOT FIRE and I nearly recorded that as the sweep being
fine. I had planted the call inside a block comment, so blanking correctly ate
it. Planted in real code it reports `firstrun.js:43` for a call at line 43.

I also introduced a regression mid-edit, caught by reading the result rather
than trusting the edit: replacing the accessSync line at `canRunClaude` left the
following `canRunClaude = true;` in place, which would have made the check
always true. That is the exact direction this card is about.

## Verification

    isRunnable(directory)          false
    raw accessSync(directory)      true      <- the defect, asserted so the rest
                                                is not arguing with a phantom
    isRunnable(real executable)    true
    isRunnable(non-executable)     false
    planted weak call              caught, at the right line
    suite                          3127 pass 0 fail, measured at 07b18e2f+4
    ⚠️ PINNED TO A COMMIT, BECAUSE THIS FIGURE WENT STALE THREE TIMES. It read
    3113, then 3124, then 3126, each correct when written and wrong one
    iteration later. A bare count in a document is a claim about NOW, and this
    branch keeps adding tests, so it cannot stay true. Tied to a commit it is a
    historical fact and stays true forever. Re-measure with the repo's own gate
    rather than trusting this line.

## Sequencing

`git merge-tree` says four live branches conflict, all in connect.js:
THREE branches are live and conflict: willinstall-1556, live-1560-pete,
live-check-1560. ⚠️ MEASURED WITH EXACT REFS at 2026-08-31 23:50, not a substring
grep of ls-remote (which matches SHAs); control `zzqq-nope` returns 0.
🛑 THIS NAMED SIX, and THREE OF THEM NO LONGER EXIST ON ORIGIN (install-997,
dryrun-probe-1568, machine-honest-1567). The guard file states the live three
correctly and carries its own control; this plan carried the stale set, so the
sequencing advice rested on branches that had gone.
dryrun-probe-1568 and machine-honest-1567 are clean; control (self) clean.

A sequencing fact rather than a problem, and the rebase should be mine: this is
a mechanical cross-cutting change that rebases cheaply, while those are feature
branches mid-flight. Land the connect.js work first, then rebase this on top.


## What the guard's own stripper cost, measured across three versions

The sweep can only report on source it can see, so its comment-stripper decides
the whole guard's reach. Three versions shipped inside this branch's review, and
the first two were both wrong in the direction that HIDES a defect:

    a regex over the whole file    treated `/*` INSIDE A STRING as a comment
                                   opener. engine/unfurl.js:309 holds the string
                                   'image' + '/' + '*'.
                                   MEASURED: 803 lines of live code across 10
                                   files invisible to the sweep, including
                                   engine/status.js 294 and engine/create.js 272.

    a character scanner            fixed the string case, broke on REGEX
                                   LITERALS. A regex ending in an escaped slash
                                   presents `//` and drove it into line-comment
                                   state: 8 live lines in 6 files blanked,
                                   including engine/policy.js:91. A regex
                                   containing a quote desynced it into a string
                                   state it never left, after which comments
                                   stopped being blanked and the sweep went RED
                                   ON CORRECT PROSE in 4 files.

    line-based and deliberately    blanks only what it can identify from the
    dumb (shipped)                 START of a line. All three trap lines survive;
                                   weak calls planted after a string, after a
                                   regex ending in an escaped slash, and after a
                                   regex containing a quote are all DETECTED; a
                                   whole-line comment naming the call does not
                                   fire.

⭐ **The result that matters is narrower than it sounds: ZERO real weak calls were
ever concealed.** The answer the sweep gave was right throughout. But it was right
by luck of placement rather than by construction, and "the answer was correct" and
"the instrument could be trusted" are different claims. Only the first was true.

📌 **`test-support/code-only.js` already stated the rule I broke twice**: the two
failure directions are not symmetric, over-stripping gives a false PASS nobody
looks at, so when in doubt strip less. I wrote a cleverer stripper twice without
reading the one already in the repo, whose docblock is the argument against doing
exactly that. It is not reused directly only because it filters lines out and this
sweep must preserve numbering.


## The sweep has no stripper. Four failed there; two ship elsewhere.

Iterations 3 to 6 each fixed the previous comment-stripper and each opened a new
hole, every one in the direction that HIDES a defect:

    1  regex over the file    a `/*` inside a STRING opened a comment.
                              803 live lines across 10 files invisible.
    2  character scanner      broke on REGEX LITERALS. 8 live lines blanked, and
                              a regex containing a quote desynced it permanently.
    3  line-initial only      a TEMPLATE LITERAL puts `/*` at a line start.
                              A planted weak call stayed green.
    4  backtick parity        defeated by a regex CONTAINING a backtick, which is
                              LIVE at engine/create.js:1449. Parity inverts and
                              never recovers; 4 files desynced today.

⇒ **Each fix was right about the case it named and wrong about the next one.**
That is a heuristic aimed at a parsing problem, and there is no parser in this
repo (no acorn, espree, esprima or babel).

✅ **THE MEASUREMENT THAT ENDED IT: exactly FOUR lines in the whole repo match the
weak-call shape in raw source.** Two are prose, two are real calls. The sweep was
never doing a parsing job; it was judging four lines, and four is a list.

So THE SWEEP has no stripper: it reads RAW source, and every matching line must

🛑 SCOPED TO THE SWEEP, DELIBERATELY, BECAUSE THE UNSCOPED VERSION WAS FALSE. This
section used to say flatly "there is no stripper". **TWO SHIP IN THE GUARD FILE**: a
`stripComments` in the resolution-count arm, and a second regex strip in the
unconditional-true arm. The guard's own header says so and names their blind spots (a
`/*` inside a string literal, an unterminated `/*`, a template literal, `//` in a URL).
⚠️ A maintainer briefed by the unscoped sentence believes NO comment stripping ships and
does not know to check those blind spots. The four that failed did so in the SWEEP, where
the population is every line in the repo; the two that ship run over ONE BOUNDED FUNCTION
with a per-region control. ⚠️ CORRECTED: only ONE of the two does. `stripComments` runs
over one sliced function body and carries a per-site control. The second runs over the
WHOLE of connect.js and carries no per-region control; it protects itself differently, by
matching over RAW and STRIPPED source and failing if either hits, plus a control list.
The guard file states this correctly and this plan carried the superseded sentence.
They remain a different bet from the four that failed in the sweep, which is why they
ship, but "both are per-region" was not the reason.

appear in `KNOWN_WEAK_CALLS` as a `{ file, call, fn }` triple. ⚠️ THIS SAID `{ file, call }` AND THAT IS THE FOURTH SUPERSEDED KEY DESCRIBED IN THIS PARAGRAPH. `fn` is not decorative: without it the key is a set with the identities thrown away and CANNOT SEE A SWAP, which is measured in the guard file. Nothing is blanked,
so nothing can be hidden, and there is no state to desync.

🛑 **THIS PARAGRAPH SAID `KNOWN_WEAK_CALLS`, "each entry declaring `kind: 'prose'`
or `kind: 'guarded'`". THE CLASSIFICATION WAS DELETED, and this plan's own pass-9
table records deleting it.** So the paragraph below, which exists because this same
sentence was wrong for two iterations under two different names, was followed by a
THIRD wrong version of itself. Corrected here, at the site.

📌 **NAMING, RECORDED BECAUSE THIS PARAGRAPH WAS WRONG FOR TWO ITERATIONS.**
It said `ACKNOWLEDGED_PROSE` and `SELF_GUARDED`, two constants that appear nowhere
in the test file; `KNOWN_WEAK_CALLS`, the thing that actually ships, appeared
nowhere in this plan. A reader taking the plan as the design went looking for a
classifier that does not exist. Caught in review pass 8 and corrected here rather
than quietly: **a plan that describes a design the branch does not ship is a
second source of truth, which is the defect this whole branch is about.**

⚠️ **The cost, stated rather than discovered later:** a new comment mentioning
accessSync(X_OK) turns the test red until it is listed. That is deliberate. It is
loud, it is a two-line fix, and it makes somebody documenting this defect notice
that they are documenting it.

One stale direction is asserted: a listed guarded file that stopped asking fails
with the action to take.

⚠️ **The other direction was DELIBERATELY REMOVED and this sentence used to claim
it.** A listed prose entry whose comment is reworded no longer fails, because the
list is keyed on the matched CALL rather than the whole line. Rewording an ordinary
comment sentence in a file carrying three live conflicting branches (connect.js is NOT the most-edited file: 19 commits, rank 31, against web/index.html's 610 by `git rev-list --count` (597 by `git log --name-only`; the guard file quotes that one, and both are right because merge commits list no files)) used to red a test named for #1592,
on a branch whose author never opened it, which is the same friction cost this plan
accepted when it scoped the `writeState(` count to `becomeStuck`. Verified by
mutation in three arms: prose reworded stays green, a new weak call goes red, and
changing the call text itself goes red.

📌 **Proven by mutation, five arms, after the rewrite:** a weak call behind a
template literal, behind a regex containing a backtick, planted in runners.js
(which used to hold a blanket file exemption), a guard replaced by a comment
mentioning isFile, and a brand-new prose mention. All five RED, baseline and
post-restore clean.

## Review pass 8: four defeats of the guard, all measured, all fixed

Most did not touch the four production call sites.

🛑 THIS READ "None of them touched the four production call sites, which is now true of
all 39 findings across eight passes. The production change has been correct since before
iteration 1. Every defect this branch has produced has been in the thing guarding it."
**ALL THREE SENTENCES ARE FALSE**, and the near-identical claim is retracted further down
this same file. Four later iterations changed production sites in response to findings.
⚠️ The retraction below even says "the correction reached one site and not this one",
while this second uncorrected instance stood 44 lines above it.
⚠️ **AND THE REMAINDER OF THAT PARAGRAPH IS DELETED, NOT QUALIFIED.** It read "The
production change has been correct since before iteration 1. Every defect this branch has
produced has been in the thing guarding it." Both are false for the same reason.
🛑 An earlier edit replaced only the FIRST of the three sentences and left these two
standing AFTER the retraction, so a reader met the retraction and then met the false
claim in the plan's own voice, which is the worse ordering.

| # | How the reviewer defeated it | Why it worked | Fix |
|---|---|---|---|
| 33 | revert BOTH connect.js sites to `existsSync`, append one string at EOF containing the token | the arm searched the WHOLE file for the token, so "somewhere in connect.js" satisfied a claim about a CALL SITE | each site anchored (`async function willInstall(`, `let canRunClaude = false;`) and bounded to a span after it; the anchor is also asserted UNIQUE |
| 34 | `const docs = 'https://x'; canRunClaude = true;` | the arm consumed `noProse`, which truncated the line at the `//` inside the string. connect.js is full of URLs | the arm reads RAW source. It asserts ABSENCE, and over-stripping is the dangerous direction for absence, which the `noProse` docblock had backwards |
| 35 | add a genuinely unguarded call in a new file, then list its line, which is the exact remediation the failure message advises | listing had become the whole justification. An entry was a claim about a line with nothing behind it | entries declare `kind`. `prose` is re-checked against the RAW line (a live call cannot be smuggled in); `guarded` requires a file in `BEHAVIOURAL_ARMS`, whose names are asserted to exist as tests |
| 36 | delete ONLY the `continue;` from machine.js's `if (!st.isFile()) { ...; continue; }` | the arm proved a TOKEN was present within 20 lines. A directory then set present=false, fell through, passed X_OK, and was overwritten to present=true | a behavioural arm: `installedCheck` is handed a real directory and must answer not-present, plus a real executable that must answer present |

⭐ **The one sentence worth carrying out of pass 8: a guard being PRESENT and a
guard REACHING the call are different claims, and no source pattern available in
this repo can tell them apart.** Running the code can, which is why the token
search became a behavioural arm rather than a better regex.

⚠️ **Two smaller ones, also fixed:** `isProseLine` had NO consumer while three
assertions tested it (it now classifies entries, which is the one judgement it is
safe to make); and the long `KNOWN_WEAK_CALLS` docblock bound by POSITION to
`isProseLine` rather than to the constant it described, which is the hazard this
branch documents elsewhere.

📌 **Proven by mutation, eight arms, every one RED on the arm aimed at and for the
message aimed at, with baseline and post-restore both green:**

    33  revert both sites + EOF decoy      RED  the two connect.js sites delegate...
    34  URL string then override           RED  a repointed site cannot be...overridden
    35a unlisted plant                     RED  the set of accessSync(X_OK) lines changed
    35b plant listed as prose              RED  is listed as prose but the raw line is not a comment
    35c plant listed as guarded            RED  keeps a weak call and has NO behavioural arm
    35d fake arm name registered           RED  no test by that name exists in this file
    36  delete only the `continue;`        RED  installedCheck reported a DIRECTORY as present
    --  second `const runnable` decoy      RED  expected exactly one lambda, found 2

🛑 **THE LIMIT OF 35d, STATED SO NOBODY READS IT AS MORE THAN IT IS:** the registry
is checked against test NAMES declared in this file. A test that exists under the
right name and asserts nothing would pass it. That is a human review job and it is
visible in the diff; no check here can close it.

## Review pass 9 defeated the guard FIVE times, and the fix was to delete most of it

**Most findings landed in the guard rather than in the four production call sites.**

🛑 THIS READ "Nine passes, 44 findings, EVERY ONE in the guard and NOT ONE in the four
production call sites", and **BOTH HALVES WERE FALSE**. Four later iterations changed
production sites in response to findings: a unification regression, three module-scope
guards that would have killed the boot, a production fix nothing guarded, and a home
resolution moved out of an env object.
⚠️ The guard file measured this and retracted it at its own copy. **The correction
reached one site and not this one**, which is the class this branch is named for, in the
document that argues about it at length.
⭐ The root cause, in one sentence: **the only arms that ever held are the ones that RUN THE CODE.**

`BEHAVIOURAL_ARMS` covered `machine.js` and `runners.js`, and **neither is a site
this branch changes.** All four changed sites were defended by source-reading arms
alone, and that is where all five defeats landed. Neither behavioural arm could be
broken.

| # | How it was defeated | Fix |
|---|---|---|
| 1 | `noProse` keeps the OPENING line of a multi-line block comment verbatim, so a comment satisfied three arms at once | the arms it fed are GONE, replaced by behavioural arms |
| 2 | the bounded region was satisfied by live DEAD code, `if (0) <token>` | ditto; the assignment-count arm this row credited is itself superseded, see the section above |
| 3 | `isProseLine` called live code behind a closed `/* ... */` prose, so an unguarded call could be listed as prose | the whole `kind` classification is GONE |
| 4 | the arm registry was a substring search of this file, so `/* TODO test('name') */` satisfied it | the registry is GONE |
| 5 | the raw-source override matcher missed `/* belt */ canRunClaude = true;`, the exact case an earlier widening existed to catch | it now runs over BOTH raw and prose-stripped text and fails if either matches |
| 6 | that matcher was narrowed on the literal `true` while its stated justification was DIRECTIONAL; `= 1` passed, and publicView serves `canRunClaude \|\| false` so a 1 reaches the page truthy | it matches `true` and `1` and NOT any truthy literal. ⚠️ "any truthy literal" was MEASURED FALSE and retracted in the guard file and not here: `0.5`, `!0`, `[]`, `{}` and `Boolean(1)` all slip past it and deliberately not `= false` |
| 7 | the lambda body match was unanchored, so `(p) => existsSync(p) \|\| isRunnable(p)` satisfied it while accepting a directory | replaced by a behavioural arm |

⭐ **Almost every fix is a DELETION.** The guard shrank materially in that pass (figures deliberately not restated: it is now far larger and a line count in a plan ages within the hour) while
gaining four behavioural arms. Four comment strippers, a prose classifier, an arm
registry and two token searches are gone.

### The four behavioural arms, each proven in BOTH directions

    devicedoor     ghBin(DIRECTORY) -> null      reverted: returns the directory path
    githubdevice   state().gh -> "missing"       reverted: "present"
    willInstall    0 version probes spawned      reverted: 1 probe, and the answer flips
    machine.js     present.claude false          the continue;-deletion mutation

📌 **`githubdevice.state()` is ASYNC.** Reading `.gh` off the promise gives
`undefined` in BOTH arms, which looks like a result and is an instrument fault. It
cost me a wrong conclusion until the control disagreed with itself.

### 🛑 A CORRECTION TO PASS 9, MEASURED, AND IT MATTERS FOR SEVERITY

Pass 9 recorded that reverting `willInstall` "does NOT change the function's
answer for a directory", because the `--version` probe catches it anyway, and
scoped the harm there as a guard failure rather than a user-facing one.

**Measured here, both arms:**

    fixed     willInstall(directory) = true,  version probes spawned = 0
    reverted  willInstall(directory) = FALSE, version probes spawned = 1

**The answer flips whenever the probe answers ok**, which is what an injected
runner and a dry run both do. `connect.js`'s own docblock names that outcome: *"we
say willInstall FALSE and it was true -> AN UNANNOUNCED 281MB DOWNLOAD"*. So the
reverted site can produce the harmful answer, not merely fail a guard.

### The one site with no behavioural arm, stated rather than hidden

🛑 **SUPERSEDED BY THIS BRANCH, AND CORRECTED HERE RATHER THAN IN A NOTE AT THE END.**
This section described the hatch as computed inline inside `becomeStuck` and defended
by a SOURCE arm counting assignments. **The branch does not ship that design.** It
extracts `claudeHatchAvailable`, exports it, gives it TWO behavioural arms, and replaces
the assignment count with an exact-text pin plus a scoped `writeState(` count.

⇒ That is the "a plan describing a design the branch does not ship" defect this plan
names about somebody else's work, in its own text. It is fixed at the site because the
correction previously sat in a closing note thirty lines below, which left two sources
of truth and made the WRONG one the first a reader meets.

**What is still true, and is why the extraction was worth doing:** `becomeStuck` itself
remains uncallable from a test. It is not exported and early-returns unless the
module-internal `driver` matches the caller, so reaching it needs a full `start()` flow
driven to failure, which nothing in this repo does today. The extraction is what made
the part worth asserting reachable. Carded.

⚠️ **The weakness that survives the rewrite:** a decoy REPLACING the real assignment
rather than adding to it leaves the right count. The exact-text pin is what answers
that; neither arm is sufficient alone.

### ⭐ One thing the rewrite gained for free

Pass 9's decoy mutation leaves `connect.js` **syntactically unparseable**, and the
old guard still went 9 pass / 0 fail on it, because it only ever read the file as
text. **The new arms load the module, so a connect.js that cannot parse now fails
loudly.** A guard that passes on a file that does not compile was never a guard.

## A review recommendation DECLINED, with the measurement that settles it

A blind reviewer proposed trimming the retraction history from the comment blocks in
`connect.js` and `githubdevice.js` (roughly 65 of ~154 added comment lines in one and
~50 of ~90 in the other), on the stated ground that **"the plan file already carries
each of these episodes in full"**, so the code copies duplicate it and are the material
most likely to go stale next.

🛑 **THE PREMISE IS FALSE, AND DELETING ON IT WOULD HAVE DESTROYED THE ONLY COPY.**
🛑 **READ THIS BEFORE RERUNNING THE COUNTS: THEY WERE TAKEN BEFORE THIS TABLE
EXISTED, AND WRITING THEM DOWN INVALIDATED THEM.** Rerun the five greps today and
each returns **1, not 0**, because the only hit is the row below saying it is 0.

⇒ **A count cannot distinguish "the plan carries this episode" from "the plan carries
a row saying it does not."** That is use-versus-mention, and it is the third time it
has bitten on this branch: once reading deleted audit notes as live pointers, once in
a colleague's review, and now inside the artifact written to record the first two.
**The artifact describing the hazard is subject to the hazard**, which the test file
already names about its own comment-closer example. Found by the reviewer whose
finding this table declines, who got 1 each, saw it looked like a refutation, and read
the five lines before sending it.

✅ **TO RE-MEASURE HONESTLY, NAME THE COMMIT RATHER THAN THIS FILE.** Verified:

    git show 727e78cc:.claude/plans/runnable-dir-1592-20260830.md | grep -ci "wrong twice"   -> 0
    git show 727e78cc:.claude/plans/runnable-dir-1592-20260830.md | grep -ci runnable        -> 11  (control)

`727e78cc` is the commit immediately before this table was written, so it is the last
state in which the question can be asked cleanly. Naming the sha rather than saying
"the commit before" is deliberate: "before" moves every time somebody commits, which is
the same expiry-with-no-clock problem as a line number.

Measured against this file at that commit, with a control:

    becomeStuck canRunClaude "wrong twice"        0 hits
    publicView carries canRunClaude (#1595)       0 hits
    the "pass their own lists" retraction         0 hits
    github.test.js pins AGENT_WORKFORCE_GH_BIN    0 hits
    willInstall stopped calling claudeBinPath     0 hits
    CONTROL: "runnable"                          11 hits

⇒ Five of six episodes exist ONLY in the code comments. There is no duplication to
remove, so the volume argument does not apply to the material it was aimed at.

✅ **What survives the refusal, because the underlying worry is fair:** layered history
IS the material most likely to go stale next, wherever it lives. If it should be
consolidated, the history must be MOVED here first and the comments left pointing at
it. **Move, then trim. Never trim first.** Recorded so the next reviewer does not
re-derive the recommendation and act on the same untested premise.

## Production surface this branch ADDS, which an earlier version of this plan did not record

A blind reviewer found the plan under-described the branch: it listed only the four
repointed checks, while the branch also adds FIVE things to the production surface.
⚠️ THE CONTROL FIGURES THAT WERE HERE ARE DELETED, NOT UPDATED. They read "the plan names
`isRunnable` five times; each of these appeared zero times". Today: isRunnable 18,
claudeHatchAvailable 2, ghCandidateList 7. Both halves false, and SELF-DEFEATINGLY so,
because writing the table is what created the mentions it counted.
📌 This same file says four sections up that a bare count is a claim about NOW and must be
tied to a commit. Not applied here.

| addition | where | what it is |
|---|---|---|
| `runners.isRunnable` | `engine/runners.js` | **newly exported.** It existed and was module-private; exporting it IS the branch, and it was missing from this table |
| `connect.claudeHatchAvailable` | `engine/connect.js` | **new exported function**, extracted from what `becomeStuck` computed inline |
| `github.ghCandidateList` | `engine/github.js` | **new exported function.** Defined in `github.js` beside `GH_CANDIDATES`; `githubdevice` imports it for internal use and does NOT re-export it (that was dropped as new public surface); consumers require it from `github.js`. Neither name was exported on main, so no caller is affected |
| `AGENT_WORKFORCE_GH_CANDIDATES` | `engine/github.js` | **new env var read by production `ghPresent`**, not test-only. Read in `github.js` (it moved with `ghCandidateList`); `ghPresent` in `githubdevice.js` is still the production caller |
The name this branch ADDS is `ghCandidateList`, and the total export surface goes
from 9 to 10. ⚠️ TWO EARLIER VERSIONS OF THIS ROW WERE WRONG, and the second was the
correction to the first. It said the branch adds `{ PHASE, ghCandidateList }` (PHASE
was already on main) and that makeDoor "returns nine more names" (nine INCLUDING
PHASE, so eight more, implying a total of 11). ⇒ The row whose closing sentence is
about overstating the public surface overstated it twice, in the section written to
prevent exactly that.
| gh door honours the override | `engine/github.js` | **BEHAVIOUR CHANGE, not just an export.** `candidates` is now a getter calling `ghCandidateList()`, so `AGENT_WORKFORCE_GH_CANDIDATES` reaches the door as well as `ghPresent` |

🛑 **THE LAST ROW IS THE ONLY PRODUCTION BEHAVIOUR CHANGE OUTSIDE THE RUNNABILITY
CHECKS, AND IT IS RECORDED AS ONE RATHER THAN AS A TIDY-UP.** The env var previously
reached `ghPresent` only, so setting it made `state().gh` say `missing` while the door
still resolved and could exec a real `gh`. **Three independent reviewers flagged this
across three iterations, and the first two answers were to document it** at three sites
and here. That volume of documentation was the signal: **documentation is what gets
written when a thing is believed unfixable.** It was fixable in one getter.

🛑 **THIS CLAIMED FOUR ARMS AND THERE WERE NONE. CORRECTED HERE, LATE.** The
sentence read "Proven end to end through `door.ghBin()`, four arms ... override to
`''` gives null ... and with no override the control still finds the real gh". The
measurement behind it was ad hoc in a shell; it was never an arm, and reverting the
getter passed the WHOLE SUITE at EXIT_CODE=0.

✅ **What is actually pinned:** a real-door arm with TWO assertions (override to a
real executable returns it, override to a DIRECTORY returns null), verified to
redden when the getter is reverted. The `''` case is pinned separately, and the
"no override finds the real gh" arm is DELIBERATELY NOT WRITTEN because it would
exec the operator's own gh.

⚠️ **This was corrected in the CODE and left standing HERE for two iterations**,
contradicting a correct paragraph thirty lines below in this same section. The
plan is the surface reviewers read for the new env var, which makes it the worse
of the two places to leave it.

🛑 **THIS DESCRIBED A GETTER THE BRANCH DOES NOT SHIP.** It said the require inside
the getter is "LAZY on purpose" to avoid a cycle. **There is no require in the getter
at all.** That lazy-require form was REMOVED because it gave `door.state()` a reject
path against `devicedoor.js:99`'s "Never rejects"; `ghCandidateList` was moved into
`github.js` so the getter calls a local function and the dependency is
one-directional. Reproduced both arms before and after: the broken-load arm REJECTED,
then RESOLVED.

⚠️ **Recorded rather than quietly rewritten, because this is the third time a fix on
this branch staled its own plan, and the first two were caught by reviewers rather
than by me.** After the move I hunted the stale records in the CODE and did not hunt
the PLAN.

⚠️ **`AGENT_WORKFORCE_GH_CANDIDATES` is the one that matters most and was least
visible.** A new environment
variable read on a production path is a surface, not an implementation detail: it changes
what the program responds to, and nothing in the plan told a reviewer to look for it.

🛑 **THAT SENTENCE SAID "THE THIRD" UNTIL A ROW WAS ADDED ABOVE IT, AND THE ORDINAL
SILENTLY STARTED POINTING AT `ghCandidateList`.** Nobody flagged it; it was found while
adding a fifth row. **A positional reference into a list is the same fragile-citation
shape as a line number**, which this plan rejects at length elsewhere and then used
about its own table. Named rather than numbered now.

📌 **Its semantics are part of the surface and belong here, not only in the code:**
it is read with ONE RULE: anything that is not a non-empty string yields the defaults.
So `''` means UNSET, both as an argument and from the env, and `':'` is how a caller asks
for NO candidates. Measured: `ghCandidateList('')` and env `''` both give the three real
paths; `':'` gives `[]`.

🛑 THIS PARAGRAPH DESCRIBED THE SUPERSEDED TWO-RULE DESIGN UNTIL ITERATION 30. It said
`''` was "a meaningful value meaning 'no candidates', not 'unset'", which is the exact
OPPOSITE of the shipped rule and of a shipped test named for it. It then reported its own
measurement, "gave the three real paths", which is the unset behaviour, so it refuted
itself two sentences later.
⚠️ Worst placement possible: this is the section headed "Production surface this branch
ADDS", about the variable it calls "the one that matters most and was least visible", and
it warns that a reader will otherwise get the semantics backwards. A reviewer briefed by
it would have.
A reader who knows only that the variable exists will get the semantics backwards, which is the half a surface table exists to prevent.

⭐ **The general shape, which is why this section exists rather than a one-line fix:** a
plan that lists what a change *repoints* will silently omit what it *adds*. The site
table was the thing everyone read, and it was complete for its own question and
incomplete for the branch.

📌 A related staleness the same reviewer found, now **corrected at its own site**
rather than here: the section headed "The one site with no behavioural arm" described a
design the branch does not ship. Kept as a pointer only, because a correction living
away from the thing it corrects is what made the stale text survive two reviews.

## History of the `becomeStuck` paragraph, moved out of `engine/connect.js`

**Moved here rather than deleted.** Two independent blind reviewers flagged 25
lines of explicitly non-operative retraction archaeology sitting in a shipped
source file's hot path. This branch's own rule, written when the first trim was
DECLINED on a measurement, is **move, then trim, never trim first** - because at
that time these episodes existed ONLY in the comments and deleting them would
have destroyed the only copy. Moving them here creates the copy, which is what
makes trimming the source safe.

⇒ **The decline was right and so is the move. They are the two halves of one
rule**, and doing the second without the first is what the measurement prevented.

Verbatim, as it stood in `connect.js`:

```
------------------------------------------------------------------
HISTORY, kept in full because each layer was wrong in a different and
instructive direction. Nothing below is operative.

⚠️ THIS PARAGRAPH HAS BEEN WRONG TWICE, IN OPPOSITE DIRECTIONS, AND THE
SECOND TIME IS THE INSTRUCTIVE ONE. It first said `canRunClaude` "decides
whether the STUCK screen tells somebody to type `claude` in Terminal";
it COMPUTES that. The correction then said `publicView` DROPS THE FIELD,
so the hatch had never rendered and this repoint was dead code. That
correction is what the block now leads by refuting.

⭐ A retraction can go stale exactly like the claim it retracted. This one
understated the change rather than overstating it, which is why nobody
caught it: an error in the modest direction reads as caution.

⚠️ THE OLD CLOSING SENTENCE SAID the reason is "it is not wired", not "it
is wired and weak". Retracted: since #1595 it IS wired. It was called out
rather than quietly deleted because it was the block's LAST line, and a
block that ends on its own retracted claim gets read ending-first.

📌 THAT ARGUMENT IS WHY THIS BLOCK WAS REORDERED RATHER THAN TRIMMED. It is
a reason to control WHICH sentence is prominent, and by its own logic the
prominent one should be the true one, not the newest retraction. Three
layers of "what an earlier paragraph got wrong" stood between a maintainer
and the single fact they needed. The honesty was never the problem; the
ordering was.
```

📌 The operative fact stays in the code, where a maintainer needs it: since #1595
`publicView` carries `canRunClaude`, so repointing `becomeStuck` is a live
user-visible change rather than dead code.

## Comment archaeology moved out of the source (iteration 21, CONVENTION)

A review measured the diff at **47 added code lines against 448 added comment/blank
lines** across the six production files, and named the sub-genre worth moving: not
annotation, but paragraphs whose SUBJECT IS A PREVIOUS REVISION OF THE PARAGRAPH. A
reader of `isRunnable` learned nothing about `isRunnable` from twelve lines about a
census that no longer exists.

Kept here because the history is real and re-deriving it wastes somebody's evening.
Trimmed from the source because it is not what the next reader of that function needs.

### From `engine/runners.js`, inside `isRunnable`

> A CENSUS OF ANOTHER FILE'S CALL SITES USED TO LIVE HERE ("connect.js asks zero times
> directly now"). True when written and checked by nothing, in a branch whose own plan
> documents comment staleness as its recurring defect. The sweep in
> engine.runnable-not-directory.test.js is what actually holds that count, and its
> failure message is where it belongs.
>
> CORRECTED: an earlier version of this sentence said "three times over there",
> counting the post-install gate. That gate already used resolveBin().present and never
> asked this question, which this branch's own plan measured and recorded. I rewrote
> this comment to fix one stale claim and introduced a second in the same edit.

### From `engine/firstrun.js`, above the probe catch

> AND THE CORRECTION THAT USED TO SIT HERE WAS ITSELF FALSE, IN THE PARAGRAPH REWRITTEN
> TO FIX A STALE CLAIM. It read "willInstall never enters isRunnable". It does, on every
> call: `resolveBin` computes `present: isRunnable(...)` on all four of its routes.
>
> SCOPE, because "four routes" undercounts: `resolveBin` has SIX return points, and two
> of them (the non-openai provider guard, and the post-scan fallthrough) return
> `present: false` without calling `isRunnable` at all.
>
> Named by route rather than by line number ON PURPOSE: those four numbers were
> accurate, and `runners.js` is edited by this same branch, so they are the
> fragile-citation shape this branch's own test file rejects as a key.

### The tension this exposes, which is worth stating once

The loop's findings are largely FALSE CLAIMS IN COMMENTS. Correcting one in place adds
a paragraph about the correction, so the remedy grows the thing the CONVENTION finding
is about. Iteration 21's own W3 and W4 fixes each added such a paragraph.

**The rule that resolves it: keep the OPERATIVE claim at the site (what the code does,
why the require is where it is, what reddens if you move it) and move the "an earlier
version of this said" material here.** A retraction belongs where somebody researching
the decision will look, not where somebody reading the function will trip over it.

## The retracted 281MB leak, in full (moved out of engine/connect.js, iteration 28)

Kept here because the reasoning is worth more than the conclusion, and removed from the
source because thirty lines of retracted claim in a hot function is what the CONVENTION
findings keep pointing at.

**What I claimed (iteration 24):** `homeDir()` was called inline in the install env
object at try-depth zero, so a throw escaped `installClaudeCode` before the unlink below
it, stranding the verified ~281MB download.

**Why it is false (iteration 25):** `store.ROOT` is
`dataRootFor(process.platform, os.homedir(), process.env)` with `os.homedir()` as an
**eager argument**, and `download()` needs `store.ROOT`. So `os.homedir()` throwing makes
`store.ROOT` throw and the download die first. There is never a verified file to strand.
Neither `AGENT_WORKFORCE_DATA` nor `AGENT_WORKFORCE_HOME` rescues it. Control: with
`homedir` working, `store.ROOT` resolves normally.

⚠️ **A second wrong reason, added while retracting the first (iteration 27):** I wrote
"neither is what store.ROOT reads". `store.ROOT` **does** read `AGENT_WORKFORCE_DATA` and
returns on it before touching home. The correct reason is the eager argument. One copy of
this retraction stated it correctly and one did not, which is the two-copies defect inside
the retraction of a two-copies defect.

**Why the arm proved nothing:** it stubs `runners.homeDir` on the module object, faulting
only connect.js's explicit call while `store.ROOT` keeps calling `os.homedir()` directly.
That state has no production cause. It reddened when the guard was removed and I read the
red as proof. **A mutation reddening an arm proves the arm sees the mutation, not that the
mutated state can occur.**

**What survives:** the guard is harmless defence in depth and is not reachable today.

## History of the firstrun `.catch` paragraph (moved out of source, iteration 29)

Three drafts, wrong in different ways, kept here so nobody re-derives them.

1. **"willInstall never enters isRunnable."** False: `resolveBin` computes `present` with
   `isRunnable` on both claude rungs, so every `resolveBin('claude')` enters it
   transitively.
2. **"four routes."** An undercount that did not matter: `resolveBin` has six return
   points and two return `present: false` without calling `isRunnable` at all. The claim
   that matters is only about the two claude rungs.
3. **`isRunnable` named as a third throw source.** It is not one: it wraps its whole body
   in try/catch and returns false. Two of the three named sources were right and the third
   was invented.

⚠️ A fourth defect was structural rather than factual: the paragraph carried the phrase
"Measured, with a control" on a claim that is a **reading of which lines call what**, not
a behavioural measurement. The identical mislabel was corrected in `connect.js` and left
standing here, which is the fixed-one-site-left-its-siblings class inside a correction.

📌 And two different sentences in one block each called themselves "THE OPERATIVE CLAIM".
Two of them means neither is.
