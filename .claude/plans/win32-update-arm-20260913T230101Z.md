# win32-update-arm -- S4: arm the Windows in-app updater

Branch: `win32-update-arm`, off `origin/main` @ `1bfd8ad2` (SELF_INSTALL still `['darwin']`;
updater inert). Design: `kosmos-scripts/win32-updater-design.md` (§1, §4, §5, §6b, §7-S4, §9).
Brief: `kosmos-scripts/brief-win32-update-arm-S4.md`.

This slice flips the updater on: `SELF_INSTALL` gains win32, `installedRoot()`/`beginInstall`/
`maybeAutoInstall` grow win32 arms that drive the S3 board-side entrypoint
(`win32update.begin()`), the status carries channel + phase + the win32 outcome record, and the
Settings card / confirm / overlay show honest Windows wording. Nothing auto-cuts a build; the
next Windows build cut from main after this merges carries a LIVE updater.

## What I found ALREADY WIRED (S0-S3) vs the S4 delta

Read at HEAD `1bfd8ad2`:

- **platform.js**: `SELF_INSTALL = Object.freeze(['darwin'])` (line 92). `canSelfInstall` reads it.
  `SUPPORTED` already includes win32 (agents run there), so `server.js` real-start ALREADY arms
  `allowLiveExecution()` on win32 (line 13155, `if (platformGate.isSupported())`). So the armed
  updater will have `liveExecutionAllowed()` true in a real Windows board and false under
  `node --test` (fail-closed).
- **update.js**:
  - `pointerFor`/`readManifest`/`updateChannel`/`releaseBase`/`windowsBuildName` — S1, done. The
    per-platform pointer + win32 manifest validation (version/sha256/versioned) is in place.
  - `windowsBundleRoot()` (line 153) already lazily asks `win32board.bundleRoot()`; `manualOffer()`
    (line 174) already gives `{version, download}` for a win32 bundle with a newer build. In S1 this
    fires for EVERY win32 bundle (in-app not armed). **S4 delta:** it must fire ONLY when the in-app
    path is refused for location (OneDrive/Program Files), else a normal bundle shows [Update].
  - `installedRoot()` (line 390) is Mac-only: checks `runtime/bin/node` + `app/server.js`; returns
    null on a win32 bundle (`runtime\node.exe`) AND on a source checkout. **S4 delta:** win32 arm
    returns `windowsBundleRoot()`.
  - `beginInstall` (line 719) ends in `selfInstallRefusal()` (throws on win32 today) → liveExec gate
    → `spawn('/bin/sh', ...)`. **S4 delta:** a win32 branch that calls `win32update.begin()` instead
    of `/bin/sh`, and `selfInstallRefusal('win32')` becomes null.
  - `maybeAutoInstall` (line 340): available() + installedRoot() + autoPref().on + 1h backoff +
    single-flight — all platform-agnostic. **Once installedRoot()/beginInstall have win32 arms this
    works on win32 with the SAME policy** (Josh decision 1). No Windows-only policy.
  - `lastAttemptView`/`seedFromDisk` read only Mac `install.status`/`install.started`. **S4 delta:**
    a win32 arm reads the win32 update outcome (`win32apply` status/journal). `updatePhase` does not
    exist yet anywhere.
- **win32update.js** (S2+S3): `prepare()` = B0-B4, `begin()` = B5 (writes journal, spawns helper),
  the CLI, and the helper entry (`--kosmos-update-apply`). `begin(opts)` takes `{root, port,
  boardPid, fromIdentity, liveExecutionAllowed, spawn, boardTask, ...}` and defaults
  `liveExecutionAllowed`→`liveExec.liveExecutionAllowed`, `boardTask`→`defaultBoardTask()` (reads
  `win32board.startedByTask()`/`status()`), port→16180, boardPid→null. **It is fully built; S4 only
  CALLS it from the board.** `unusualLocationRefusal(root, env, base, policy)` (the ONE named rule,
  `UNUSUAL_LOCATION_POLICY='refuse'`) is exported and already set to REFUSE — decision 5 is
  confirmable, not new. B0 preserves the full precondition set + never-downgrade (readOffer's
  `newer()` gate) + keeps the `previous-<from>` folder (whileLocked writes it via win32apply).
- **win32board.bundleRoot()**: returns the bundle root for a real win32 build, and **null for a
  source checkout and for a bundle inside the updater's work dir**. On THIS dev box the board runs
  from `src\kosmos\engine`, so root = `src\kosmos`, which has no `runtime\node.exe` / `app\server.js`
  → null. This is the source-checkout guard.
- **server.js**: status carries `updateChannel` (line 3253) but NOT `updatePhase`. `update` field =
  `installedRoot() ? available() : null`; `updateManual` = `manualOffer()`. Install route (8219)
  refuses via `selfInstallRefusal()` then `installedRoot()`. Check route (8198) mirrors.
- **web/index.html**: `paintUpdateCard(running, offer, look, manual, channel)` (15233): offer arm
  "Version X is ready." [Update] (act=update); manual arm = Windows download + steps; sole live
  "Up to date." at 15349. Confirm `#uc-small` (8656) = Mac wording. Overlay initial `<b>Updating.</b>
  Your agents keep working.` (15601); `settle()` failure/slow copy (15640-15645) carries the Mac-only
  "open it again from Applications" (15645) — the one un-gated update string. `updateVerdict` pure
  (15680). The board-copy layer (`onWindows()`/`windowsCopy()`/`platformCopy()`/`data-win-copy`,
  13271-13410) is the seam for Windows wording; Mac reads inline strings and stays byte-identical.

## The S4 build

### 1. platform.js
- `SELF_INSTALL = Object.freeze(['darwin', 'win32'])`. Keep frozen. Update the doc comment (win32
  now self-installs because the S2-S4 updater exists).

### 2. update.js
- `installedRoot()`: add a win32 arm returning `windowsBundleRoot()` (null on source checkout).
- New `windowsLocationRefusal()`: on win32 with a bundle root, delegate to
  `win32update.unusualLocationRefusal(root, process.env, cache.base || releaseBase())` (lazy
  require, cycle-safe). Sentence or null. THE single client-facing surface of the ONE named rule.
- New `installOffer()`: `installedRoot() && !windowsLocationRefusal() ? available() : null` — the
  [Update] offer. One derivation for both status call sites and the install route.
- `manualOffer()`: keep the source-checkout guard (null when no bundle root); ADD `if
  (!windowsLocationRefusal()) return null;` so a normal armed bundle shows [Update], and only a
  OneDrive/Program Files bundle shows the manual download.
- `beginInstall` win32 branch (`beginWindowsInstall`): after single-flight + fresh lastAttempt,
  call `win32update.begin({ root: installedRoot(), port, boardPid })` via a `windowsInstallerFn`
  seam. On `{ok:false}` release single-flight, record `because` on lastAttempt, set `autoFailedAt`
  when `opts.auto`. On `{ok:true}` leave single-flight set (helper will stop+restart this board).
  Board port/pid come from a new `setBoardContext({port,pid})` the server calls at listen time.
- `maybeAutoInstall`: unchanged logic — now reaches the win32 branch via installedRoot()/beginInstall.
- `updatePhase()`: win32 only — `win32apply.readJournal(journalPathFor(anchorDir)).journal.phase`
  or null.
- `lastAttemptView()` win32 arm: when no Mac record, read the win32 update-status.json
  (`win32apply.statusPathFor(anchorDir)`) and map its outcome to the `{startedAt,endedAt,code,
  because,log}` shape the overlay reads (`code` non-zero on a failed/stuck outcome so
  `updateVerdict` says 'failed'; null log).
- Exports: `installOffer`, `windowsLocationRefusal`, `updatePhase`, `setBoardContext`,
  `setWindowsInstaller`.

### 3. server.js
- Status (3247/3253): `update: updates.installOffer()`, add `updatePhase: updates.updatePhase()`.
- Check route (8198): `offer: updates.installOffer()`.
- Install route (8219): after the `selfInstallRefusal`/`installedRoot` arms, add
  `const loc = updates.windowsLocationRefusal(); if (loc) 409 {error: loc}` (readable refusal for a
  OneDrive/Program Files bundle — belt-and-braces; status already shows manual there).
- Real-start block (~13052): `updates.setBoardContext({ port: server.address().port, pid:
  process.pid })` next to `startPolling`.

### 4. web/index.html (Mac byte-identical via the copy layer)
- Confirm `#uc-small` (8656): add `data-win-copy="updateConfirmBody"`; table key
  `updateConfirmBody` = "Kosmos downloads the update, then closes for a few seconds while it swaps
  it in. <b>Your agents keep working the whole time.</b>". Mac inline text unchanged.
- Overlay initial lead: `onWindows()` → `<b>Downloading {body.updating}.</b> Your agents keep
  working.`; Mac branch keeps the exact `<b>Updating.</b> Your agents keep working.` bytes.
- Overlay poll loop: on Windows, set the `<b>` lead per reachability — reachable = "Downloading X.",
  unreachable (swap) = "Updating." — and skip the Mac 'slow' "Still updating." relabel on Windows.
- `settle()` failure/slow copy: Windows-ize via `platformCopy`. did-not-take/failed →
  "...Kosmos is still on Y. Nothing else was changed." (design/brief); slow → the Windows
  "double-click Kosmos.exe in your Kosmos folder." instead of "open it again from Applications".
  Add table keys (`updateStillOn`, `updateSlowRemedy`).
- Card: no change needed — a normal win32 bundle now arrives with `update` (offer) set, so the
  existing offer arm paints "Version X is ready." [Update]; a OneDrive/Program Files bundle arrives
  with `updateManual` set and paints the existing manual arm. Staging label already handled.

### 5. Channel (design §6b) — confirm, don't rebuild
`KOSMOS_UPDATE_CHANNEL=staging` (win32 ignores AGENT_WORKFORCE_*), `KOSMOS_RELEASE_BASE` honoured,
channel in status + boot log, unreachable staging never falls back to prod. All S1; a test confirms
it holds once armed.

## Tests (sandboxed; scratch ROOT/anchor, stub scheduler + board prober; NEVER the real ones)
- `platform.js`: SELF_INSTALL now `['darwin','win32']`, frozen; `canSelfInstall('win32')` true.
- `installedRoot()`: bundle layout → bundle root; source checkout / non-bundle → null (THIS box).
- `beginInstall` win32: happy path reaches the `win32update.begin` seam with `root=installedRoot()`;
  a `{ok:false}` from begin releases single-flight and records `because`; auto sets backoff.
- Every B0 precondition + the OneDrive and Program Files refusals (env-var AND path-inside), via the
  real `win32update.begin`/`unusualLocationRefusal` against a scratch ROOT with a stub boardTask.
- `maybeAutoInstall`: default ON installs (seam called); off (`autoupdate.json`) suppresses; corrupt
  = OFF; backoff + single-flight hold; `liveExecutionAllowed()` false suppresses.
- Never downgrade: a not-newer offer/auto refuses (readOffer/newer already; assert armed path keeps it).
- Status carries channel + phase; win32 `update`/`updateManual` are mutually exclusive; the false
  "Up to date." is gone on win32.
- Source-checkout guard: on THIS box, `installedRoot()`/`begin()` refuse (a test proves it).
- Update the pins: `update.win32-570.test.js`, `update.win32-check.test.js` to the S4 truth; add the
  frozen-set + self-installs arms.
- UI: DOM-level tests (run on macOS CI, win32 forced) for the confirm body, the overlay lead
  ("Downloading X" → "Updating"), and the settle() failure/slow Windows copy; extend
  `web.win32-update-offer.test.js` for the offer/manual split. Browser-check arms
  (`render-update-win32-manual.js` extended or a new `render-update-win32-arm.js`) + README row +
  copy a passing check's `pageerror` handling.
- Revert controls: each new assertion goes red when its production line is hand-reverted (no
  `git checkout`/`restore` on working files; scratch copy or hand-edit + restore).

Run via PowerShell with `C:\Users\joshu\AppData\Local\Kosmos\runtime\node.exe` (v24.19), the
schtasks preload (`no-schtasks-preload.cjs`), `KOSMOS_SCHTASKS_BLOCK_LOG` + APPDATA/LOCALAPPDATA on
scratch. Compare the full suite against a `git archive` of origin/main by NAME and by first error
line for every test touching a changed function; grep source-extraction harnesses; confirm the
block log is empty for touched suites.

## Follow-up issues to file (do not build here)
- Authenticode signature check of the staged Kosmos.exe (blocked on Azure signing / DUNS). Leave a
  named TODO seam in win32update B4.
- Launcher-side double-click downgrade guard (an old Kosmos.exe re-pointing the fleet).
- S5 "Roll back" button (wire to the `previous-<from>` folder the armed success path keeps).

## V1-V4 live-check runbook (design §6c) — DO NOT RUN; coordinator runs after a staging build + Josh go
- **V1** sha matches the pointer + the `.sha256` sidecar for the staged versioned zip.
- **V2** unpack through Explorer (Mark of the Web set), run Z0-Z6 on the fresh install.
- **V3** a PROD install self-updates in-app with `KOSMOS_UPDATE_CHANNEL=staging`: board identity
  shows the new version, agent pids unchanged, Z0-Z6 pass, a reply reaches the board.
- **V4** write this box's verification record for the staged sha, then promote on Josh's explicit go
  (`--approved-sha`, staging-first).
- Needs Josh's explicit go (coordinator asks): anything touching the real install, `engine-path`,
  `\Kosmos\*` tasks, or the live board on 16180 — i.e. V2, V3, V4.

## Weakest premises
- The overlay's "Downloading → Updating" transition keys on board reachability, not on a phase read
  during the swap (the board is down then). Honest but coarse; recorded here.
- `updating.announce()` (the Mac "back in a moment" coordinator banner) is enrolment-gated and inert
  on win32, so the armed win32 path does not call it; no Windows banner exists yet.

## Review round 2 — Finding 2: the detached-helper single-flight witness
`beginWindowsInstall` holds `installStarted=true` on `{ok:true}` because the detached helper is about
to stop and restart this board. If the helper spawns then dies BEFORE it stops the board (early throw
/ missing dep after a clean spawn), nothing released the flag — the route answered every retry
idempotently and `maybeAutoInstall` was blocked until a board restart. `armWindowsHelperWitness` is
the win32 analogue of the Mac `wireChild` exit/error release (#2503/#988): a bounded, `unref`'d timer
(`windowsHelperWitnessMs` = `win32apply.STAGED_HELPER_STARTUP_GRACE_MS`, 2 min — the same window
begin() treats a just-started helper as "young", far above win32apply's 30s+1s board-stop budget, and
the download is already done before the helper spawns). When it fires it releases single-flight ONLY
if `updatePhase()` is still `staged` (the helper never began the swap). **Why it can't be fully
symmetric with the Mac:** begin() hands back only the helper's pid, not its child handle (detached,
spawned inside win32update), so there is no `child.on('exit'/'error')` to bind — the timer plus the
journal-phase check is the observable substitute. **Why it never trips the normal swap:** the helper's
first forward act is `setPhase('stopping')` then it ends the board, which in a real board kills this
process, so the timer never fires; if it does fire and the phase has advanced, the flag is left.
Tested both ways (`update.win32-arm.test.js`): helper dies before board-stop → released; swap in
progress (journal past `staged`) → not released; plus a revert control (disable the witness → the
release test reds).

**Round 3 hardening (defense in depth):** the witness now reads `windowsJournalRead()` (which
distinguishes `readJournal`'s `'none'` = no file from `'unreadable'` = a file present but torn) rather
than `updatePhase()` (which collapses both to null). It releases ONLY on a definitively absent (`none`)
or still-`staged` read; a present-but-unreadable journal (a writer replacing it mid-swap, a scanner
holding it) HOLDS the flag. This makes the in-memory flag independently correct rather than leaning on
the on-disk WORK lock — the true single-flight authority — that round 3 proved backstops it anyway (a
spurious release can only start a resumer, which serializes on that lock, so two concurrent swaps are
impossible). Tested (`a journal file present but torn/unreadable HOLDS single-flight`) with its own
revert control (restore the pre-hardening `updatePhase()`-based release → the torn-journal test reds).

## Release-safety property (ACCEPTED)
With auto-install ON (the fleet default) on the prod channel there is **no CODE-level staging-first
gate**: the moment the prod Windows pointer (`latest-win.json`) names a version newer than a box is
running, every armed prod win32 box with auto ON self-updates fleet-wide within ~15 min (the check
TTL) — identical to the Mac model. Merging S4 triggers nothing (auto fires only on a version strictly
newer than the running build, and no newer prod Windows build exists at merge). The exposure is the
NEXT prod pointer bump, which is gated by staging-first + Josh's explicit promote approval — a
PROCESS gate (win-staging-verified record + `--approved-sha`), not a code gate. Stated here so the
property is explicit rather than implicit. (Coordinator is confirming with Josh separately.)

## Not exercisable on this box (covered elsewhere)
- **The full `/api/status` field-inventory test** (`server.test.js` "no field the board sends is
  unknown to the page") needs a full board boot with the tmux stub, which cannot run on win32 — here
  it dies early on a 2-field payload. Covered by macOS CI (full >20-field payload) plus the static
  inventory replication done in round 1 (every S4-emitted key is referenced in index.html or excused,
  and `updatePhase` is now in `UNREAD_ON_PURPOSE`).
- **A real end-to-end swap** (begin → win32apply H1–H9 against a real ROOT/board) is barred by
  HARD SAFETY on this box. Covered by macOS CI for the platform-agnostic arms, the win32-arm seams
  (`setWindowsInstaller`, `setWindowsHelperWitnessMs`, scratch anchor/journal), and the coordinator's
  V1–V4 live checks on a staging build with Josh's go.
