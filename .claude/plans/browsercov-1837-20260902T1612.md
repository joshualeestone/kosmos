# browsercov-1837 -- rendered browser-check coverage for #1710 and #1786

Card: kosmos#1837. #1710 (disconnect-confirm `.acct-disconnect.armed` danger CSS,
merged 019f16cc) and #1786 (create-form whole-reset on role change, merged
07c11fa4) both changed `web/index.html` but shipped only source/unit coverage, no
`docs/browser-checks/` rendered assertion -- they bypassed the #1720 gate. This
adds the rendered coverage the gate wanted.

Blocker note: the card was filed "Blocked on #1769 (no agent can run a browser
check)." That is RESOLVED -- Josh confirmed on the card (2026-09-01) that
Playwright + both engines run on this box, and I have run headless checks twice
today (#1801 verification, #1845). So this is unblocked.

## #1710 -- extend render-accounts-openai.js (no new wiring)

`render-accounts-openai.js` already renders the Accounts page, completes first
run through the product route, and exercises the two-press disconnect confirm
(#1683/#1702): first press ARMS the button, second removes. So it already holds
the armed `.acct-disconnect` state -- the gap #1710 left is only that nobody
asserts the armed button PAINTS the danger treatment (CSS at index.html:578,
`.acct-disconnect.armed { font-weight: 600; color: var(--danger, #b3261e); }`).
The #1710 fix shipped a source-presence grep; the gate wanted a rendered/
computed-style check.

Add, at the point the check has armed a button (first press):
- read `getComputedStyle` `color` + `fontWeight` of the UNARMED button (control),
- click once to arm,
- assert the armed button's `fontWeight` is 600 AND its `color` is the danger
  color (resolve `--danger` / rgb(179,38,30)) and DIFFERS from the unarmed color.
Control is built in: if the `.armed` rule regressed (only the word changed, which
is exactly the #1710 defect), the armed color would equal the unarmed color and
the assertion fails. render-accounts-openai.js is chromium-only + computed-STATE
by design; computed `color`/`fontWeight` are style resolution (not compositing),
so they read reliably headless -- consistent with the file's existing stance.

## #1786 -- new check render-create-reset.js (wired into the runner)

`refillDetails(resetDirty)` (index.html:25108) is the single reset choke-point.
On a role CHANGE (roleChanged=true -> refillDetails(true)) it resets the whole
step-two field set (name, create-msg, avatar+input+hint, projects, mark seed,
pending avatar) plus the dirty-guarded label/instructions -- #1786 fixed a
half-reset where a role re-pick left name/avatar carrying the previous role's
entries. On a SAME-role re-pick (roleChanged=false -> refillDetails(false)) the
dirty label/instructions are preserved.

New focused check, modeled on render-create-form.js's boot (sandbox with first
run complete, step two reachable):
- open create, reach step two, type a name + edit instructions (make dirty),
- change to a DIFFERENT role -> assert name is cleared AND instructions refilled
  to the new template (whole reset happened),
- re-pick the SAME role -> assert an edited (dirty) instructions/label is
  PRESERVED (control for the other direction: the reset must NOT fire on a
  same-role re-pick).
Wire it into tools/browser-checks.sh (near the other create checks) and add it to
tools.browser-checks-wired.test.js's expected set. Dual-engine (chromium+webkit)
like render-create-form.js.

## Verification (measured, not assumed)

Boot a sandboxed board (AGENT_WORKFORCE_* tempdir + fake-tmux + DRY_RUN=1) and run
each check headless via pw-runtime. For #1710: confirm the armed assertion passes,
and prove the control by transiently reverting the `.armed` color rule (or reading
the unarmed color) so armed==unarmed reddens. For #1786: confirm reset-on-change
and preserve-on-same-role both pass; control by asserting the pre-change dirty
value is gone after a different-role change. Run tools.browser-checks-wired.test.js
after wiring the new check.

## Weakest premise (named) -- RESOLVED for #1710

That the Accounts page renders a forgettable `.acct-disconnect` button that arms
with a real danger-color rule. CONFIRMED by running: the WALK (OpenAI) row's
button arms, and computed style at armed is rgb(179,38,30)/600 vs rested
rgb(74,79,87)/400; stripping the .armed rule (the #1710 defect) reds the
assertion (exit 1). #1710 coverage is done and verified.

## What changed during execution -- #1786 is re-scoped (measured finding)

**#1786's role-change whole-reset has NO normal UI entry any more.** The
role-next handler comment (index.html:24685) records that the "Back then Next"
button was DELETED on 2026-08-19, and the role-change reset (refillDetails(true),
fired when FILLED_ROLE !== PICKED) is now "reached only by create-go's defensive
`cstep('role')` bail-out." create-go bails to the role step only on `!role`
(index.html:25299 -- an invalid/empty PICKED, an error path), not on a user
changing their mind about a role.

⇒ A rendered browser check of the whole-reset would have to DRIVE that defensive
bail-out (force `!role`, land back on the picker, pick a different role, re-advance)
-- a fragile test of a defensive path, not the "role change" a person performs.
The card assumed a normal role-change flow that effectively no longer exists.

**Decision (reversible, so I made it):** ship the #1710 rendered coverage now
(clean, high-value, verified) and leave #1786's rendered check as a documented
residual rather than build a fragile bail-out-driving check at the tail of this.
#1786 already carries a VM-extraction unit test of refillDetails; for a code path
with no normal UI entry, that unit test is arguably the right coverage and the
#1720 gate's want of a *rendered* check may not cleanly apply. The next person
should decide: drive the bail-out for a rendered check, or close the
rendered-check ask for #1786 as not-applicable given the deleted Back button.
This PR therefore `Addresses #1837` (does not close it) and names the #1786
residual explicitly.

**What would change my mind:** a real, non-defensive UI path back to the role
picker (e.g. if a Back button is restored), which would make a normal rendered
role-change check straightforward and worth adding.
