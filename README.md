# Screenshots for #718 (Scorpion, agent chat on a phone)

Images only, never merged. Offline sample data rendered from file:// with fetch stubbed; every shot passed a privacy tripwire (no network, no real names or accounts in the page). WebKit is an engine approximation, not Safari.
