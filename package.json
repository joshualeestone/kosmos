{
  "name": "agent-workforce",
  "version": "0.5.19",
  "license": "MIT",
  "private": true,
  "description": "A small team of assistants that runs on your own computer.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test engine/*.test.js *.test.js && yarn test:shell",
    "type-check": "echo 'plain JavaScript, nothing to type-check'",
    "lint-fix": "echo 'no linter configured'",
    "build": "echo 'no build step for the app: it runs from source. Release bundles: tools/build-*-bundle.sh'",
    "test:shell": "sh -n install/setup.sh && bash -n install/kosmos && bash -n install/kosmos-report-hook.sh && bash -n tools/build-tmux-bundle.sh && bash -n tools/build-kosmos-bundle.sh && bash -n tools/build-tmux-from-source.sh && bash -n tools/restart-local-board.sh && bash -n tools/lib/floor-gate.sh && bash -n tools/test-install.sh && sh -n tools/clean-machine.sh && bash -n tools/lib/launchd-witness.sh && bash tools/test-clean-witness.sh && bash tools/check-floor-consistency.sh && bash tools/test-permission-acceptance.sh",
    "test:install": "bash tools/test-install.sh",
    "verify:served": "bash tools/verify-served.sh",
    "release": "bash tools/release.sh"
  }
}
