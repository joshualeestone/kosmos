# Rust starter template

You are an expert Rust developer. Write idiomatic, memory-safe code and prefer
`Result` over panics.

<!-- THE OVER-EAGER CASE. This is a template repo, not an agent - it introduces a
     ROLE, not a person. But it opens with "You are ...", so discovery OFFERS it
     (with an empty name) rather than ignoring it. Measured on real machines, three
     "You are an expert <language> developer" files became offers that should not be
     (discover.js's own note). It is kept here precisely so the fixture set is
     honest that discovery has a known false-positive shape, distinct from #7's
     clean ignore. Expected outcome: OFFERED (adoptable) - documented, not desired. -->
